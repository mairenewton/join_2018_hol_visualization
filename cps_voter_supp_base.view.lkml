view: CPS_Voting_and_Registration {
  sql_table_name: census.cps_final ;;

  dimension: hehousut {
    label: "Type of living quarters"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hehousut = 0
          ;;
        label: "Other Unit"
      }

      when: {
        sql: ${TABLE}.hehousut = 1
          ;;
        label: "House, Apartment, Flat"
      }

      when: {
        sql: ${TABLE}.hehousut = 2
          ;;
        label: "Hu In Nontransient Hotel, Motel, Etc."
      }

      when: {
        sql: ${TABLE}.hehousut = 3
          ;;
        label: "Hu Permanent In Transient Hotel, Motel"
      }

      when: {
        sql: ${TABLE}.hehousut = 4
          ;;
        label: "Hu In Rooming House"
      }

      when: {
        sql: ${TABLE}.hehousut = 5
          ;;
        label: "Mobile Home Or Trlr W/No Perm Rm Added"
      }

      when: {
        sql: ${TABLE}.hehousut = 6
          ;;
        label: "Mh Or Trlr W/1 Or More Perm Rms Added"
      }

      when: {
        sql: ${TABLE}.hehousut = 7
          ;;
        label: "Hu Not Specified Above"
      }

      when: {
        sql: ${TABLE}.hehousut = 8
          ;;
        label: "Quarters Not Hu In Rooming Or Brding Hs"
      }

      when: {
        sql: ${TABLE}.hehousut = 9
          ;;
        label: "Unit Not Perm. In Transient Hotl, Motl"
      }

      when: {
        sql: ${TABLE}.hehousut = 10
          ;;
        label: "Unoccupied Tent Site Or Trlr Site"
      }

      when: {
        sql: ${TABLE}.hehousut = 11
          ;;
        label: "Student Quarters In College Dorm"
      }

      when: {
        sql: ${TABLE}.hehousut = 12
          ;;
        label: "Other Unit Not Specified Above"
      }
    }
  }

  dimension: hephoneo {
    label: "Phone interview acceptable,y/n"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hephoneo = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.hephoneo = 0
          ;;
        label: "Unknown"
      }

      when: {
        sql: ${TABLE}.hephoneo = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.hephoneo = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: hetelavl {
    label: "No residential phone,available elsewhere,y/n"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hetelavl = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hetelavl = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hetelavl = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hetelavl = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.hetelavl = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: hetelhhd {
    label: "Phone in living quarters,y/n"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hetelhhd = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hetelhhd = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hetelhhd = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hetelhhd = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.hetelhhd = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: hetenure {
    label: "Own/rent living quarters"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hetenure = -2
          ;;
        label: "Not in universe"
      }

      when: {
        sql: ${TABLE}.hetenure = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.hetenure = 1
          ;;
        label: "Owned Or Being Bought By A Hh Member"
      }

      when: {
        sql: ${TABLE}.hetenure = 2
          ;;
        label: "Rented for cash"
      }

      when: {
        sql: ${TABLE}.hetenure = 3
          ;;
        label: "Occupied without payment of cash rent"
      }
    }
  }

  dimension: hrhtype {
    label: "Type of family/single individual"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hrhtype = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.hrhtype = 0
          ;;
        label: "Non-Interview Household"
      }

      when: {
        sql: ${TABLE}.hrhtype = 1
          ;;
        label: "Husband/Wife Primary Family(neither AF)"
      }

      when: {
        sql: ${TABLE}.hrhtype = 2
          ;;
        label: "Husb/Wife Prim. Family(either/Both AF)"
      }

      when: {
        sql: ${TABLE}.hrhtype = 3
          ;;
        label: "Unmarried Civilian Male-Prim Fam Hhlder"
      }

      when: {
        sql: ${TABLE}.hrhtype = 4
          ;;
        label: "Unmarried Civ. Female-Prim Fam Hhlder"
      }

      when: {
        sql: ${TABLE}.hrhtype = 5
          ;;
        label: "Primary Family Hhlder-Rp In AF,unmar."
      }

      when: {
        sql: ${TABLE}.hrhtype = 6
          ;;
        label: "Civilian Male Primary Individual"
      }

      when: {
        sql: ${TABLE}.hrhtype = 7
          ;;
        label: "Civilian Female Primary Individual"
      }

      when: {
        sql: ${TABLE}.hrhtype = 8
          ;;
        label: "Primary Individual Hhld-Rp In AF"
      }

      when: {
        sql: ${TABLE}.hrhtype = 9
          ;;
        label: "Group Quarters With Family"
      }

      when: {
        sql: ${TABLE}.hrhtype = 10
          ;;
        label: "Grp Quarters Without Family"
      }
    }
  }

  dimension: hrintsta {
    label: "Interview/non-interview status"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hrintsta = 1
          ;;
        label: "Interview"
      }

      when: {
        sql: ${TABLE}.hrintsta = 2
          ;;
        label: "Type A Non-Interview"
      }

      when: {
        sql: ${TABLE}.hrintsta = 3
          ;;
        label: "Type B Non-Interview"
      }

      when: {
        sql: ${TABLE}.hrintsta = 4
          ;;
        label: "Type C Non-Interview"
      }
    }
  }

  dimension: hrlonglk {
    label: "Longitudinal link indicator"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hrlonglk = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.hrlonglk = 0
          ;;
        label: "MIS 1 Or Replacement HH (no Link)"
      }

      when: {
        sql: ${TABLE}.hrlonglk = 2
          ;;
        label: "MIS 2-4 Or MIS 6-8 (link To Db02)"
      }

      when: {
        sql: ${TABLE}.hrlonglk = 3
          ;;
        label: "MIS 5 (link To Db03)"
      }
    }
  }

  dimension: hrmis {
    label: "Month-in-sample"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hrmis = 1
          ;;
        label: "MIS 1"
      }

      when: {
        sql: ${TABLE}.hrmis = 2
          ;;
        label: "MIS 2"
      }

      when: {
        sql: ${TABLE}.hrmis = 3
          ;;
        label: "MIS 3"
      }

      when: {
        sql: ${TABLE}.hrmis = 4
          ;;
        label: "MIS 4"
      }

      when: {
        sql: ${TABLE}.hrmis = 5
          ;;
        label: "MIS 5"
      }

      when: {
        sql: ${TABLE}.hrmis = 6
          ;;
        label: "MIS 6"
      }

      when: {
        sql: ${TABLE}.hrmis = 7
          ;;
        label: "MIS 7"
      }

      when: {
        sql: ${TABLE}.hrmis = 8
          ;;
        label: "MIS 8"
      }
    }
  }

  dimension: hrmonth {
    label: "Month of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hrmonth = 1
          ;;
        label: "January"
      }

      when: {
        sql: ${TABLE}.hrmonth = 2
          ;;
        label: "February"
      }

      when: {
        sql: ${TABLE}.hrmonth = 3
          ;;
        label: "March"
      }

      when: {
        sql: ${TABLE}.hrmonth = 4
          ;;
        label: "April"
      }

      when: {
        sql: ${TABLE}.hrmonth = 5
          ;;
        label: "May"
      }

      when: {
        sql: ${TABLE}.hrmonth = 6
          ;;
        label: "June"
      }

      when: {
        sql: ${TABLE}.hrmonth = 7
          ;;
        label: "July"
      }

      when: {
        sql: ${TABLE}.hrmonth = 8
          ;;
        label: "August"
      }

      when: {
        sql: ${TABLE}.hrmonth = 9
          ;;
        label: "September"
      }

      when: {
        sql: ${TABLE}.hrmonth = 10
          ;;
        label: "October"
      }

      when: {
        sql: ${TABLE}.hrmonth = 11
          ;;
        label: "November"
      }

      when: {
        sql: ${TABLE}.hrmonth = 12
          ;;
        label: "December"
      }
    }
  }

  dimension: hrnumhou {
    label: "Total # of members"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [
      0,
      1,
      2,
      3,
      5,
      6,
      10
    ]
    style: integer
    sql: CASE WHEN ${TABLE}.hrnumhou between 0 AND 16 THEN ${TABLE}.hrnumhou END ;;
  }

  dimension: hubus {
    label: "Presence of business/farm in hhld,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hubus = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hubus = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hubus = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hubus = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.hubus = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: hubusl1 {
    label: "Line numbers of business/farm owners 1"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.hubusl1 between 1 AND 99 THEN ${TABLE}.hubusl1 END ;;
  }

  dimension: hubusl2 {
    label: "Line numbers of business/farm owners 2"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [1, 4, 7, 10, 13]
    style: classic
    sql: CASE WHEN ${TABLE}.hubusl2 between 1 AND 12 THEN ${TABLE}.hubusl2 END ;;
  }

  dimension: hubusl3 {
    label: "Line numbers of business/farm owners 3"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [1, 4, 7, 10, 13]
    style: classic
    sql: CASE WHEN ${TABLE}.hubusl3 between 1 AND 12 THEN ${TABLE}.hubusl3 END ;;
  }

  dimension: hubusl4 {
    label: "Line numbers of business/farm owners 4"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [1, 4, 7, 10, 13]
    style: classic
    sql: CASE WHEN ${TABLE}.hubusl4 between 1 AND 12 THEN ${TABLE}.hubusl4 END ;;
  }

  dimension: huinttyp {
    label: "Type of interview,personal/telephone"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.huinttyp = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.huinttyp = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.huinttyp = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.huinttyp = 1
          ;;
        label: "Personal"
      }

      when: {
        sql: ${TABLE}.huinttyp = 2
          ;;
        label: "Telephone"
      }
    }
  }

  dimension: hurespli {
    label: "Line number of current respondent"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.hurespli between 0 AND 99 THEN ${TABLE}.hurespli END ;;
  }

  dimension: hutypb {
    label: "Type b non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypb = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypb = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypb = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypb = 1
          ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hutypb = 2
          ;;
        label: "Temporarily Occupied By Persons W/ URE"
      }

      when: {
        sql: ${TABLE}.hutypb = 3
          ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hutypb = 4
          ;;
        label: "Unfit or to be demolished"
      }

      when: {
        sql: ${TABLE}.hutypb = 5
          ;;
        label: "Under construction, not ready"
      }

      when: {
        sql: ${TABLE}.hutypb = 6
          ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypb = 7
          ;;
        label: "Unoccupied Tent Site Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hutypb = 8
          ;;
        label: "Permit Granted Construction Not Started"
      }

      when: {
        sql: ${TABLE}.hutypb = 9
          ;;
        label: "Other Type B - Specify"
      }
    }
  }

  dimension: peafnow {
    label: "Currently in armed forces,y/n"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafnow = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peafnow = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peafnow = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peeduca {
    label: "Highest level of school completed"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peeduca = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peeduca = 31
          ;;
        label: "Less Than 1st Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 32
          ;;
        label: "1st,2nd,3rd Or 4th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 33
          ;;
        label: "5th Or 6th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 34
          ;;
        label: "7th Or 8th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 35
          ;;
        label: "9th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 36
          ;;
        label: "10th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 37
          ;;
        label: "11th Grade"
      }

      when: {
        sql: ${TABLE}.peeduca = 38
          ;;
        label: "12th Grade No Diploma"
      }

      when: {
        sql: ${TABLE}.peeduca = 39
          ;;
        label: "High School Grad-Diploma Or Equiv (ged)"
      }

      when: {
        sql: ${TABLE}.peeduca = 40
          ;;
        label: "Some College But No Degree"
      }

      when: {
        sql: ${TABLE}.peeduca = 41
          ;;
        label: "Associate Degree-Occupational/Vocationl"
      }

      when: {
        sql: ${TABLE}.peeduca = 42
          ;;
        label: "Associate Deg.-Academic Program"
      }

      when: {
        sql: ${TABLE}.peeduca = 43
          ;;
        label: "Bachelor's Degree(ex\":\"ba,ab,bs)"
      }

      when: {
        sql: ${TABLE}.peeduca = 44
          ;;
        label: "MASTER'S DEGREE(EX\":\"MA,MS,MEng,MEd,MSW)"
      }

      when: {
        sql: ${TABLE}.peeduca = 45
          ;;
        label: "Professional School Deg(ex\":\"md,dds,dvm)"
      }

      when: {
        sql: ${TABLE}.peeduca = 46
          ;;
        label: "DOCTORATE DEGREE(EX\":\"PhD,EdD)"
      }
    }
  }

  dimension: pemaritl {
    label: "Marital status"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemaritl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pemaritl = 1
          ;;
        label: "Married - Spouse Present"
      }

      when: {
        sql: ${TABLE}.pemaritl = 2
          ;;
        label: "Married-Spouse Absent"
      }

      when: {
        sql: ${TABLE}.pemaritl = 3
          ;;
        label: "Widowed"
      }

      when: {
        sql: ${TABLE}.pemaritl = 4
          ;;
        label: "Divorced"
      }

      when: {
        sql: ${TABLE}.pemaritl = 5
          ;;
        label: "Separated"
      }

      when: {
        sql: ${TABLE}.pemaritl = 6
          ;;
        label: "Never Married"
      }
    }
  }

  dimension: peparent {
    label: "Parent line number"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.peparent between 0 AND 99 THEN ${TABLE}.peparent END ;;
  }

  dimension: perrp {
    label: "Relationship to reference person"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.perrp = 1
          ;;
        label: "Ref Pers with other relativew in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 2
          ;;
        label: "REF PERS WITH NO OTHER RELATIVES IN HH"
      }

      when: {
        sql: ${TABLE}.perrp = 3
          ;;
        label: "SPOUSE"
      }

      when: {
        sql: ${TABLE}.perrp = 4
          ;;
        label: "CHILD"
      }

      when: {
        sql: ${TABLE}.perrp = 5
          ;;
        label: "GRANDCHILD"
      }

      when: {
        sql: ${TABLE}.perrp = 6
          ;;
        label: "PARENT"
      }

      when: {
        sql: ${TABLE}.perrp = 7
          ;;
        label: "BROTHER/SISTER"
      }

      when: {
        sql: ${TABLE}.perrp = 8
          ;;
        label: "OTHER RELATIVE"
      }

      when: {
        sql: ${TABLE}.perrp = 9
          ;;
        label: "FOSTER CHILD"
      }

      when: {
        sql: ${TABLE}.perrp = 10
          ;;
        label: "Non-rel of ref. per w/own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 11
          ;;
        label: "Not used"
      }

      when: {
        sql: ${TABLE}.perrp = 12
          ;;
        label: "NON-REL OF REF PER W/NO OWN RELS IN HH"
      }

      when: {
        sql: ${TABLE}.perrp = 13
          ;;
        label: "Unmarried partner w/ own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 14
          ;;
        label: "Unmar. partner w/ no own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 15
          ;;
        label: "Housemate/roommate w/ own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 16
          ;;
        label: "Hsemate/roommate w/ no own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 17
          ;;
        label: "Roomer/boarder w/ own rels in HH"
      }

      when: {
        sql: ${TABLE}.perrp = 18
          ;;
        label: "Roomer/brder w/ no own rels in HH"
      }
    }
  }

  dimension: pesex {
    label: "Sex"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pesex = 1
          ;;
        label: "Male"
      }

      when: {
        sql: ${TABLE}.pesex = 2
          ;;
        label: "Female"
      }
    }
  }

  dimension: pespouse {
    label: "Spouse line number"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pespouse between 1 AND 99 THEN ${TABLE}.pespouse END ;;
  }

  dimension: prcitflg {
    label: "Citizenship allocation flag"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcitflg = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prcitflg = 0
          ;;
        label: "Value - No Change"
      }

      when: {
        sql: ${TABLE}.prcitflg = 1
          ;;
        label: "Blank - No Change"
      }

      when: {
        sql: ${TABLE}.prcitflg = 2
          ;;
        label: "Don't Know - No Change"
      }

      when: {
        sql: ${TABLE}.prcitflg = 3
          ;;
        label: "Refused - No Change"
      }

      when: {
        sql: ${TABLE}.prcitflg = 10
          ;;
        label: "Value To Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 11
          ;;
        label: "Blank To Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 12
          ;;
        label: "Don't Know To Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 13
          ;;
        label: "Refused To Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 20
          ;;
        label: "Value To Longitudinal Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 21
          ;;
        label: "Blank To Longitudinal Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 22
          ;;
        label: "Don't Know To Longitudinal Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 23
          ;;
        label: "Refused To Longitudinal Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 30
          ;;
        label: "Value to Allocated Value Long."
      }

      when: {
        sql: ${TABLE}.prcitflg = 31
          ;;
        label: "Blank to Allocated Value Long."
      }

      when: {
        sql: ${TABLE}.prcitflg = 32
          ;;
        label: "Don't Know to Allocated Value Long."
      }

      when: {
        sql: ${TABLE}.prcitflg = 33
          ;;
        label: "Refused to Allocated Value Long."
      }

      when: {
        sql: ${TABLE}.prcitflg = 40
          ;;
        label: "Value To Allocated Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 41
          ;;
        label: "Blank To Allocated Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 42
          ;;
        label: "Don't Know To Allocated Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 43
          ;;
        label: "Refused To Allocated Value"
      }

      when: {
        sql: ${TABLE}.prcitflg = 50
          ;;
        label: "Value To Blank"
      }

      when: {
        sql: ${TABLE}.prcitflg = 52
          ;;
        label: "Don't Know To Blank"
      }

      when: {
        sql: ${TABLE}.prcitflg = 53
          ;;
        label: "Refused To Blank"
      }
    }
  }

  dimension: prcitshp {
    label: "United states citizenship group"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcitshp = 1
          ;;
        label: "Native, Born In US"
      }

      when: {
        sql: ${TABLE}.prcitshp = 2
          ;;
        label: "Native, Born in PR or US Outlying Area"
      }

      when: {
        sql: ${TABLE}.prcitshp = 3
          ;;
        label: "Native, Born Abroad Of US Parent(s)"
      }

      when: {
        sql: ${TABLE}.prcitshp = 4
          ;;
        label: "Foreign Born, US Cit By Naturalization"
      }

      when: {
        sql: ${TABLE}.prcitshp = 5
          ;;
        label: "Foreign Born, Not a US Citizen"
      }
    }
  }

  dimension: prfamnum {
    label: "Family number"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prfamnum = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prfamnum = 0
          ;;
        label: "Not a family member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 1
          ;;
        label: "Primary family member only"
      }

      when: {
        sql: ${TABLE}.prfamnum = 2
          ;;
        label: "Subfamily No. 2 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 3
          ;;
        label: "Subfamily No. 3 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 4
          ;;
        label: "Subfamily No. 4 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 5
          ;;
        label: "Subfamily No. 5 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 6
          ;;
        label: "Subfamily No. 6 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 7
          ;;
        label: "Subfamily No. 7 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 8
          ;;
        label: "Subfamily No. 8 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 9
          ;;
        label: "Subfamily No. 9 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 10
          ;;
        label: "Subfamily No. 10 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 11
          ;;
        label: "Subfamily No. 11 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 12
          ;;
        label: "Subfamily No. 12 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 13
          ;;
        label: "Subfamily No. 13 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 14
          ;;
        label: "Subfamily No. 14 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 15
          ;;
        label: "Subfamily No. 15 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 16
          ;;
        label: "Subfamily No. 16 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 17
          ;;
        label: "Subfamily No. 17 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 18
          ;;
        label: "Subfamily No. 18 Member"
      }

      when: {
        sql: ${TABLE}.prfamnum = 19
          ;;
        label: "Subfamily No. 19 Member"
      }
    }
  }

  dimension: prfamrel {
    label: "Family relationship(recode)"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prfamrel = 0
          ;;
        label: "Not a family member"
      }

      when: {
        sql: ${TABLE}.prfamrel = 1
          ;;
        label: "Reference person"
      }

      when: {
        sql: ${TABLE}.prfamrel = 2
          ;;
        label: "Spouse"
      }

      when: {
        sql: ${TABLE}.prfamrel = 3
          ;;
        label: "Child"
      }

      when: {
        sql: ${TABLE}.prfamrel = 4
          ;;
        label: "Other Relative (primary Family only)"
      }
    }
  }

  dimension: prfamtyp {
    label: "Family type(recode)"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prfamtyp = 1
          ;;
        label: "Primary family"
      }

      when: {
        sql: ${TABLE}.prfamtyp = 2
          ;;
        label: "Primary Individual"
      }

      when: {
        sql: ${TABLE}.prfamtyp = 3
          ;;
        label: "Related subfamily"
      }

      when: {
        sql: ${TABLE}.prfamtyp = 4
          ;;
        label: "Unrelated subfamily"
      }

      when: {
        sql: ${TABLE}.prfamtyp = 5
          ;;
        label: "Secondary individual"
      }
    }
  }

  dimension: prmarsta {
    label: "Marital status(recode)"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmarsta = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmarsta = 1
          ;;
        label: "Married, Civilian Spouse Present"
      }

      when: {
        sql: ${TABLE}.prmarsta = 2
          ;;
        label: "Married, Armed Forces Spouse Present"
      }

      when: {
        sql: ${TABLE}.prmarsta = 3
          ;;
        label: "Married, Spouse Absent (exc. Separated)"
      }

      when: {
        sql: ${TABLE}.prmarsta = 4
          ;;
        label: "Widowed"
      }

      when: {
        sql: ${TABLE}.prmarsta = 5
          ;;
        label: "Divorced"
      }

      when: {
        sql: ${TABLE}.prmarsta = 6
          ;;
        label: "Separated"
      }

      when: {
        sql: ${TABLE}.prmarsta = 7
          ;;
        label: "Never married"
      }
    }
  }

  dimension: prpertyp {
    label: "Person type(recode)"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prpertyp = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prpertyp = 1
          ;;
        label: "Child Household Member"
      }

      when: {
        sql: ${TABLE}.prpertyp = 2
          ;;
        label: "Adult Civilian Household Member"
      }

      when: {
        sql: ${TABLE}.prpertyp = 3
          ;;
        label: "Adult Armed Forces Household Member"
      }

      when: {
        sql: ${TABLE}.prpertyp = 4
          ;;
        label: "Non-Household Member"
      }
    }
  }

  dimension: prtage {
    label: "Demographics - age topcoded at 85, 90 or 80 (see full description)"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 23, 45, 68, 90]
    style: classic
    sql: CASE WHEN ${TABLE}.prtage between 0 AND 90 THEN ${TABLE}.prtage END ;;
  }

  dimension: prtfage {
    label: "Top coded flag for age"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prtfage = 0
          ;;
        label: "No Top Code"
      }

      when: {
        sql: ${TABLE}.prtfage = 1
          ;;
        label: "Top Coded Value For Age"
      }
    }
  }

  dimension: puchinhh {
    label: "Reason for the changes in household composition"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puchinhh = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puchinhh = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puchinhh = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puchinhh = 1
          ;;
        label: "Person Added"
      }

      when: {
        sql: ${TABLE}.puchinhh = 2
          ;;
        label: "Person Added - URE"
      }

      when: {
        sql: ${TABLE}.puchinhh = 3
          ;;
        label: "Person Undeleted"
      }

      when: {
        sql: ${TABLE}.puchinhh = 4
          ;;
        label: "Person Died"
      }

      when: {
        sql: ${TABLE}.puchinhh = 5
          ;;
        label: "Deleted For Reason Other Than Death"
      }

      when: {
        sql: ${TABLE}.puchinhh = 6
          ;;
        label: "Person Joined Armed Forces"
      }

      when: {
        sql: ${TABLE}.puchinhh = 7
          ;;
        label: "Person No Longer In AF"
      }

      when: {
        sql: ${TABLE}.puchinhh = 9
          ;;
        label: "Change In Demographic Information"
      }
    }
  }

  dimension: pulineno {
    label: "Line number"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pulineno between 1 AND 99 THEN ${TABLE}.pulineno END ;;
  }

  dimension: peabspdo {
    label: "Paid absence from work,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peabspdo = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peabspdo = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peabspdo = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peabsrsn {
    label: "Reason for work absence last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peabsrsn = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 1
          ;;
        label: "On Layoff"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 2
          ;;
        label: "Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 3
          ;;
        label: "Waiting For A New Job To Begin"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 4
          ;;
        label: "Vacation/Personal Days"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 5
          ;;
        label: "Own Illness/Injury/Medical Problems"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 6
          ;;
        label: "Child Care Problems"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 7
          ;;
        label: "Other Family/Personal Obligation"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 8
          ;;
        label: "Maternity/Paternity Leave"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 9
          ;;
        label: "Labor Dispute"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 10
          ;;
        label: "Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 11
          ;;
        label: "School/Training"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 12
          ;;
        label: "Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 13
          ;;
        label: "Does Not Work In The Business"
      }

      when: {
        sql: ${TABLE}.peabsrsn = 14
          ;;
        label: "Other (specify)"
      }
    }
  }

  dimension: pedw4wk {
    label: "(not in,discouraged)worked in last 4 weeks"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedw4wk = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedw4wk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedw4wk = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pedwavl {
    label: "(not in,discouraged)available for job last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwavl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwavl = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedwavl = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pedwavr {
    label: "(not in,discouraged)reason can't work"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwavr = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwavr = 1
          ;;
        label: "Own Temporary Illness"
      }

      when: {
        sql: ${TABLE}.pedwavr = 2
          ;;
        label: "Going To School"
      }

      when: {
        sql: ${TABLE}.pedwavr = 3
          ;;
        label: "Other"
      }
    }
  }

  dimension: pedwlko {
    label: "(not in,discouraged)look for work in past year"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwlko = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwlko = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedwlko = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pedwlkwk {
    label: "(not in,discouraged)looked since last job,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwlkwk = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwlkwk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedwlkwk = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pedwrsn {
    label: "(not in,discouraged)reason not looking"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwrsn = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 1
          ;;
        label: "Believes No Wrk Avl In Line Lk Or Area"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 2
          ;;
        label: "Couldn't Find Any Work"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 3
          ;;
        label: "Lacks Necessary Schooling/Training"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 4
          ;;
        label: "Employers Think Too Young Or Too Old"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 5
          ;;
        label: "Other Types Of Discrimination"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 6
          ;;
        label: "Can't Arrange Child Care"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 7
          ;;
        label: "Family Responsibilities"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 8
          ;;
        label: "In School Or Other Training"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 9
          ;;
        label: "Ill-Health, Physical Disability"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 10
          ;;
        label: "Transportation Problems"
      }

      when: {
        sql: ${TABLE}.pedwrsn = 11
          ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: pedwwk {
    label: "(not in,discouraged)worked in past year,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwwk = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwwk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedwwk = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pedwwnto {
    label: "(not in,discouraged)wanted a job,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedwwnto = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedwwnto = 1
          ;;
        label: "Yes, Or Maybe, It Depends"
      }

      when: {
        sql: ${TABLE}.pedwwnto = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pedwwnto = 3
          ;;
        label: "Retired"
      }

      when: {
        sql: ${TABLE}.pedwwnto = 4
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.pedwwnto = 5
          ;;
        label: "Unable"
      }
    }
  }

  dimension: pehract1 {
    label: "# hours actually worked at main job"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      0,
      10,
      20,
      30,
      40,
      50,
      75,
      100
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pehract1 between 0 AND 99 THEN ${TABLE}.pehract1 END ;;
  }

  dimension: pehract2 {
    label: "# hours actually worked at other job(s)"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      0,
      10,
      20,
      30,
      40,
      50,
      75,
      100
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pehract2 between 0 AND 99 THEN ${TABLE}.pehract2 END ;;
  }

  dimension: pehractt {
    label: "# hours actually worked at all jobs"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 50, 99, 149, 198]
    style: classic
    sql: CASE WHEN ${TABLE}.pehractt between 0 AND 198 THEN ${TABLE}.pehractt END ;;
  }

  dimension: pehravl {
    label: "(part-timer)available for full-time,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehravl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehravl = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pehravl = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pehrftpt {
    label: "Usually work full-time,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehrftpt = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehrftpt = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pehrftpt = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pehrftpt = 3
          ;;
        label: "Hours Vary"
      }
    }
  }

  dimension: pehrrsn1 {
    label: "(part-timer)reason"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehrrsn1 = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 1
          ;;
        label: "Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 2
          ;;
        label: "Could Only Find Part-Time Work"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 3
          ;;
        label: "Seasonal Work"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 4
          ;;
        label: "Child Care Problems"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 5
          ;;
        label: "Other Family/Personal Obligations"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 6
          ;;
        label: "Health/Medical Limitations"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 7
          ;;
        label: "School/Training"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 8
          ;;
        label: "Retired/Social Security Limit On Earnin"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 9
          ;;
        label: "Full-Time Workweek Is Less Than 35 Hrs"
      }

      when: {
        sql: ${TABLE}.pehrrsn1 = 10
          ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: pehrrsn2 {
    label: "(part-timer)reason not full-time"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehrrsn2 = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 1
          ;;
        label: "Child Care Problems"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 2
          ;;
        label: "Other Family/Personal Obligations"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 3
          ;;
        label: "Health/Medical Limitiations"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 4
          ;;
        label: "School/Training"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 5
          ;;
        label: "Retired/Social Security Limit On Earnin"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 6
          ;;
        label: "Full-Time Workweek Less Than 35 Hours"
      }

      when: {
        sql: ${TABLE}.pehrrsn2 = 7
          ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: pehrrsn3 {
    label: "(full-timer)reason part-time last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehrrsn3 = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 1
          ;;
        label: "Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 2
          ;;
        label: "Seasonal Work"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 3
          ;;
        label: "Job Started Or Ended During Week"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 4
          ;;
        label: "Vacation/Personal Day"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 5
          ;;
        label: "Own Illness/Injury/Medical Appointment"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 6
          ;;
        label: "Holiday (legal Or Religious)"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 7
          ;;
        label: "Child Care Problems"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 8
          ;;
        label: "Other Family/Personal Obligations"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 9
          ;;
        label: "Labor Dispute"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 10
          ;;
        label: "Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 11
          ;;
        label: "School/Training"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 12
          ;;
        label: "Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.pehrrsn3 = 13
          ;;
        label: "Other Reason"
      }
    }
  }

  dimension: pehrusl1 {
    label: "# hours usually worked at main job"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      0,
      10,
      20,
      30,
      40,
      50,
      75,
      100
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pehrusl1 between 0 AND 99 THEN ${TABLE}.pehrusl1 END ;;
  }

  dimension: pehrusl2 {
    label: "# hours usually worked at other job(s)"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      0,
      10,
      20,
      30,
      40,
      50,
      75,
      100
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pehrusl2 between 0 AND 99 THEN ${TABLE}.pehrusl2 END ;;
  }

  dimension: pehruslt {
    label: "# hours usually worked at all jobs"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      0,
      10,
      20,
      30,
      40,
      50,
      75,
      100
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pehruslt between 0 AND 198 THEN ${TABLE}.pehruslt END ;;
  }

  dimension: pehrwant {
    label: "Full-time work desired,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehrwant = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehrwant = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pehrwant = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pehrwant = 3
          ;;
        label: "Regular Hours Are Full-Time"
      }
    }
  }

  dimension: pejhrsn {
    label: "(not in,job history)reason left last job"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pejhrsn = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 1
          ;;
        label: "Personal/Family (including Pregnancy)"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 2
          ;;
        label: "Return To School"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 3
          ;;
        label: "Health"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 4
          ;;
        label: "Retirement Or Old Age"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 5
          ;;
        label: "Temp, Seasonal Or Intermitt. Job Compl."
      }

      when: {
        sql: ${TABLE}.pejhrsn = 6
          ;;
        label: "Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 7
          ;;
        label: "Unsatis. Wrk. Arrangements(hrs,pay,etc)"
      }

      when: {
        sql: ${TABLE}.pejhrsn = 8
          ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: pejhwant {
    label: "(not in,job history)look for work in next year"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pejhwant = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pejhwant = 1
          ;;
        label: "Yes, Or It Depends"
      }

      when: {
        sql: ${TABLE}.pejhwant = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pejhwko {
    label: "(not in,job history)worked in past year,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pejhwko = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pejhwko = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pejhwko = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pelayavl {
    label: "(layoff)available for work if recalled,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelayavl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelayavl = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pelayavl = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pelaydur {
    label: "(layoff)# weeks looking for job"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [
      1,
      4,
      8,
      16,
      24,
      36,
      52,
      76,
      104
    ]
    style: classic
    sql: CASE WHEN ${TABLE}.pelaydur between 1 AND 260 THEN ${TABLE}.pelaydur END ;;
  }

  dimension: pelayfto {
    label: "(layoff)from full-time job,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelayfto = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelayfto = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pelayfto = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pelaylk {
    label: "(layoff)called back,still looking for work,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelaylk = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelaylk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pelaylk = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pelkavl {
    label: "(unemployed)available for work last week,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelkavl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelkavl = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pelkavl = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pelkdur {
    label: "(unemployed)#weeks on job search"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 250, 500, 750, 999]
    style: classic
    sql: CASE WHEN ${TABLE}.pelkdur between 0 AND 999 THEN ${TABLE}.pelkdur END ;;
  }

  dimension: pelkfto {
    label: "(unemployed)looking-full-time work wanted,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelkfto = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelkfto = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pelkfto = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pelkfto = 3
          ;;
        label: "Doesn't Matter"
      }
    }
  }

  dimension: pelkll1o {
    label: "(unemployed)looking-activity before search"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelkll1o = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelkll1o = 1
          ;;
        label: "Working"
      }

      when: {
        sql: ${TABLE}.pelkll1o = 2
          ;;
        label: "School"
      }

      when: {
        sql: ${TABLE}.pelkll1o = 3
          ;;
        label: "Left Military Service"
      }

      when: {
        sql: ${TABLE}.pelkll1o = 4
          ;;
        label: "Something Else"
      }
    }
  }

  dimension: pelkll2o {
    label: "(unemployed)looking-lost/quit job"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelkll2o = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelkll2o = 1
          ;;
        label: "Lost Job"
      }

      when: {
        sql: ${TABLE}.pelkll2o = 2
          ;;
        label: "Quit Job"
      }

      when: {
        sql: ${TABLE}.pelkll2o = 3
          ;;
        label: "Temporary Job Ended"
      }
    }
  }

  dimension: pelklwo {
    label: "(unemployed)looking-when last worked"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelklwo = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pelklwo = 1
          ;;
        label: "Within The Last 12 Months"
      }

      when: {
        sql: ${TABLE}.pelklwo = 2
          ;;
        label: "More Than 12 Months Ago"
      }

      when: {
        sql: ${TABLE}.pelklwo = 3
          ;;
        label: "Never Worked"
      }
    }
  }

  dimension: pelkm1 {
    label: "(unemployed)looking-search methods"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pelkm1 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 12
          ;;
        label: "Nothing"
      }

      when: {
        sql: ${TABLE}.pelkm1 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pemjnum {
    label: "# of jobs had"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemjnum = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pemjnum = 2
          ;;
        label: "2 Jobs"
      }

      when: {
        sql: ${TABLE}.pemjnum = 3
          ;;
        label: "3 Jobs"
      }

      when: {
        sql: ${TABLE}.pemjnum = 4
          ;;
        label: "4 Or More Jobs"
      }
    }
  }

  dimension: pemjot {
    label: "Had multiple jobs,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemjot = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pemjot = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pemjot = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pemlr {
    label: "Employment status"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemlr = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pemlr = 1
          ;;
        label: "Employed-At Work"
      }

      when: {
        sql: ${TABLE}.pemlr = 2
          ;;
        label: "Employed-Absent"
      }

      when: {
        sql: ${TABLE}.pemlr = 3
          ;;
        label: "Unemployed-On Layoff"
      }

      when: {
        sql: ${TABLE}.pemlr = 4
          ;;
        label: "Unemployed-Looking"
      }

      when: {
        sql: ${TABLE}.pemlr = 5
          ;;
        label: "Retired-Not In Labor Force"
      }

      when: {
        sql: ${TABLE}.pemlr = 6
          ;;
        label: "Disabled-Not In Labor Force"
      }

      when: {
        sql: ${TABLE}.pemlr = 7
          ;;
        label: "Other-Not In Labor Force"
      }
    }
  }

  dimension: penlfact {
    label: "(not in)reason"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penlfact = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.penlfact = 1
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.penlfact = 2
          ;;
        label: "Ill"
      }

      when: {
        sql: ${TABLE}.penlfact = 3
          ;;
        label: "In School"
      }

      when: {
        sql: ${TABLE}.penlfact = 4
          ;;
        label: "Taking Care Of House Or Family"
      }

      when: {
        sql: ${TABLE}.penlfact = 5
          ;;
        label: "In Retirement"
      }

      when: {
        sql: ${TABLE}.penlfact = 6
          ;;
        label: "Something Else/Other"
      }
    }
  }

  dimension: penlfjh {
    label: "(not in)last worked at job/business"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penlfjh = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.penlfjh = 1
          ;;
        label: "Within The Last 12 Months"
      }

      when: {
        sql: ${TABLE}.penlfjh = 2
          ;;
        label: "More Than 12 Months Ago"
      }

      when: {
        sql: ${TABLE}.penlfjh = 3
          ;;
        label: "Never Worked"
      }
    }
  }

  dimension: penlfret {
    label: "(retired) from job/business,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penlfret = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.penlfret = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.penlfret = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peret1 {
    label: "(retired)want full/part-time job,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peret1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.peret1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.peret1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.peret1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peret1 = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.peret1 = 3
          ;;
        label: "HAS A JOB"
      }
    }
  }

  dimension: prabsrea {
    label: "Work absence,reason & pay status"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prabsrea = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prabsrea = 1
          ;;
        label: "FT Paid-Vacation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 2
          ;;
        label: "FT Paid-Own Illness"
      }

      when: {
        sql: ${TABLE}.prabsrea = 3
          ;;
        label: "FT Paid-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prabsrea = 4
          ;;
        label: "FT Paid-Other Family/Personal Oblig."
      }

      when: {
        sql: ${TABLE}.prabsrea = 5
          ;;
        label: "FT Paid-Maternity/Paternity Leave"
      }

      when: {
        sql: ${TABLE}.prabsrea = 6
          ;;
        label: "FT Paid-Labor Dispute"
      }

      when: {
        sql: ${TABLE}.prabsrea = 7
          ;;
        label: "FT Paid-Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.prabsrea = 8
          ;;
        label: "FT Paid-School/Training"
      }

      when: {
        sql: ${TABLE}.prabsrea = 9
          ;;
        label: "FT Paid-Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.prabsrea = 10
          ;;
        label: "FT Paid-Other"
      }

      when: {
        sql: ${TABLE}.prabsrea = 11
          ;;
        label: "FT Unpaid-Vacation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 12
          ;;
        label: "FT Unpaid-Own Illness"
      }

      when: {
        sql: ${TABLE}.prabsrea = 13
          ;;
        label: "FT Unpaid-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prabsrea = 14
          ;;
        label: "FT Unpaid-Other Fam/Personal Obligation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 15
          ;;
        label: "FT Unpaid-Maternity/Paternity Leave"
      }

      when: {
        sql: ${TABLE}.prabsrea = 16
          ;;
        label: "FT Unpaid-Labor Dispute"
      }

      when: {
        sql: ${TABLE}.prabsrea = 17
          ;;
        label: "FT Unpaid-Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.prabsrea = 18
          ;;
        label: "FT Unpaid-School/Training"
      }

      when: {
        sql: ${TABLE}.prabsrea = 19
          ;;
        label: "FT Unpaid-Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.prabsrea = 20
          ;;
        label: "FT Unpaid-Other"
      }

      when: {
        sql: ${TABLE}.prabsrea = 21
          ;;
        label: "PT Paid-Vacation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 22
          ;;
        label: "PT Paid-Own Illness"
      }

      when: {
        sql: ${TABLE}.prabsrea = 23
          ;;
        label: "PT Paid-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prabsrea = 24
          ;;
        label: "PT Paid-Other Family/Personal Oblig."
      }

      when: {
        sql: ${TABLE}.prabsrea = 25
          ;;
        label: "PT Paid-Maternity/Paternity Leave"
      }

      when: {
        sql: ${TABLE}.prabsrea = 26
          ;;
        label: "PT Paid-Labor Dispute"
      }

      when: {
        sql: ${TABLE}.prabsrea = 27
          ;;
        label: "PT Paid-Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.prabsrea = 28
          ;;
        label: "PT Paid-School/Training"
      }

      when: {
        sql: ${TABLE}.prabsrea = 29
          ;;
        label: "PT Paid-Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.prabsrea = 30
          ;;
        label: "PT Paid-Other"
      }

      when: {
        sql: ${TABLE}.prabsrea = 31
          ;;
        label: "PT Unpaid-Vacation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 32
          ;;
        label: "PT Unpaid-Own Illness"
      }

      when: {
        sql: ${TABLE}.prabsrea = 33
          ;;
        label: "PT Unpaid-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prabsrea = 34
          ;;
        label: "PT Unpaid-Other Fam/Personal Obligation"
      }

      when: {
        sql: ${TABLE}.prabsrea = 35
          ;;
        label: "PT Unpaid-Maternity/Paternity Leave"
      }

      when: {
        sql: ${TABLE}.prabsrea = 36
          ;;
        label: "PT Unpaid-Labor Dispute"
      }

      when: {
        sql: ${TABLE}.prabsrea = 37
          ;;
        label: "PT Unpaid-Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.prabsrea = 38
          ;;
        label: "PT Unpaid-School/Training"
      }

      when: {
        sql: ${TABLE}.prabsrea = 39
          ;;
        label: "PT Unpaid-Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.prabsrea = 40
          ;;
        label: "PT Unpaid-Other"
      }
    }
  }

  dimension: prcivlf {
    label: "Part of/not part of"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcivlf = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prcivlf = 1
          ;;
        label: "In Civilian Labor Force"
      }

      when: {
        sql: ${TABLE}.prcivlf = 2
          ;;
        label: "Not In Civilian Labor Force"
      }
    }
  }

  dimension: prdisc {
    label: "Discouraged worker/conditionally interested"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdisc = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdisc = 1
          ;;
        label: "Discouraged worker"
      }

      when: {
        sql: ${TABLE}.prdisc = 2
          ;;
        label: "Conditionally interested"
      }

      when: {
        sql: ${TABLE}.prdisc = 3
          ;;
        label: "Not available"
      }
    }
  }

  dimension: premphrs {
    label: "Reason for work absence or hours at work"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.premphrs = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.premphrs = 0
          ;;
        label: "Unemployed And Nilf"
      }

      when: {
        sql: ${TABLE}.premphrs = 1
          ;;
        label: "W/Job, Illness-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 2
          ;;
        label: "W/Job, Vacation-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 3
          ;;
        label: "W/Job, Weather affected job-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 4
          ;;
        label: "W/Job, Labor dispute-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 5
          ;;
        label: "W/Job, Child care problems-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 6
          ;;
        label: "W/Job, Fam/Pers/obligation-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 7
          ;;
        label: "W/Job, Maternity/Paternity-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 8
          ;;
        label: "W/Job, School/Training-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 9
          ;;
        label: "W/Job, Civic/Military duty-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 10
          ;;
        label: "W/Job, Does not work in bus.-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 11
          ;;
        label: "W/Job, Other-Not At Work"
      }

      when: {
        sql: ${TABLE}.premphrs = 12
          ;;
        label: "At Work- 1-4 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 13
          ;;
        label: "At Work- 5-14 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 14
          ;;
        label: "At Work- 15-21 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 15
          ;;
        label: "At Work- 22-29 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 16
          ;;
        label: "At Work- 30-34 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 17
          ;;
        label: "At Work- 35-39 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 18
          ;;
        label: "At Work- 40 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 19
          ;;
        label: "At Work- 41-47 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 20
          ;;
        label: "At Work- 48 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 21
          ;;
        label: "At Work- 49-59 Hrs"
      }

      when: {
        sql: ${TABLE}.premphrs = 22
          ;;
        label: "At Work- 60 Hrs Or More"
      }
    }
  }

  dimension: prempnot {
    label: "Employment status,recode"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prempnot = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prempnot = 1
          ;;
        label: "Employed"
      }

      when: {
        sql: ${TABLE}.prempnot = 2
          ;;
        label: "Unemployed"
      }

      when: {
        sql: ${TABLE}.prempnot = 3
          ;;
        label: "Discouraged-Not in labor force (NILF)"
      }

      when: {
        sql: ${TABLE}.prempnot = 4
          ;;
        label: "Other Not in labor force (NILF)"
      }
    }
  }

  dimension: prerelg {
    label: "Earnings edit eligibility flag"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prerelg = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prerelg = 0
          ;;
        label: "Not Eligible For Edit"
      }

      when: {
        sql: ${TABLE}.prerelg = 1
          ;;
        label: "Eligible For Edit"
      }
    }
  }

  dimension: prexplf {
    label: "Employed/unemployed"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prexplf = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prexplf = 1
          ;;
        label: "Employed"
      }

      when: {
        sql: ${TABLE}.prexplf = 2
          ;;
        label: "Unemployed"
      }
    }
  }

  dimension: prftlf {
    label: "Full-time/part-time"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prftlf = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prftlf = 1
          ;;
        label: "Full time labor force"
      }

      when: {
        sql: ${TABLE}.prftlf = 2
          ;;
        label: "Part Time Labor Force"
      }
    }
  }

  dimension: prhrusl {
    label: "Hours usually worked weekly"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prhrusl = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prhrusl = 1
          ;;
        label: "0-20 Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 2
          ;;
        label: "21-34 Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 3
          ;;
        label: "35-39 Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 4
          ;;
        label: "40 Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 5
          ;;
        label: "41-49 Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 6
          ;;
        label: "50 Or More Hrs"
      }

      when: {
        sql: ${TABLE}.prhrusl = 7
          ;;
        label: "Varies-Full Time"
      }

      when: {
        sql: ${TABLE}.prhrusl = 8
          ;;
        label: "Varies-Part Time"
      }
    }
  }

  dimension: prjobsea {
    label: "Job search,recode"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prjobsea = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prjobsea = 1
          ;;
        label: "Looked last 12 months, since completing previous job"
      }

      when: {
        sql: ${TABLE}.prjobsea = 2
          ;;
        label: "Worked-Looked Last 4 Weeks"
      }

      when: {
        sql: ${TABLE}.prjobsea = 3
          ;;
        label: "Layoff-Looked Last 4 Weeks"
      }

      when: {
        sql: ${TABLE}.prjobsea = 4
          ;;
        label: "Unavailable Job Seekers"
      }

      when: {
        sql: ${TABLE}.prjobsea = 5
          ;;
        label: "No Recent Job Search"
      }
    }
  }

  dimension: prpthrs {
    label: "(part-timer)economic/non-economic reasons,hours"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prpthrs = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prpthrs = 0
          ;;
        label: "Usualy FT, PT For Noneconomic Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 1
          ;;
        label: "1-4 Hrs;Usu.FT, PT Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 2
          ;;
        label: "5-14 Hrs;Usu.FT, PT Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 3
          ;;
        label: "15-29 Hrs;Usu.FT, PT Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 4
          ;;
        label: "30-34 Hrs;Usu.FT, PT Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 5
          ;;
        label: "1-4 Hrs; Usu.PT, Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 6
          ;;
        label: "5-14 Hrs; Usu.PT, Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 7
          ;;
        label: "15-29 Hrs; Usu.PT, Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 8
          ;;
        label: "30-34 Hrs; Usu.PT, Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 9
          ;;
        label: "1-4 Hrs;Usu.PT, Non-Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 10
          ;;
        label: "5-14 Hrs;Usu.PT, Non-Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 11
          ;;
        label: "15-29 Hrs;Usu.PT, Non-Econ Reasons"
      }

      when: {
        sql: ${TABLE}.prpthrs = 12
          ;;
        label: "30-34 HrsUsu.PT, Non-Econ Reasons"
      }
    }
  }

  dimension: prptrea {
    label: "(part-timer)specific reason"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prptrea = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prptrea = 1
          ;;
        label: "Usu. FT-Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.prptrea = 2
          ;;
        label: "Usu. FT-Seasonal Work"
      }

      when: {
        sql: ${TABLE}.prptrea = 3
          ;;
        label: "Usu. FT-Job Started/Ended During Week"
      }

      when: {
        sql: ${TABLE}.prptrea = 4
          ;;
        label: "Usu. FT-Vacation/Personal Day"
      }

      when: {
        sql: ${TABLE}.prptrea = 5
          ;;
        label: "Usu. FT-Own Illness/Injury/Medical Appt"
      }

      when: {
        sql: ${TABLE}.prptrea = 6
          ;;
        label: "Usu. FT-Holiday (religious Or Legal)"
      }

      when: {
        sql: ${TABLE}.prptrea = 7
          ;;
        label: "Usu. FT-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prptrea = 8
          ;;
        label: "Usu. FT-Other Fam/Pers Obligations"
      }

      when: {
        sql: ${TABLE}.prptrea = 9
          ;;
        label: "Usu. FT-Labor Dispute"
      }

      when: {
        sql: ${TABLE}.prptrea = 10
          ;;
        label: "Usu. FT-Weather Affected Job"
      }

      when: {
        sql: ${TABLE}.prptrea = 11
          ;;
        label: "Usu. FT-School/Training"
      }

      when: {
        sql: ${TABLE}.prptrea = 12
          ;;
        label: "Usu. FT-Civic/Military Duty"
      }

      when: {
        sql: ${TABLE}.prptrea = 13
          ;;
        label: "Usu. FT-Other Reason"
      }

      when: {
        sql: ${TABLE}.prptrea = 14
          ;;
        label: "Usu. PT-Slack Work/Business Conditions"
      }

      when: {
        sql: ${TABLE}.prptrea = 15
          ;;
        label: "Usu. PT-Could Only Find PT Work"
      }

      when: {
        sql: ${TABLE}.prptrea = 16
          ;;
        label: "Usu. PT-Seasonal Work"
      }

      when: {
        sql: ${TABLE}.prptrea = 17
          ;;
        label: "Usu. PT-Child Care Problems"
      }

      when: {
        sql: ${TABLE}.prptrea = 18
          ;;
        label: "Usu. PT-Other Fam/Pers Obligations"
      }

      when: {
        sql: ${TABLE}.prptrea = 19
          ;;
        label: "Usu. PT-Health/Medical Limitations"
      }

      when: {
        sql: ${TABLE}.prptrea = 20
          ;;
        label: "Usu. PT-School/Training"
      }

      when: {
        sql: ${TABLE}.prptrea = 21
          ;;
        label: "Usu. PT-Retired/Ss Limit On Earnings"
      }

      when: {
        sql: ${TABLE}.prptrea = 22
          ;;
        label: "Usu. PT-Workweek <35 Hours"
      }

      when: {
        sql: ${TABLE}.prptrea = 23
          ;;
        label: "Usu. PT-Other Reason"
      }
    }
  }

  dimension: prunedur {
    label: "Unemployment duration"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 250, 500, 750, 999]
    style: classic
    sql: CASE WHEN ${TABLE}.prunedur between 0 AND 999 THEN ${TABLE}.prunedur END ;;
  }

  dimension: pruntype {
    label: "Unemployment reason"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pruntype = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.pruntype = 1
          ;;
        label: "Job loser/on layoff"
      }

      when: {
        sql: ${TABLE}.pruntype = 2
          ;;
        label: "Other job loser"
      }

      when: {
        sql: ${TABLE}.pruntype = 3
          ;;
        label: "Temporary Job Ended"
      }

      when: {
        sql: ${TABLE}.pruntype = 4
          ;;
        label: "Job leaver"
      }

      when: {
        sql: ${TABLE}.pruntype = 5
          ;;
        label: "Re-entrant"
      }

      when: {
        sql: ${TABLE}.pruntype = 6
          ;;
        label: "New-entrant"
      }
    }
  }

  dimension: prwksch {
    label: "Employment status by time worked/lost"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prwksch = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prwksch = 0
          ;;
        label: "Not In Labor Force"
      }

      when: {
        sql: ${TABLE}.prwksch = 1
          ;;
        label: "At work"
      }

      when: {
        sql: ${TABLE}.prwksch = 2
          ;;
        label: "With job, not at work"
      }

      when: {
        sql: ${TABLE}.prwksch = 3
          ;;
        label: "Unemployed, Seeks FT"
      }

      when: {
        sql: ${TABLE}.prwksch = 4
          ;;
        label: "Unemp., Seeks PT"
      }
    }
  }

  dimension: prwkstat {
    label: "Full-time/part-time work status"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prwkstat = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prwkstat = 1
          ;;
        label: "Not in labor force"
      }

      when: {
        sql: ${TABLE}.prwkstat = 2
          ;;
        label: "FT hours (35+), usually fT"
      }

      when: {
        sql: ${TABLE}.prwkstat = 3
          ;;
        label: "PT for economic reasons, usually FT"
      }

      when: {
        sql: ${TABLE}.prwkstat = 4
          ;;
        label: "PT for non-economic reasons, usually FT"
      }

      when: {
        sql: ${TABLE}.prwkstat = 5
          ;;
        label: "Not At Work, Usually FT"
      }

      when: {
        sql: ${TABLE}.prwkstat = 6
          ;;
        label: "PT Hours, Usually PT For Economic Reasons"
      }

      when: {
        sql: ${TABLE}.prwkstat = 7
          ;;
        label: "PT hrs, Usually PT For Non-Economic"
      }

      when: {
        sql: ${TABLE}.prwkstat = 8
          ;;
        label: "FT Hours, Usually PT For Economic Reas"
      }

      when: {
        sql: ${TABLE}.prwkstat = 9
          ;;
        label: "FT hrs, Usually PT For Non-Economic"
      }

      when: {
        sql: ${TABLE}.prwkstat = 10
          ;;
        label: "Not at work, Usually Part-Time"
      }

      when: {
        sql: ${TABLE}.prwkstat = 11
          ;;
        label: "Unemployed FT"
      }

      when: {
        sql: ${TABLE}.prwkstat = 12
          ;;
        label: "Unemployed PT"
      }
    }
  }

  dimension: prwntjob {
    label: "(not in)or wants job-recode"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prwntjob = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prwntjob = 1
          ;;
        label: "Want a job"
      }

      when: {
        sql: ${TABLE}.prwntjob = 2
          ;;
        label: "Other not in labor force"
      }
    }
  }

  dimension: puabsot {
    label: "Output var determines absence from job"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puabsot = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puabsot = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puabsot = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puabsot = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puabsot = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.puabsot = 3
          ;;
        label: "Retired"
      }

      when: {
        sql: ${TABLE}.puabsot = 4
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.puabsot = 5
          ;;
        label: "Unable To Work"
      }
    }
  }

  dimension: pubus1 {
    label: "Unpaid work in family business/farm,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubus1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubus1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubus1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubus1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pubus1 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pubus2ot {
    label: "Stores bus2 entry"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubus2ot = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubus2ot = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubus2ot = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubus2ot = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pubus2ot = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pubusck1 {
    label: "Filter for question on unpaid work"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubusck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubusck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubusck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubusck1 = 1
          ;;
        label: "Goto PUBUS1"
      }

      when: {
        sql: ${TABLE}.pubusck1 = 2
          ;;
        label: "GOTO PURETCK1"
      }
    }
  }

  dimension: pubusck2 {
    label: "(family business)skips owners,no work last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubusck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubusck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubusck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubusck2 = 1
          ;;
        label: "Goto Puhrusl1"
      }

      when: {
        sql: ${TABLE}.pubusck2 = 2
          ;;
        label: "Goto PUBUS2"
      }
    }
  }

  dimension: pubusck3 {
    label: "Filter for business owners to absence reason"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubusck3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubusck3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubusck3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubusck3 = 1
          ;;
        label: "Goto PUABSRSN"
      }

      when: {
        sql: ${TABLE}.pubusck3 = 2
          ;;
        label: "Goto PULAY"
      }
    }
  }

  dimension: pubusck4 {
    label: "Filter for business owners skip pattern"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pubusck4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pubusck4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pubusck4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pubusck4 = 1
          ;;
        label: "Goto Puhrusl1"
      }

      when: {
        sql: ${TABLE}.pubusck4 = 2
          ;;
        label: "Goto PUABAPD"
      }
    }
  }

  dimension: pudis {
    label: "Verify disability status from previous month"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudis = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudis = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudis = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudis = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pudis = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pudis = 3
          ;;
        label: "Did Not Have Disability Last Month"
      }
    }
  }

  dimension: pudis1 {
    label: "Probe #1 for disability"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudis1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudis1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudis1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudis1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pudis1 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pudis2 {
    label: "Probe #2 for disability"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudis2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudis2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudis2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudis2 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pudis2 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pudwck1 {
    label: "Filter for discouraged worker screening"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudwck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudwck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudwck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudwck1 = 1
          ;;
        label: "Goto Puschck"
      }

      when: {
        sql: ${TABLE}.pudwck1 = 2
          ;;
        label: "Goto PUNLFCK1"
      }

      when: {
        sql: ${TABLE}.pudwck1 = 3
          ;;
        label: "Goto PUDWCK4"
      }

      when: {
        sql: ${TABLE}.pudwck1 = 4
          ;;
        label: "Goto PUDWWANT"
      }
    }
  }

  dimension: pudwck2 {
    label: "Filter for disabled"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudwck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudwck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudwck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudwck2 = 1
          ;;
        label: "Goto Pujhck1"
      }

      when: {
        sql: ${TABLE}.pudwck2 = 2
          ;;
        label: "Goto Pudis1"
      }

      when: {
        sql: ${TABLE}.pudwck2 = 3
          ;;
        label: "Goto Pudis2"
      }

      when: {
        sql: ${TABLE}.pudwck2 = 4
          ;;
        label: "Goto PUDWCK4"
      }
    }
  }

  dimension: pudwck3 {
    label: "Filter for retired"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudwck3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudwck3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudwck3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudwck3 = 1
          ;;
        label: "Goto PUDWCK4"
      }

      when: {
        sql: ${TABLE}.pudwck3 = 2
          ;;
        label: "Goto PUNLFCK2"
      }
    }
  }

  dimension: pudwck4 {
    label: "Filter/plug for passive jobseekers"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudwck4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudwck4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudwck4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudwck4 = 1
          ;;
        label: "Goto PUDWCK5"
      }

      when: {
        sql: ${TABLE}.pudwck4 = 2
          ;;
        label: "Go To PUDWCK5"
      }

      when: {
        sql: ${TABLE}.pudwck4 = 3
          ;;
        label: "Go to PUDWCK5"
      }

      when: {
        sql: ${TABLE}.pudwck4 = 4
          ;;
        label: "Goto PUDWRSN"
      }
    }
  }

  dimension: pudwck5 {
    label: "Filter/plug for passive jobseeker"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pudwck5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pudwck5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pudwck5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pudwck5 = 1
          ;;
        label: "Goto PUDWWK"
      }

      when: {
        sql: ${TABLE}.pudwck5 = 2
          ;;
        label: "Goto PUDWLK"
      }
    }
  }

  dimension: puhrck1 {
    label: "Remove groups from actual hours series"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck1 = 1
          ;;
        label: "Goto PUHRUSL2"
      }

      when: {
        sql: ${TABLE}.puhrck1 = 2
          ;;
        label: "Goto PUHRUSLT"
      }
    }
  }

  dimension: puhrck12 {
    label: "Filter for <15 hours to go to looking series"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck12 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck12 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck12 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck12 = 1
          ;;
        label: "Goto Pulk"
      }

      when: {
        sql: ${TABLE}.puhrck12 = 2
          ;;
        label: "Goto Puiock1"
      }
    }
  }

  dimension: puhrck2 {
    label: "Skips persons out of pt series"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck2 = 1
          ;;
        label: "Goto PUHRFTPT"
      }

      when: {
        sql: ${TABLE}.puhrck2 = 2
          ;;
        label: "Go To PUHRFTPT"
      }

      when: {
        sql: ${TABLE}.puhrck2 = 3
          ;;
        label: "Go to PUHRFTPT"
      }

      when: {
        sql: ${TABLE}.puhrck2 = 4
          ;;
        label: "go to PUHRFTPT"
      }

      when: {
        sql: ${TABLE}.puhrck2 = 5
          ;;
        label: "Goto PUHRCK3"
      }
    }
  }

  dimension: puhrck3 {
    label: "Skips persons out of pt series"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 1
          ;;
        label: "Goto PUHRCK8"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 2
          ;;
        label: "Go To PUHRCK8"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 3
          ;;
        label: "Goto PUHRACT1"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 4
          ;;
        label: "Goto PUHRCK4"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 5
          ;;
        label: "Goto PUHROFF1"
      }

      when: {
        sql: ${TABLE}.puhrck3 = 6
          ;;
        label: "Go To PUHRCK4"
      }
    }
  }

  dimension: puhrck4 {
    label: "Skips persons"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck4 = 1
          ;;
        label: "Goto PUHRACT1"
      }

      when: {
        sql: ${TABLE}.puhrck4 = 2
          ;;
        label: "Goto PUHROFF1"
      }

      when: {
        sql: ${TABLE}.puhrck4 = 3
          ;;
        label: "Goto PUHRWANT"
      }

      when: {
        sql: ${TABLE}.puhrck4 = 4
          ;;
        label: "Go to PUHRWANT"
      }

      when: {
        sql: ${TABLE}.puhrck4 = 5
          ;;
        label: "Go to PUHRACT1"
      }
    }
  }

  dimension: puhrck5 {
    label: "Filter for multiple jobholders for job 2 hours"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck5 = 1
          ;;
        label: "Goto PUHRACT2"
      }

      when: {
        sql: ${TABLE}.puhrck5 = 2
          ;;
        label: "Goto PUHRCK6"
      }
    }
  }

  dimension: puhrck6 {
    label: "Filter for actual hours jobs 1 and 2"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck6 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck6 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck6 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck6 = 1
          ;;
        label: "Goto Pulk"
      }

      when: {
        sql: ${TABLE}.puhrck6 = 2
          ;;
        label: "Goto PUABSRSN"
      }

      when: {
        sql: ${TABLE}.puhrck6 = 3
          ;;
        label: "Goto PUHRACTT"
      }
    }
  }

  dimension: puhrck7 {
    label: "Filter for hours worked paths"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrck7 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrck7 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrck7 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrck7 = 1
          ;;
        label: "Goto PUHRCK8"
      }

      when: {
        sql: ${TABLE}.puhrck7 = 5
          ;;
        label: "Go To PUHRCK8"
      }

      when: {
        sql: ${TABLE}.puhrck7 = 3
          ;;
        label: "Goto PUHRRSN3"
      }

      when: {
        sql: ${TABLE}.puhrck7 = 4
          ;;
        label: "Goto PUHRAVL"
      }
    }
  }

  dimension: puhroff1 {
    label: "Any work hours were lost last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhroff1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhroff1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhroff1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhroff1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puhroff1 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: puhroff2 {
    label: "Number of work hours lost last week"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.puhroff2 between 0 AND 99 THEN ${TABLE}.puhroff2 END ;;
  }

  dimension: puhrot1 {
    label: "Extra hours worked last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puhrot1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puhrot1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puhrot1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puhrot1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puhrot1 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: puhrot2 {
    label: "Number of extra hours worked last week"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.puhrot2 between 0 AND 99 THEN ${TABLE}.puhrot2 END ;;
  }

  dimension: pujhck1 {
    label: "Filter for outgoing rotations"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhck1 = 1
          ;;
        label: "Goto PUJHCK2"
      }

      when: {
        sql: ${TABLE}.pujhck1 = 2
          ;;
        label: "Goto PUNLFCK1"
      }

      when: {
        sql: ${TABLE}.pujhck1 = 3
          ;;
        label: "Goto"
      }
    }
  }

  dimension: pujhck2 {
    label: "Filter for persons going thru i&o series"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhck2 = 1
          ;;
        label: "Goto PUJHRSN"
      }

      when: {
        sql: ${TABLE}.pujhck2 = 2
          ;;
        label: "Goto PUJHWANT"
      }

      when: {
        sql: ${TABLE}.pujhck2 = 3
          ;;
        label: "Goto PUJHWK"
      }
    }
  }

  dimension: pujhck3 {
    label: "Filter for unemployed job history"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhck3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhck3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhck3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhck3 = 1
          ;;
        label: "Goto PUJHDP1"
      }

      when: {
        sql: ${TABLE}.pujhck3 = 2
          ;;
        label: "Goto PUJHRSN"
      }
    }
  }

  dimension: pujhck4 {
    label: "(not in)filter for dependent"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhck4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhck4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhck4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhck4 = 1
          ;;
        label: "Goto PUJHCK5"
      }

      when: {
        sql: ${TABLE}.pujhck4 = 2
          ;;
        label: "Goto PUIO1INT"
      }

      when: {
        sql: ${TABLE}.pujhck4 = 3
          ;;
        label: "Go to PUJHCK5"
      }

      when: {
        sql: ${TABLE}.pujhck4 = 4
          ;;
        label: "Go to PUIO1INT"
      }

      when: {
        sql: ${TABLE}.pujhck4 = 5
          ;;
        label: "Goto PUNLFCK1"
      }
    }
  }

  dimension: pujhck5 {
    label: "(not in)filter/carryover for dependent"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhck5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhck5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhck5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhck5 = 1
          ;;
        label: "Goto Puio1int"
      }

      when: {
        sql: ${TABLE}.pujhck5 = 2
          ;;
        label: "Goto PUIOCK5"
      }
    }
  }

  dimension: pujhdp1o {
    label: "Out variable for jhdp1"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pujhdp1o = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pujhdp1o = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pujhdp1o = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pujhdp1o = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pujhdp1o = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pulay {
    label: "Person on layoff from job"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulay = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulay = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulay = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulay = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pulay = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pulay = 3
          ;;
        label: "Retired"
      }

      when: {
        sql: ${TABLE}.pulay = 4
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.pulay = 5
          ;;
        label: "Unable To Work"
      }
    }
  }

  dimension: pulay6m {
    label: "Person on layoff,recalled in 6 months,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulay6m = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulay6m = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulay6m = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulay6m = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pulay6m = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pulayavr {
    label: "Person on layoff,reason unavailable to work"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulayavr = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulayavr = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulayavr = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulayavr = 1
          ;;
        label: "Own Temporary Illness"
      }

      when: {
        sql: ${TABLE}.pulayavr = 2
          ;;
        label: "Going To School"
      }

      when: {
        sql: ${TABLE}.pulayavr = 3
          ;;
        label: "Other"
      }
    }
  }

  dimension: pulayck1 {
    label: "Filter for previous month layoff status"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulayck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulayck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulayck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulayck1 = 1
          ;;
        label: "Goto PULAYCK3"
      }

      when: {
        sql: ${TABLE}.pulayck1 = 2
          ;;
        label: "Goto PULAYFT"
      }

      when: {
        sql: ${TABLE}.pulayck1 = 3
          ;;
        label: "Goto PULAYDR"
      }
    }
  }

  dimension: pulayck2 {
    label: "Filter/plug for dependent layoff"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulayck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulayck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulayck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulayck2 = 1
          ;;
        label: "Goto PULAYDR3"
      }

      when: {
        sql: ${TABLE}.pulayck2 = 2
          ;;
        label: "Goto PULAYFT"
      }
    }
  }

  dimension: pulayck3 {
    label: "Dependent i & o filter/carryover"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulayck3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulayck3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulayck3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulayck3 = 1
          ;;
        label: "MISCK=5 Goto IO1INT"
      }

      when: {
        sql: ${TABLE}.pulayck3 = 2
          ;;
        label: "I-ICR=1 Or I-OCR=1, Goto IO1INT"
      }

      when: {
        sql: ${TABLE}.pulayck3 = 3
          ;;
        label: "All Others Goto SCHCK"
      }
    }
  }

  dimension: pulaydt {
    label: "Person on layoff,has return date,y/n"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulaydt = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulaydt = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulaydt = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulaydt = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pulaydt = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pulk {
    label: "Looked for work in last 4 wks"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulk = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulk = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulk = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pulk = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.pulk = 3
          ;;
        label: "Retired"
      }

      when: {
        sql: ${TABLE}.pulk = 4
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.pulk = 5
          ;;
        label: "Unable To Work"
      }
    }
  }

  dimension: pulkavr {
    label: "Reason jobseeker unavailable last week"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkavr = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkavr = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkavr = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkavr = 1
          ;;
        label: "Waiting For New Job To Begin"
      }

      when: {
        sql: ${TABLE}.pulkavr = 2
          ;;
        label: "Own Temporary Illness"
      }

      when: {
        sql: ${TABLE}.pulkavr = 3
          ;;
        label: "Going to school"
      }

      when: {
        sql: ${TABLE}.pulkavr = 4
          ;;
        label: "OTHER - SPECIFY"
      }
    }
  }

  dimension: pulkdk1 {
    label: "(unemployed) followup to lkm1"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = -1
          ;;
        label: "BLANK or NO"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 12
          ;;
        label: "Nothing"
      }

      when: {
        sql: ${TABLE}.pulkdk1 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkdk2 {
    label: "(looking for work)followup to lkm2"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk2 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkdk3 {
    label: "(looking for work)followup to lkm3"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk3 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkdk4 {
    label: "(looking for work)followup to lkm4"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk4 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkdk5 {
    label: "(looking for work)followup to lkm5"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk5 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkdk6 {
    label: "(looking for work)followup to lkm6"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkdk6 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkdk6 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkm2 {
    label: "(job search)methods,all in last 4 weeks"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkm2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkm2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkm2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkm2 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkm3 {
    label: "(job search)methods-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkm3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkm3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkm3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkm3 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkm4 {
    label: "(job search)methods-followup2"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkm4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkm4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkm4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkm4 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkm5 {
    label: "(job search)methods followup3"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkm5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkm5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkm5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkm5 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkm6 {
    label: "(job search)methods followup4"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkm6 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkm6 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkm6 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkm6 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps1 {
    label: "(job search)passive entry to lkm1-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps1 = -1
          ;;
        label: "BLANK or NO"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 12
          ;;
        label: "Nothing"
      }

      when: {
        sql: ${TABLE}.pulkps1 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps2 {
    label: "(job search)passive entry to lkm2-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps2 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps3 {
    label: "(job search)passive entry to lkm3-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps3 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps4 {
    label: "(job search)passive entry to lkm4-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps4 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps4 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps4 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps4 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps5 {
    label: "(job search)passive entry to lkm5-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps5 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps5 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps5 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps5 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: pulkps6 {
    label: "(job search)passive entry to lkm6-followup"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pulkps6 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pulkps6 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pulkps6 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 1
          ;;
        label: "Contacted Employer Directly/Interview"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 2
          ;;
        label: "Contacted Pulbic Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 3
          ;;
        label: "Contacted Private Employment Agency"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 4
          ;;
        label: "Contacted Friends Or Relatives"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 5
          ;;
        label: "Contacted School/University Empl Center"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 6
          ;;
        label: "Sent Out Resumes/Filled Out Application"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 7
          ;;
        label: "Checked Union/Professional Registers"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 8
          ;;
        label: "Placed Or Answered Ads"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 9
          ;;
        label: "Other Active"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 10
          ;;
        label: "Looked At Ads"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 11
          ;;
        label: "Attended Job Training Programs/Courses"
      }

      when: {
        sql: ${TABLE}.pulkps6 = 13
          ;;
        label: "Other Passive"
      }
    }
  }

  dimension: punlfck1 {
    label: "Age filter for retirement question"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.punlfck1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.punlfck1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.punlfck1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.punlfck1 = 1
          ;;
        label: "AGERNG-I=1-4,9, Goto NLFACT"
      }

      when: {
        sql: ${TABLE}.punlfck1 = 2
          ;;
        label: "All Others Goto NLFRET"
      }
    }
  }

  dimension: punlfck2 {
    label: "Outgoing rotation filter"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.punlfck2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.punlfck2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.punlfck2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.punlfck2 = 1
          ;;
        label: "MIS-CK=4, Goto NLFJH"
      }

      when: {
        sql: ${TABLE}.punlfck2 = 2
          ;;
        label: "All Other Rotations, End Questions"
      }
    }
  }

  dimension: puretot {
    label: "Verify retirement status,previous month"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puretot = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puretot = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puretot = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puretot = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puretot = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.puretot = 3
          ;;
        label: "WAS NOT RETIRED LAST MONTH"
      }
    }
  }

  dimension: puslfprx {
    label: "Information given by self/proxy"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puslfprx = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puslfprx = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puslfprx = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puslfprx = 1
          ;;
        label: "Self"
      }

      when: {
        sql: ${TABLE}.puslfprx = 2
          ;;
        label: "Proxy"
      }

      when: {
        sql: ${TABLE}.puslfprx = 3
          ;;
        label: "Both self and proxy"
      }
    }
  }

  dimension: puwk {
    label: "Did work for pay/profit"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puwk = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puwk = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puwk = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puwk = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puwk = 2
          ;;
        label: "No"
      }

      when: {
        sql: ${TABLE}.puwk = 3
          ;;
        label: "Retired"
      }

      when: {
        sql: ${TABLE}.puwk = 4
          ;;
        label: "Disabled"
      }

      when: {
        sql: ${TABLE}.puwk = 5
          ;;
        label: "Unable To Work"
      }
    }
  }

  dimension: peio1cow {
    label: "(main job)class of worker"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio1cow = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peio1cow = 1
          ;;
        label: "Government - Federal"
      }

      when: {
        sql: ${TABLE}.peio1cow = 2
          ;;
        label: "Government - State"
      }

      when: {
        sql: ${TABLE}.peio1cow = 3
          ;;
        label: "Government - Local"
      }

      when: {
        sql: ${TABLE}.peio1cow = 4
          ;;
        label: "Private, For Profit"
      }

      when: {
        sql: ${TABLE}.peio1cow = 5
          ;;
        label: "Private, Nonprofit"
      }

      when: {
        sql: ${TABLE}.peio1cow = 6
          ;;
        label: "Self-Employed, Incorporated"
      }

      when: {
        sql: ${TABLE}.peio1cow = 7
          ;;
        label: "Self-Employed, Unincorporated"
      }

      when: {
        sql: ${TABLE}.peio1cow = 8
          ;;
        label: "Without Pay"
      }
    }
  }

  dimension: peio2cow {
    label: "(second job)class of worker"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio2cow = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peio2cow = 1
          ;;
        label: "Government - Federal"
      }

      when: {
        sql: ${TABLE}.peio2cow = 2
          ;;
        label: "Government - State"
      }

      when: {
        sql: ${TABLE}.peio2cow = 3
          ;;
        label: "Government - Local"
      }

      when: {
        sql: ${TABLE}.peio2cow = 4
          ;;
        label: "Private, For Profit"
      }

      when: {
        sql: ${TABLE}.peio2cow = 5
          ;;
        label: "Private, Nonprofit"
      }

      when: {
        sql: ${TABLE}.peio2cow = 6
          ;;
        label: "Self-Employed, Incorporated"
      }

      when: {
        sql: ${TABLE}.peio2cow = 7
          ;;
        label: "Self-Employed, Unincorporated"
      }

      when: {
        sql: ${TABLE}.peio2cow = 8
          ;;
        label: "Without Pay"
      }
    }
  }

  dimension: puio1mfg {
    label: "(main job)in manufacturing/wholesale/retail"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puio1mfg = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puio1mfg = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puio1mfg = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puio1mfg = 1
          ;;
        label: "Manufacturing"
      }

      when: {
        sql: ${TABLE}.puio1mfg = 2
          ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.puio1mfg = 3
          ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.puio1mfg = 4
          ;;
        label: "Something Else"
      }
    }
  }

  dimension: puio2mfg {
    label: "(second job)in manufacturing/wholesale/retail"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puio2mfg = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puio2mfg = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puio2mfg = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puio2mfg = 1
          ;;
        label: "Manufacturing"
      }

      when: {
        sql: ${TABLE}.puio2mfg = 2
          ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.puio2mfg = 3
          ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.puio2mfg = 4
          ;;
        label: "Something Else"
      }
    }
  }

  dimension: puiock1 {
    label: "Filter for dependent industry & occupation"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiock1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiock1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiock1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiock1 = 5
          ;;
        label: "Goto Puio1int"
      }

      when: {
        sql: ${TABLE}.puiock1 = 2
          ;;
        label: "Go to PUIO1INT"
      }

      when: {
        sql: ${TABLE}.puiock1 = 3
          ;;
        label: "Go To PUIO1INT"
      }

      when: {
        sql: ${TABLE}.puiock1 = 4
          ;;
        label: "Goto PUIODP1"
      }
    }
  }

  dimension: puiock2 {
    label: "Filter for previous month's referred i&o cases"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiock2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiock2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiock2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiock2 = 1
          ;;
        label: "Goto PUIO1IND"
      }

      when: {
        sql: ${TABLE}.puiock2 = 2
          ;;
        label: "Goto PUIO1OCC"
      }

      when: {
        sql: ${TABLE}.puiock2 = 3
          ;;
        label: "Goto PUIODP2"
      }
    }
  }

  dimension: puiock3 {
    label: "Filter for previous month's unknown occupation"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiock3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiock3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiock3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiock3 = 1
          ;;
        label: "Goto PUIO1OCC"
      }

      when: {
        sql: ${TABLE}.puiock3 = 2
          ;;
        label: "Goto PUIO10CC"
      }

      when: {
        sql: ${TABLE}.puiock3 = 3
          ;;
        label: "Goto PUIODP3"
      }
    }
  }

  dimension: puiodp1 {
    label: "Verification of previous month's employer"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiodp1 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiodp1 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiodp1 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiodp1 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puiodp1 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: puiodp2 {
    label: "Job duties changed since last month,y/n"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiodp2 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiodp2 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiodp2 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiodp2 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puiodp2 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: puiodp3 {
    label: "Verify previous month's occupation description"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puiodp3 = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puiodp3 = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puiodp3 = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puiodp3 = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puiodp3 = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: ptern {
    label: "(weekly)amount of overtime earnings"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 722, 1443, 2164, 2885]
    style: classic
    sql: CASE WHEN ${TABLE}.ptern between 0.0 AND 2884.61 THEN ${TABLE}.ptern END ;;
  }

  dimension: peerncov {
    label: "Covered by labor union/employee contract,y/n"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peerncov = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peerncov = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peerncov = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: pternh1o {
    label: "Hourly pay rate,amount"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 25, 50, 75, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pternh1o between 0.0 AND 99.99 THEN ${TABLE}.pternh1o END ;;
  }

  dimension: pternh2 {
    label: "(main job)hourly pay rate,amount"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 25, 50, 75, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pternh2 between 0.0 AND 99.99 THEN ${TABLE}.pternh2 END ;;
  }

  dimension: peernhro {
    label: "#hours usually worked"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.peernhro between 0 AND 99 THEN ${TABLE}.peernhro END ;;
  }

  dimension: peernhry {
    label: "Hourly/non-hourly worker"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peernhry = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peernhry = 1
          ;;
        label: "Hourly Worker"
      }

      when: {
        sql: ${TABLE}.peernhry = 2
          ;;
        label: "Nonhourly Worker"
      }
    }
  }

  dimension: peernlab {
    label: "Union member,y/n"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peernlab = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peernlab = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peernlab = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peernper {
    label: "When received,periodicity"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peernper = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peernper = 1
          ;;
        label: "Hourly"
      }

      when: {
        sql: ${TABLE}.peernper = 2
          ;;
        label: "Weekly"
      }

      when: {
        sql: ${TABLE}.peernper = 3
          ;;
        label: "Bi-Weekly"
      }

      when: {
        sql: ${TABLE}.peernper = 4
          ;;
        label: "Twice Monthly"
      }

      when: {
        sql: ${TABLE}.peernper = 5
          ;;
        label: "Monthly"
      }

      when: {
        sql: ${TABLE}.peernper = 6
          ;;
        label: "Annually"
      }

      when: {
        sql: ${TABLE}.peernper = 7
          ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: peernrt {
    label: "Paid hourly,y/n"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peernrt = -3
          ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.peernrt = -2
          ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.peernrt = -1
          ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.peernrt = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peernrt = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peernuot {
    label: "Overtime pay/tips/commission,y/n"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peernuot = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peernuot = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peernuot = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peernwkp {
    label: "#paid weeks per year"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [1, 14, 27, 40, 53]
    style: classic
    sql: CASE WHEN ${TABLE}.peernwkp between 1 AND 52 THEN ${TABLE}.peernwkp END ;;
  }

  dimension: pternhly {
    label: "Hourly pay rate,amount-recode"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 25, 50, 75, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pternhly between 0.0 AND 99.99 THEN ${TABLE}.pternhly END ;;
  }

  dimension: pternwa {
    label: "Weekly earnings,amount-recode"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 722, 1443, 2164, 2885]
    style: classic
    sql: CASE WHEN ${TABLE}.pternwa between 0.0 AND 2884.61 THEN ${TABLE}.pternwa END ;;
  }

  dimension: prhernal {
    label: "Persons hourly earnings recode allocation flag"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prhernal = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prhernal = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No Components Allocated"
      }

      when: {
        sql: ${TABLE}.prhernal = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "1 or more components Allocated"
      }
    }
  }

  dimension: prwernal {
    label: "(person)weekly earnings allocation flag-recode"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prwernal = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prwernal = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No Component Allocated"
      }

      when: {
        sql: ${TABLE}.prwernal = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "1 or more components Allocated"
      }
    }
  }

  dimension: pthr {
    label: "Hourly pay-top code"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pthr = 0
          ;;
        label: "Not Topcoded"
      }

      when: {
        sql: ${TABLE}.pthr = 1
          ;;
        label: "Topcoded"
      }
    }
  }

  dimension: ptot {
    label: "Weekly overtime,amount-top code flag"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptot = 0
          ;;
        label: "Not Topcoded"
      }

      when: {
        sql: ${TABLE}.ptot = 1
          ;;
        label: "Topcoded"
      }
    }
  }

  dimension: ptwk {
    label: "Weekly-top code flag"
    view_label: "Cohort Earnings Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptwk = 0
          ;;
        label: "Not Topcoded"
      }

      when: {
        sql: ${TABLE}.ptwk = 1
          ;;
        label: "Topcoded"
      }
    }
  }

  dimension: ptern2 {
    label: "(calculated)weekly overtime"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 722, 1443, 2164, 2885]
    style: classic
    sql: CASE WHEN ${TABLE}.ptern2 between 0.0 AND 2884.61 THEN ${TABLE}.ptern2 END ;;
  }

  dimension: pternh1c {
    label: "Hourly pay rate,excluding overtime"
    view_label: "Cohort Earnings Variables"
    type: tier
    tiers: [0, 25, 50, 75, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.pternh1c between 0.0 AND 99.99 THEN ${TABLE}.pternh1c END ;;
  }

  dimension: peschenr {
    label: "In high school/college/university,y/n"
    view_label: "Cohort Basic CPS School Enrollment Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peschenr = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peschenr = 1
          ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peschenr = 2
          ;;
        label: "No"
      }
    }
  }

  dimension: peschft {
    label: "Full-time/part-time student"
    view_label: "Cohort Basic CPS School Enrollment Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peschft = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peschft = 1
          ;;
        label: "FULL-TIME"
      }

      when: {
        sql: ${TABLE}.peschft = 2
          ;;
        label: "PART-TIME"
      }
    }
  }

  dimension: peschlvl {
    label: "In high school/college/university"
    view_label: "Cohort Basic CPS School Enrollment Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peschlvl = -1
          ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peschlvl = 1
          ;;
        label: "HIGH SCHOOL"
      }

      when: {
        sql: ${TABLE}.peschlvl = 2
          ;;
        label: "COLLEGE OR UNIVERSITY"
      }
    }
  }

  dimension: prnlfsch {
    label: "(not in labor force)school/not in school"
    view_label: "Cohort Basic CPS School Enrollment Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prnlfsch = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prnlfsch = 1
          ;;
        label: "IN SCHOOL"
      }

      when: {
        sql: ${TABLE}.prnlfsch = 2
          ;;
        label: "NOT IN SCHOOL"
      }
    }
  }

  dimension: gereg {
    label: "Region"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gereg = 1
          ;;
        label: "Northeast"
      }

      when: {
        sql: ${TABLE}.gereg = 2
          ;;
        label: "Midwest (formerly North Central)"
      }

      when: {
        sql: ${TABLE}.gereg = 3
          ;;
        label: "South"
      }

      when: {
        sql: ${TABLE}.gereg = 4
          ;;
        label: "West"
      }
    }
  }

  dimension: pedipged {
    label: "High school,graduation/ged"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedipged = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedipged = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Graduation from High School"
      }

      when: {
        sql: ${TABLE}.pedipged = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "GED or  other Equivalent"
      }
    }
  }

  dimension: pehgcomp {
    label: "Highest grade completed before ged"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehgcomp = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Less than 1st grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "1st, 2nd, 3rd, or 4th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "5th or 6th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "7th or 8th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "9th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "10th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "11th Grade"
      }

      when: {
        sql: ${TABLE}.pehgcomp = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "12th Grade, NO DIPLOMA"
      }
    }
  }

  dimension: pecyc {
    label: "Years of college credit completed"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pecyc = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pecyc = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Less than 1 year (includes 0 years completed)?"
      }

      when: {
        sql: ${TABLE}.pecyc = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "The first, or Freshman year?"
      }

      when: {
        sql: ${TABLE}.pecyc = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "The second, or Sophomore year?"
      }

      when: {
        sql: ${TABLE}.pecyc = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "The third, or Junior year?"
      }

      when: {
        sql: ${TABLE}.pecyc = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Four or more years?"
      }
    }
  }

  dimension: pegrprof {
    label: "(have b.s./b.a.)taken graduate/prof. courses"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pegrprof = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pegrprof = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pegrprof = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "No"
      }
    }
  }

  dimension: pegr6cor {
    label: "Completed 6 or more graduate courses,y/n"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pegr6cor = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pegr6cor = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pegr6cor = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "No"
      }
    }
  }

  dimension: pems123 {
    label: "Master's program 1, 2, or 3 years"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pems123 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pems123 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "1 year program"
      }

      when: {
        sql: ${TABLE}.pems123 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "2 year program"
      }

      when: {
        sql: ${TABLE}.pems123 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998')
           ;;
        label: "3 year program (or longer)"
      }
    }
  }

  dimension: hryear4 {
    label: "4 digit year of interview"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1998, 2748, 3498, 4248, 4997]
    style: classic
    sql: CASE WHEN ${TABLE}.hryear4 between 1998 AND 2999 THEN ${TABLE}.hryear4 END ;;
  }

  dimension: prchld {
    label: "Presence of own children <18 years by age group"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prchld = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "NIU (Not a parent)"
      }

      when: {
        sql: ${TABLE}.prchld = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "No own children under 18 years of age"
      }

      when: {
        sql: ${TABLE}.prchld = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "All own children 0- 2 years of age"
      }

      when: {
        sql: ${TABLE}.prchld = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "All own chldrn 3- 5 years of age"
      }

      when: {
        sql: ${TABLE}.prchld = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "All own chldrn 6-13 years of age"
      }

      when: {
        sql: ${TABLE}.prchld = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "All own chldrn 14-17 years of age"
      }

      when: {
        sql: ${TABLE}.prchld = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 0-2,3-5 years of age (none  6-17)"
      }

      when: {
        sql: ${TABLE}.prchld = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 0-2,6-13 years of age (none  3- 5 or 14-17)"
      }

      when: {
        sql: ${TABLE}.prchld = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 0-2,14-17 years of age (none  3-13)"
      }

      when: {
        sql: ${TABLE}.prchld = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 3-5,6-13 years of age (none  0- 2 or 14-17)"
      }

      when: {
        sql: ${TABLE}.prchld = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 3-5,14-17 years of age (none  0- 2 or  6-13)"
      }

      when: {
        sql: ${TABLE}.prchld = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own children 6-13 and 14-17 years of age (none  0- 5)"
      }

      when: {
        sql: ${TABLE}.prchld = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own children 0-2, 3- 5, and  6-13 years of age (none 14-17)"
      }

      when: {
        sql: ${TABLE}.prchld = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own chldrn 0-2, 3- 5, and 14-17 years of age (none 6-13)"
      }

      when: {
        sql: ${TABLE}.prchld = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own childrn 0-2, 6-13, and 14-17 years of age (none 3- 5)"
      }

      when: {
        sql: ${TABLE}.prchld = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own children 3-5, 6-13, and 14-17 years of age (none  0- 2)"
      }

      when: {
        sql: ${TABLE}.prchld = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000')
           ;;
        label: "Own children from all age groups"
      }
    }
  }

  dimension: prnmchld {
    label: "Number of own children <18 years of age"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.prnmchld between 0 AND 99 THEN ${TABLE}.prnmchld END ;;
  }

  dimension: pehspnon {
    label: "Hispanic origin"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pehspnon = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hispanic"
      }

      when: {
        sql: ${TABLE}.pehspnon = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Non-Hispanic"
      }
    }
  }

  dimension: prmlr {
    label: "Employment status recode"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmlr = 1
          ;;
        label: "Employed - {1 ,2 }"
      }

      when: {
        sql: ${TABLE}.prmlr = 2
          ;;
        label: "Unemployed - {3 ,4 }"
      }

      when: {
        sql: ${TABLE}.prmlr = 3
          ;;
        label: "Not in Labor Force - {5 ,6 ,7 }"
      }
    }
  }

  dimension: prmjind1 {
    label: "(main job)industry,major groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agriculture, forestry, fishing, and hunting"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Manufacturing"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale and retail trade"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and utilities"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial activities"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and business services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational and health services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Leisure and hospitality"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjind2 {
    label: "(second job)industry,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agriculture, forestry, fishing, and hunting"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Manufacturing"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale and retail trade"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and utilities"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial activities"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and business services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational and health services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Leisure and hospitality"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc1 {
    label: "(main job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management, business, and financial occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and related occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Service occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and related occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farming, fishing, and forestry occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction and extraction occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Installation, maintenance, and repair occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and material moving occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc2 {
    label: "(second job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management, business, and financial occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and related occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Service occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and related occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farming, fishing, and forestry occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction and extraction occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Installation, maintenance, and repair occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and material moving occupations"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prcow1 {
    label: "(main job)class of worker-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcow1 = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prcow1 = 1
          ;;
        label: "Federal govt"
      }

      when: {
        sql: ${TABLE}.prcow1 = 2
          ;;
        label: "State govt"
      }

      when: {
        sql: ${TABLE}.prcow1 = 3
          ;;
        label: "Local govt"
      }

      when: {
        sql: ${TABLE}.prcow1 = 4
          ;;
        label: "Private (incl. self-employed incorp.)"
      }

      when: {
        sql: ${TABLE}.prcow1 = 5
          ;;
        label: "Self-employed, unincorp."
      }

      when: {
        sql: ${TABLE}.prcow1 = 6
          ;;
        label: "Without pay"
      }
    }
  }

  dimension: prcow2 {
    label: "(second job)class of worker-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcow2 = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prcow2 = 1
          ;;
        label: "Federal govt"
      }

      when: {
        sql: ${TABLE}.prcow2 = 2
          ;;
        label: "State govt"
      }

      when: {
        sql: ${TABLE}.prcow2 = 3
          ;;
        label: "Local govt"
      }

      when: {
        sql: ${TABLE}.prcow2 = 4
          ;;
        label: "Private (incl. self-employed incorp.)"
      }

      when: {
        sql: ${TABLE}.prcow2 = 5
          ;;
        label: "Self-employed, unincorp."
      }

      when: {
        sql: ${TABLE}.prcow2 = 6
          ;;
        label: "Without pay"
      }
    }
  }

  dimension: prdtcow1 {
    label: "(main job)detailed class of worker"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtcow1 = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 1
          ;;
        label: "Agri.,wage & Salary,private"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 2
          ;;
        label: "Agri,wage & Salary, Government"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 3
          ;;
        label: "Agri., Self-Employed"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 4
          ;;
        label: "Agri., Unpaid"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 5
          ;;
        label: "Nonag,ws,private,private Hhlds"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 6
          ;;
        label: "Nonag,ws,priv.,other Private"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 7
          ;;
        label: "Nonag,ws,govt,federal"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 8
          ;;
        label: "Nonag,ws,govt,state"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 9
          ;;
        label: "Nonag,ws,govt,local"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 10
          ;;
        label: "Nonag, Self-Employed"
      }

      when: {
        sql: ${TABLE}.prdtcow1 = 11
          ;;
        label: "Nonag, Unpaid"
      }
    }
  }

  dimension: prdtcow2 {
    label: "(second job)detailed class of worker-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtcow2 = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 1
          ;;
        label: "Agri.,wage & Salary,private"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 2
          ;;
        label: "Agri,wage & Salary, Government"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 3
          ;;
        label: "Agri., Self-Employed"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 4
          ;;
        label: "Agri., Unpaid"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 5
          ;;
        label: "Nonag,ws,private,private Hhlds"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 6
          ;;
        label: "Nonag,ws,priv.,other Private"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 7
          ;;
        label: "Nonag,ws,govt,federal"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 8
          ;;
        label: "Nonag,ws,govt,state"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 9
          ;;
        label: "Nonag,ws,govt,local"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 10
          ;;
        label: "Nonag, Self-Employed"
      }

      when: {
        sql: ${TABLE}.prdtcow2 = 11
          ;;
        label: "Nonag, Unpaid"
      }
    }
  }

  dimension: prdtind1 {
    label: "(main job)detailed industry"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtind1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forestry, logging, fishing, hunting, and trapping"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetallic mineral products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Primary metals and fabricated metal products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machinery manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and electronic products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical equipment, appliance manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wood products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furniture and fixtures manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous and not specified manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Beverage and tobacco products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile, apparel, and leather manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paper and printing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Petroleum and coal products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Plastics and rubber products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Retail trade"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and warehousing"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 24
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Utilities"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 25
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Publishing industries (except internet)"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 26
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Motion picure and sound recording industries"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 27
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Broadcasting (except internet)"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 28
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Internet publishing and broadcasting"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 29
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Telecommunications"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 30
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Internet service providers and data processing services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 31
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other information services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 32
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Finance"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 33
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 34
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Real Estate"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 35
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Rental and leasing Services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 36
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and technical services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 37
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management of companies and enterprises"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 38
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administrative and support services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 39
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waste management and remediation services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 40
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 41
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 42
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health care services, except hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 43
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social assistance"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 44
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, entertainment, and recreation"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 45
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accommodation"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 46
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food services and drinking places"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 47
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Repair and maintenance"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 48
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal and laundry services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 49
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Membership associations and organizations"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 50
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private households"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 51
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 52
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtind2 {
    label: "(second job)detailed industry-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtind2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forestry, logging, fishing, hunting, and trapping"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetallic mineral products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Primary metals and fabricated metal products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machinery manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and electronic products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical equipment, appliance manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wood products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furniture and fixtures manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous and not specified manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Beverage and tobacco products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile, apparel, and leather manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paper and printing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Petroleum and coal products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical manufacturing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Plastics and rubber products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Retail trade"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and warehousing"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 24
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Utilities"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 25
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Publishing industries (except internet)"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 26
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Motion picure and sound recording industries"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 27
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Broadcasting (except internet)"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 28
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Internet publishing and broadcasting"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 29
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Telecommunications"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 30
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Internet service providers and data processing services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 31
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other information services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 32
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Finance"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 33
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 34
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Real Estate"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 35
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Rental and leasing Services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 36
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and technical services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 37
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management of companies and enterprises"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 38
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administrative and support services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 39
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waste management and remediation services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 40
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 41
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 42
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health care services, except hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 43
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social assistance"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 44
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, entertainment, and recreation"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 45
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accommodation"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 46
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food services and drinking places"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 47
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Repair and maintenance"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 48
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal and laundry services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 49
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Membership associations and organizations"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 50
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private households"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 51
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 52
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtocc1 {
    label: "(main job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Business and financial operations occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and mathematical science occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Architecture and engineering occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Life, physical, and social service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Community and social service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Legal occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Education, training, and library occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, design, entertainment, sports, and media occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Healthcare practitioner and technical occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Healthcare support occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Protective service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food preparation and serving related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Building and grounds cleaning and maintenance occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal care and service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farming, fishing, and forestry occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction and extraction occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Installation, maintenance, and repair occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and material moving occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtocc2 {
    label: "(second job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Business and financial operations occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and mathematical science occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Architecture and engineering occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Life, physical, and social service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Community and social service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Legal occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Education, training, and library occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, design, entertainment, sports, and media occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Healthcare practitioner and technical occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Healthcare support occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Protective service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food preparation and serving related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Building and grounds cleaning and maintenance occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal care and service occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farming, fishing, and forestry occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction and extraction occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Installation, maintenance, and repair occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and material moving occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: premp {
    label: "Employed non-farm/non-private hhld industries"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.premp = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.premp = 1
          ;;
        label: "Employed Persons (exc. Farm & Priv HH)"
      }
    }
  }

  dimension: prnagws {
    label: "Non-agriculture/salary workers"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prnagws = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prnagws = 1
          ;;
        label: "Non-Ag Wage And Salary Workers"
      }
    }
  }

  dimension: prnagpws {
    label: "Non-agriculture/private/salary workers-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prnagpws = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prnagpws = 1
          ;;
        label: "Non-Ag Priv Wage & Salary (ex Priv HH)"
      }
    }
  }

  dimension: pragna {
    label: "Industry,agriculture/non-agriculture"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pragna = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.pragna = 1
          ;;
        label: "Agricultural"
      }

      when: {
        sql: ${TABLE}.pragna = 2
          ;;
        label: "Non-Agricultural"
      }
    }
  }

  dimension: prsjmj {
    label: "Single/multiple jobholder-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prsjmj = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prsjmj = 1
          ;;
        label: "Single jobholder"
      }

      when: {
        sql: ${TABLE}.prsjmj = 2
          ;;
        label: "Multiple jobholder"
      }
    }
  }

  dimension: prcowpg {
    label: "(main job)class of worker,private/government"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prcowpg = -1
          ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prcowpg = 1
          ;;
        label: "Private"
      }

      when: {
        sql: ${TABLE}.prcowpg = 2
          ;;
        label: "Government"
      }
    }
  }

  dimension: prmjocgr {
    label: "(main job)occupation,7 groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocgr = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management, professional, and related occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Service Occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and office occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farming, fishing, and forestry occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction, and maintenance occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production, transportation, and material moving occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prioelg {
    label: "Industry and occupation edit eligibility flag"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prioelg = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prioelg = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not eligible for I&O edit"
      }

      when: {
        sql: ${TABLE}.prioelg = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Eligible for I&O edit"
      }
    }
  }

  dimension: primind1 {
    label: "(main job)detailed industry"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.primind1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.primind1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural, forestry, fishing, and hunting"
      }

      when: {
        sql: ${TABLE}.primind1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.primind1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.primind1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Durable goods manufacturing"
      }

      when: {
        sql: ${TABLE}.primind1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nondurable goods manufacturing"
      }

      when: {
        sql: ${TABLE}.primind1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.primind1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Retail trade"
      }

      when: {
        sql: ${TABLE}.primind1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and warehousing"
      }

      when: {
        sql: ${TABLE}.primind1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Utilities"
      }

      when: {
        sql: ${TABLE}.primind1 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information"
      }

      when: {
        sql: ${TABLE}.primind1 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Finance and insurance"
      }

      when: {
        sql: ${TABLE}.primind1 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Real estate and rental and leasing"
      }

      when: {
        sql: ${TABLE}.primind1 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and technical services"
      }

      when: {
        sql: ${TABLE}.primind1 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management, administrative and waste management services"
      }

      when: {
        sql: ${TABLE}.primind1 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.primind1 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health care and social assistance"
      }

      when: {
        sql: ${TABLE}.primind1 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, enterainment, and recreation"
      }

      when: {
        sql: ${TABLE}.primind1 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accommodation and food services"
      }

      when: {
        sql: ${TABLE}.primind1 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private households"
      }

      when: {
        sql: ${TABLE}.primind1 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other services, except private households"
      }

      when: {
        sql: ${TABLE}.primind1 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.primind1 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: primind2 {
    label: "(second job)detailed industry"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.primind2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.primind2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural, forestry, fishing, and hunting"
      }

      when: {
        sql: ${TABLE}.primind2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.primind2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.primind2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Durable goods manufacturing"
      }

      when: {
        sql: ${TABLE}.primind2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nondurable goods manufacturing"
      }

      when: {
        sql: ${TABLE}.primind2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.primind2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Retail trade"
      }

      when: {
        sql: ${TABLE}.primind2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation and warehousing"
      }

      when: {
        sql: ${TABLE}.primind2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Utilities"
      }

      when: {
        sql: ${TABLE}.primind2 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information"
      }

      when: {
        sql: ${TABLE}.primind2 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Finance and insurance"
      }

      when: {
        sql: ${TABLE}.primind2 = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Real estate and rental and leasing"
      }

      when: {
        sql: ${TABLE}.primind2 = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Professional and technical services"
      }

      when: {
        sql: ${TABLE}.primind2 = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management, administrative and waste management services"
      }

      when: {
        sql: ${TABLE}.primind2 = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.primind2 = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health care and social assistance"
      }

      when: {
        sql: ${TABLE}.primind2 = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Arts, enterainment, and recreation"
      }

      when: {
        sql: ${TABLE}.primind2 = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accommodation and food services"
      }

      when: {
        sql: ${TABLE}.primind2 = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private households"
      }

      when: {
        sql: ${TABLE}.primind2 = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other services, except private households"
      }

      when: {
        sql: ${TABLE}.primind2 = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public administration"
      }

      when: {
        sql: ${TABLE}.primind2 = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: preduca4 {
    label: "Recode - 4 categories"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.preduca4 = 1
          ;;
        label: "Less than a high school diploma - {31, 32, 33, 34, 35, 36, 37, 38 }"
      }

      when: {
        sql: ${TABLE}.preduca4 = 2
          ;;
        label: "High school graduates, no college - {39 }"
      }

      when: {
        sql: ${TABLE}.preduca4 = 3
          ;;
        label: "Some college or associate degree - {40, 41, 42 }"
      }

      when: {
        sql: ${TABLE}.preduca4 = 4
          ;;
        label: "Bachelor's degree or higher - {43, 44, 45, 46 }"
      }
    }
  }

  dimension: preduca5 {
    label: "Recode - 5 categories"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.preduca5 = 1
          ;;
        label: "Less than a high school diploma - {31, 32, 33, 34, 35, 36, 37, 38 }"
      }

      when: {
        sql: ${TABLE}.preduca5 = 2
          ;;
        label: "High school graduates, no college - {39 }"
      }

      when: {
        sql: ${TABLE}.preduca5 = 3
          ;;
        label: "Some college, no degree - {40 }"
      }

      when: {
        sql: ${TABLE}.preduca5 = 4
          ;;
        label: "Associate degree - {41, 42 }"
      }

      when: {
        sql: ${TABLE}.preduca5 = 5
          ;;
        label: "Bachelor's degree or higher - {43, 44, 45, 46 }"
      }
    }
  }

  dimension: gtcbsast {
    label: "Principal city/balance status"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtcbsast = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Principal City"
      }

      when: {
        sql: ${TABLE}.gtcbsast = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Balance Metropolitan"
      }

      when: {
        sql: ${TABLE}.gtcbsast = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetroploitan"
      }

      when: {
        sql: ${TABLE}.gtcbsast = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not Identified"
      }
    }
  }

  dimension: gtmetsta {
    label: "Metropolitan status"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtmetsta = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metropolitan"
      }

      when: {
        sql: ${TABLE}.gtmetsta = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetropolitan"
      }

      when: {
        sql: ${TABLE}.gtmetsta = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not Identified"
      }
    }
  }

  dimension: gtindvpc {
    label: "Individual central city code"
    view_label: "Cohort Geography Variables"
    type: tier
    tiers: [1, 3, 5, 7, 8]
    style: classic
    sql: CASE WHEN ${TABLE}.gtindvpc between 1 AND 7 THEN ${TABLE}.gtindvpc END ;;
  }

  dimension: gtcbsasz {
    label: "Metropolitan statistical area size"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtcbsasz = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not Identified or NonMetropolitan"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "100,000 - 249,999"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "250,000 - 499,999"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "500,000 - 999,999"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "1,000,000 - 2,499,999"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "2,500,000 - 4,999,999"
      }

      when: {
        sql: ${TABLE}.gtcbsasz = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "5,000,000+"
      }
    }
  }

  dimension: hutypc {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Removed during subsampling"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unit already had a chance of selection"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: huprscnt {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt between 1 AND 99 THEN ${TABLE}.huprscnt END ;;
  }

  dimension: hufinal {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Other - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 256
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Removed during Subsampling"
      }

      when: {
        sql: ${TABLE}.hufinal = 257
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Unit already had a chance of selection"
      }
    }
  }

  dimension: peafever {
    label: "Ever serve on active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafever = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.peafever = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "No"
      }
    }
  }

  dimension: peafwhn1 {
    label: "Past military service, period of active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafwhn1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "September 2001 or later"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "August 1990 to August 2001"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "May 1975 to July 1990"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vietnam Era (August 1964 to April 1975)"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "February 1955 to July 1964"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Korean War (July 1950) to January 1955)"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "January 1947 to June 1950"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "World War II (December 1941 to December 1946)"
      }

      when: {
        sql: ${TABLE}.peafwhn1 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "November 1941 or earlier"
      }
    }
  }

  dimension: peafwhn2 {
    label: "Past military service, period of active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafwhn2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "September 2001 or later"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "August 1990 to August 2001"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "May 1975 to July 1990"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vietnam Era (August 1964 to April 1975)"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "February 1955 to July 1964"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Korean War (July 1950) to January 1955)"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "January 1947 to June 1950"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "World War II (December 1941 to December 1946)"
      }

      when: {
        sql: ${TABLE}.peafwhn2 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "November 1941 or earlier"
      }
    }
  }

  dimension: peafwhn3 {
    label: "Past military service, period of active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafwhn3 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "September 2001 or later"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "August 1990 to August 2001"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "May 1975 to July 1990"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vietnam Era (August 1964 to April 1975)"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "February 1955 to July 1964"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Korean War (July 1950) to January 1955)"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "January 1947 to June 1950"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "World War II (December 1941 to December 1946)"
      }

      when: {
        sql: ${TABLE}.peafwhn3 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "November 1941 or earlier"
      }
    }
  }

  dimension: peafwhn4 {
    label: "Past military service, period of active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafwhn4 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "September 2001 or later"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "August 1990 to August 2001"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "May 1975 to July 1990"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Vietnam Era (August 1964 to April 1975)"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "February 1955 to July 1964"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "Korean War (July 1950) to January 1955)"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "January 1947 to June 1950"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "World War II (December 1941 to December 1946)"
      }

      when: {
        sql: ${TABLE}.peafwhn4 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006')
           ;;
        label: "November 1941 or earlier"
      }
    }
  }

  dimension: pedadtyp {
    label: "Type of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedadtyp = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "No Father Present"
      }

      when: {
        sql: ${TABLE}.pedadtyp = 01
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Biological"
      }

      when: {
        sql: ${TABLE}.pedadtyp = 02
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Step"
      }

      when: {
        sql: ${TABLE}.pedadtyp = 03
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Adopted"
      }
    }
  }

  dimension: pelnmom {
    label: "Line number of mother"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 4, 8, 12, 16]
    style: classic
    sql: CASE WHEN ${TABLE}.pelnmom between 0 AND 16 THEN ${TABLE}.pelnmom END ;;
  }

  dimension: pelndad {
    label: "Line number of father"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 4, 8, 12, 16]
    style: classic
    sql: CASE WHEN ${TABLE}.pelndad between 0 AND 16 THEN ${TABLE}.pelndad END ;;
  }

  dimension: pemomtyp {
    label: "Type of mother"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemomtyp = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "No Mother Present"
      }

      when: {
        sql: ${TABLE}.pemomtyp = 01
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Biological"
      }

      when: {
        sql: ${TABLE}.pemomtyp = 02
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Step"
      }

      when: {
        sql: ${TABLE}.pemomtyp = 03
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008')
           ;;
        label: "Adopted"
      }
    }
  }

  dimension: pecohab {
    label: "Line number of cohabiting partner"
    view_label: "Cohort Demographic Variables"
    type: tier
    tiers: [0, 4, 8, 12, 16]
    style: classic
    sql: CASE WHEN ${TABLE}.pecohab between 0 AND 16 THEN ${TABLE}.pecohab END ;;
  }

  dimension: pedisear {
    label: "Disability - deaf or serious difficulty hearing"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedisear = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedisear = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedisear = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: pediseye {
    label: "Disability - blind or difficulty seeing even with glasses"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pediseye = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pediseye = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pediseye = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: pedisrem {
    label: "Disability - difficulty remembering or making decisions"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedisrem = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedisrem = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedisrem = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: pedisphy {
    label: "Disability - difficulty walking or climbing stairs"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedisphy = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedisphy = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedisphy = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: pedisdrs {
    label: "Disability - difficulty dressing or bathing"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedisdrs = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedisdrs = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedisdrs = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: pedisout {
    label: "Disability - difficulty doing errands"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pedisout = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pedisout = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pedisout = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: prdisflg {
    label: "Disability - recode, disabled"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdisflg = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdisflg = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.prdisflg = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No"
      }
    }
  }

  dimension: hutypea {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Language barrier"
      }

      when: {
        sql: ${TABLE}.hutypea = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Unable to locate"
      }

      when: {
        sql: ${TABLE}.hutypea = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: hefaminc {
    label: "Total family income in past 12 months"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hefaminc = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Less Than $5,000"
      }

      when: {
        sql: ${TABLE}.hefaminc = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "5,000 To 7,499"
      }

      when: {
        sql: ${TABLE}.hefaminc = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "7,500 To 9,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "10,000 To 12,499"
      }

      when: {
        sql: ${TABLE}.hefaminc = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "12,500 To 14,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "15,000 To 19,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "20,000 To 24,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "25,000 To 29,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "30,000 To 34,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "35,000 To 39,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "40,000 To 49,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "50,000 To 59,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "60,000 To 74,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "75,000 To 99,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "100,000 To 149,999"
      }

      when: {
        sql: ${TABLE}.hefaminc = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "150,000 or More"
      }
    }
  }

  dimension: ptdtrace {
    label: "Demographics- race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptdtrace = 01
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "White only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 02
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Black only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 03
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "American Indian, Alaskan Native Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 04
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Asian only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 05
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Hawaiian/Pacific Islander Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 06
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "White-Black"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 07
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "White-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 08
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "White-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 09
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "White-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Black-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Black-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 12
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Black-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 13
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "AI-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 14
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "AI-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 15
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Asian-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 16
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-B-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 17
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-B-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 18
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-B-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 19
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 20
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-AI-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 21
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 22
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 23
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 24
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "W-AI-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 25
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Other 3 Race Combinations"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 26
          AND ${TABLE}.src_table in ('census_2014', 'census_2012')
           ;;
        label: "Other 4 and 5 Race Combinations"
      }
    }
  }

  dimension: penatvty {
    label: "Demographics - native country of sample person"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penatvty = 057
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.penatvty = 066
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guam"
      }

      when: {
        sql: ${TABLE}.penatvty = 073
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.penatvty = 078
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "U. S. Virgin Islands"
      }

      when: {
        sql: ${TABLE}.penatvty = 096
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Other U. S. Island Areas"
      }

      when: {
        sql: ${TABLE}.penatvty = 100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Albania"
      }

      when: {
        sql: ${TABLE}.penatvty = 102
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.penatvty = 103
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.penatvty = 104
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bulgaria"
      }

      when: {
        sql: ${TABLE}.penatvty = 105
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 106
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.penatvty = 108
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.penatvty = 109
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.penatvty = 110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.penatvty = 116
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.penatvty = 117
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.penatvty = 119
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.penatvty = 126
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Netherlands"
      }

      when: {
        sql: ${TABLE}.penatvty = 127
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.penatvty = 128
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.penatvty = 129
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.penatvty = 130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.penatvty = 132
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.penatvty = 134
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.penatvty = 136
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.penatvty = 137
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.penatvty = 138
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United Kingdom"
      }

      when: {
        sql: ${TABLE}.penatvty = 139
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.penatvty = 140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.penatvty = 141
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Wales"
      }

      when: {
        sql: ${TABLE}.penatvty = 142
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 147
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.penatvty = 148
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 149
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Slovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bosnia & Herzegovina"
      }

      when: {
        sql: ${TABLE}.penatvty = 151
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Croatia"
      }

      when: {
        sql: ${TABLE}.penatvty = 152
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Macedonia"
      }

      when: {
        sql: ${TABLE}.penatvty = 154
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Serbia"
      }

      when: {
        sql: ${TABLE}.penatvty = 156
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.penatvty = 157
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.penatvty = 158
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.penatvty = 159
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azerbaijan"
      }

      when: {
        sql: ${TABLE}.penatvty = 160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belarus"
      }

      when: {
        sql: ${TABLE}.penatvty = 161
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Georgia"
      }

      when: {
        sql: ${TABLE}.penatvty = 162
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Moldova"
      }

      when: {
        sql: ${TABLE}.penatvty = 163
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.penatvty = 164
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.penatvty = 165
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.penatvty = 166
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Europe,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 167
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kosovo"
      }

      when: {
        sql: ${TABLE}.penatvty = 200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 202
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.penatvty = 205
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Myanmar (Burma)"
      }

      when: {
        sql: ${TABLE}.penatvty = 206
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.penatvty = 207
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.penatvty = 208
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cyprus"
      }

      when: {
        sql: ${TABLE}.penatvty = 209
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.penatvty = 210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.penatvty = 211
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.penatvty = 212
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.penatvty = 213
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.penatvty = 214
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.penatvty = 215
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.penatvty = 216
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.penatvty = 217
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 222
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kuwait"
      }

      when: {
        sql: ${TABLE}.penatvty = 223
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.penatvty = 224
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.penatvty = 226
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.penatvty = 229
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nepal"
      }

      when: {
        sql: ${TABLE}.penatvty = 231
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 233
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.penatvty = 235
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.penatvty = 236
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.penatvty = 238
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sri Lanka"
      }

      when: {
        sql: ${TABLE}.penatvty = 239
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.penatvty = 240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.penatvty = 242
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.penatvty = 243
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.penatvty = 246
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uzbekistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 247
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.penatvty = 248
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yemen"
      }

      when: {
        sql: ${TABLE}.penatvty = 249
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Asia,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 301
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.penatvty = 303
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.penatvty = 310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.penatvty = 311
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.penatvty = 312
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.penatvty = 313
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.penatvty = 314
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.penatvty = 315
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.penatvty = 316
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.penatvty = 321
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Antigua and Barbuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 323
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.penatvty = 324
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.penatvty = 327
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.penatvty = 328
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.penatvty = 329
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.penatvty = 332
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.penatvty = 333
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.penatvty = 338
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Kitts--Nevis"
      }

      when: {
        sql: ${TABLE}.penatvty = 339
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Lucia"
      }

      when: {
        sql: ${TABLE}.penatvty = 340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Vincent and the Grenadines"
      }

      when: {
        sql: ${TABLE}.penatvty = 341
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Trinidad and Tobago"
      }

      when: {
        sql: ${TABLE}.penatvty = 343
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "West Indies,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.penatvty = 361
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.penatvty = 362
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.penatvty = 363
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.penatvty = 364
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Columbia"
      }

      when: {
        sql: ${TABLE}.penatvty = 365
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.penatvty = 368
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.penatvty = 369
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Paraguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 370
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.penatvty = 372
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 373
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.penatvty = 374
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South America,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 399
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Americas,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Algeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 407
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cameroon"
      }

      when: {
        sql: ${TABLE}.penatvty = 408
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cape Verde"
      }

      when: {
        sql: ${TABLE}.penatvty = 414
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.penatvty = 416
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.penatvty = 417
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Eritrea"
      }

      when: {
        sql: ${TABLE}.penatvty = 421
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.penatvty = 427
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.penatvty = 429
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Liberia"
      }

      when: {
        sql: ${TABLE}.penatvty = 436
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.penatvty = 440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 444
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Senegal"
      }

      when: {
        sql: ${TABLE}.penatvty = 447
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sierra Leone"
      }

      when: {
        sql: ${TABLE}.penatvty = 448
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Somalia"
      }

      when: {
        sql: ${TABLE}.penatvty = 449
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 451
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sudan"
      }

      when: {
        sql: ${TABLE}.penatvty = 453
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tanzania"
      }

      when: {
        sql: ${TABLE}.penatvty = 457
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uganda"
      }

      when: {
        sql: ${TABLE}.penatvty = 461
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Zimbabwe"
      }

      when: {
        sql: ${TABLE}.penatvty = 462
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Africa,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 501
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.penatvty = 508
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.penatvty = 515
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.penatvty = 523
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tonga"
      }

      when: {
        sql: ${TABLE}.penatvty = 527
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Samoa"
      }

      when: {
        sql: ${TABLE}.penatvty = 528
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Oceania,not specified"
      }

      when: {
        sql: ${TABLE}.penatvty = 555
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Elsewhere"
      }
    }
  }

  dimension: pefntvty {
    label: "Demographics - native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 057
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 066
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 073
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 078
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "U. S. Virgin Islands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 096
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Other U. S. Island Areas"
      }

      when: {
        sql: ${TABLE}.pefntvty = 100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Albania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 104
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bulgaria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United Kingdom"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 141
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Wales"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 149
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Slovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bosnia & Herzegovina"
      }

      when: {
        sql: ${TABLE}.pefntvty = 151
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Croatia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 152
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Macedonia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 154
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Serbia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 157
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 158
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 159
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azerbaijan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belarus"
      }

      when: {
        sql: ${TABLE}.pefntvty = 161
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Georgia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 162
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Moldova"
      }

      when: {
        sql: ${TABLE}.pefntvty = 163
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 164
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 165
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 166
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Europe,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 167
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kosovo"
      }

      when: {
        sql: ${TABLE}.pefntvty = 200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 202
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pefntvty = 205
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Myanmar (Burma)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 206
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 207
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pefntvty = 208
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cyprus"
      }

      when: {
        sql: ${TABLE}.pefntvty = 209
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pefntvty = 210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pefntvty = 211
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 212
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pefntvty = 213
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pefntvty = 214
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pefntvty = 215
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 216
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 217
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 222
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kuwait"
      }

      when: {
        sql: ${TABLE}.pefntvty = 223
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pefntvty = 224
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 226
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 229
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nepal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 231
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 233
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 235
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 236
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pefntvty = 238
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sri Lanka"
      }

      when: {
        sql: ${TABLE}.pefntvty = 239
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 242
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 243
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pefntvty = 246
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uzbekistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 247
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 248
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yemen"
      }

      when: {
        sql: ${TABLE}.pefntvty = 249
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Asia,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 301
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 303
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pefntvty = 311
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 312
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 313
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pefntvty = 314
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pefntvty = 315
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pefntvty = 316
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pefntvty = 321
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Antigua and Barbuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 323
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pefntvty = 324
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pefntvty = 327
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pefntvty = 328
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 329
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 332
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pefntvty = 333
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 338
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Kitts--Nevis"
      }

      when: {
        sql: ${TABLE}.pefntvty = 339
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Lucia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Vincent and the Grenadines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 341
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Trinidad and Tobago"
      }

      when: {
        sql: ${TABLE}.pefntvty = 343
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "West Indies,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pefntvty = 361
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 362
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pefntvty = 363
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pefntvty = 364
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Columbia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 365
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 368
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 369
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Paraguay"
      }

      when: {
        sql: ${TABLE}.pefntvty = 370
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pefntvty = 372
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pefntvty = 373
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pefntvty = 374
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South America,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 399
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Americas,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Algeria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 407
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cameroon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 408
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cape Verde"
      }

      when: {
        sql: ${TABLE}.pefntvty = 414
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pefntvty = 416
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 417
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Eritrea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 421
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 427
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pefntvty = 429
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Liberia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 436
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pefntvty = 440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 444
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Senegal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 447
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sierra Leone"
      }

      when: {
        sql: ${TABLE}.pefntvty = 448
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Somalia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 449
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 451
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sudan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 453
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tanzania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 457
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uganda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 461
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Zimbabwe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 462
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Africa,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 501
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 508
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pefntvty = 515
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 523
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tonga"
      }

      when: {
        sql: ${TABLE}.pefntvty = 527
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Samoa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 528
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Oceania,not specified"
      }

      when: {
        sql: ${TABLE}.pefntvty = 555
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Elsewhere"
      }
    }
  }

  dimension: pemntvty {
    label: "Demographics - native country of mother"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemntvty = 057
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pemntvty = 066
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guam"
      }

      when: {
        sql: ${TABLE}.pemntvty = 073
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 078
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "U. S. Virgin Islands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 096
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Other U. S. Island Areas"
      }

      when: {
        sql: ${TABLE}.pemntvty = 100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Albania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 102
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 103
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pemntvty = 104
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bulgaria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 105
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 106
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pemntvty = 108
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 109
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pemntvty = 110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pemntvty = 116
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pemntvty = 117
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pemntvty = 119
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ireland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pemntvty = 126
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Netherlands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 127
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pemntvty = 128
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 129
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pemntvty = 132
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 134
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 136
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pemntvty = 137
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 138
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "United Kingdom"
      }

      when: {
        sql: ${TABLE}.pemntvty = 139
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pemntvty = 140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 141
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Wales"
      }

      when: {
        sql: ${TABLE}.pemntvty = 142
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 147
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 148
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 149
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Slovakia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bosnia & Herzegovina"
      }

      when: {
        sql: ${TABLE}.pemntvty = 151
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Croatia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 152
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Macedonia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 154
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Serbia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 156
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 157
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 158
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 159
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Azerbaijan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belarus"
      }

      when: {
        sql: ${TABLE}.pemntvty = 161
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Georgia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 162
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Moldova"
      }

      when: {
        sql: ${TABLE}.pemntvty = 163
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 164
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 165
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pemntvty = 166
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Europe,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 167
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kosovo"
      }

      when: {
        sql: ${TABLE}.pemntvty = 200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 202
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pemntvty = 205
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Myanmar (Burma)"
      }

      when: {
        sql: ${TABLE}.pemntvty = 206
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 207
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pemntvty = 208
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cyprus"
      }

      when: {
        sql: ${TABLE}.pemntvty = 209
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pemntvty = 210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pemntvty = 211
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 212
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pemntvty = 213
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pemntvty = 214
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pemntvty = 215
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 216
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 217
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 222
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kuwait"
      }

      when: {
        sql: ${TABLE}.pemntvty = 223
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pemntvty = 224
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pemntvty = 226
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 229
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nepal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 231
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 233
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pemntvty = 235
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 236
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pemntvty = 238
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sri Lanka"
      }

      when: {
        sql: ${TABLE}.pemntvty = 239
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 242
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 243
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pemntvty = 246
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uzbekistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 247
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pemntvty = 248
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Yemen"
      }

      when: {
        sql: ${TABLE}.pemntvty = 249
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Asia,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 301
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 303
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pemntvty = 311
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 312
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 313
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pemntvty = 314
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pemntvty = 315
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pemntvty = 316
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pemntvty = 321
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Antigua and Barbuda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 323
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pemntvty = 324
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pemntvty = 327
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pemntvty = 328
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 329
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 332
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pemntvty = 333
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 338
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Kitts--Nevis"
      }

      when: {
        sql: ${TABLE}.pemntvty = 339
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Lucia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "St. Vincent and the Grenadines"
      }

      when: {
        sql: ${TABLE}.pemntvty = 341
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Trinidad and Tobago"
      }

      when: {
        sql: ${TABLE}.pemntvty = 343
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "West Indies,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pemntvty = 361
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 362
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pemntvty = 363
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pemntvty = 364
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Columbia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 365
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 368
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 369
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Paraguay"
      }

      when: {
        sql: ${TABLE}.pemntvty = 370
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pemntvty = 372
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pemntvty = 373
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pemntvty = 374
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South America,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 399
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Americas,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Algeria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 407
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cameroon"
      }

      when: {
        sql: ${TABLE}.pemntvty = 408
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Cape Verde"
      }

      when: {
        sql: ${TABLE}.pemntvty = 414
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pemntvty = 416
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 417
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Eritrea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 421
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 427
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pemntvty = 429
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Liberia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 436
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pemntvty = 440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 444
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Senegal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 447
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sierra Leone"
      }

      when: {
        sql: ${TABLE}.pemntvty = 448
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Somalia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 449
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 451
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Sudan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 453
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tanzania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 457
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Uganda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 461
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Zimbabwe"
      }

      when: {
        sql: ${TABLE}.pemntvty = 462
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Africa,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 501
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 508
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pemntvty = 515
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 523
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Tonga"
      }

      when: {
        sql: ${TABLE}.pemntvty = 527
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Samoa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 528
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Oceania,not specified"
      }

      when: {
        sql: ${TABLE}.pemntvty = 555
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008')
           ;;
        label: "Elsewhere"
      }
    }
  }

  dimension: ptio1ocd {
    label: "(main job)occupation code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptio1ocd = -1
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chief executives  (11-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "General and operations managers (11-1021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Advertising and promotions managers (11-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Marketing and sales managers  (11-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public relations managers (11-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administrative services managers (11-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and information systems managers (11-3021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial managers (11-3031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources managers (11-3040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial production managers (11-3051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing managers (11-3061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation, storage, and distribution managers (11-3071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farm, ranch, and other agricultural managers (11-9011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farmers and ranchers (11-9012)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction managers (11-9021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Education administrators (11-9030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineering managers  (11-9041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food service managers (11-9051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Funeral directors (11-9061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Gaming managers (11-9071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lodging managers (11-9081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical and health services managers (11-9111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Natural sciences managers (11-9121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Property, real estate, and community association managers (11-9141)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social and community service managers (11-9151)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Managers, all other (11-9199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agents and business managers of artists, performers, and athletes (13-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing agents and buyers, farm products (13-1021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale and retail buyers, except farm products (13-1022)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing agents, except wholesale, retail, and farm products (13-1023)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Claims adjusters, appraisers, examiners, and investigators (13-1030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Compliance officers, except agriculture, construction, health and safety, and transportation (13-104"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cost estimators (13-1051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources, training, and labor relations specialists (13-1070)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Logisticians (13-1081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management analysts (13-1111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Meeting and convention planners (13-1121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other business operations specialists (13-11XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accountants and auditors (13-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Appraisers and assessors of real estate (13-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Budget analysts (13-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Credit analysts (13-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial analysts (13-2051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal financial advisors (13-2052)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance underwriters  (13-2053)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial examiners (13-2061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Loan counselors and officers  (13-2070)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tax examiners, collectors, and revenue agents (13-2081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tax prepares (13-2082)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 0950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial specialists, all other (13-2099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer scientists and systems analysts  (15-10XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer programmers (15-1021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer software engineers (15-1030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer support specialists  (15-1041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Database administrators (15-1061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Network and computer systems administrators (15-1071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Network systems and data communications analysts (15-1081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Actuaries (15-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mathematicians (15-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Operations research analysts  (15-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Statisticians (15-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous mathematical science occupations  (15-2090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Architects, except naval (17-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Surveyors, cartographers, and photogrammetrists (17-1020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aerospace engineers (17-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural engineers  (17-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biomedical engineers (17-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical engineers (17-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Civil engineers (17-2051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer hardware engineers (17-2061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical and electronic engineers (17-2070)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Environmental engineers (17-2081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial engineers, including health and safety (17-2110)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Marine engineers and naval architects (17-2121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1450
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Materials engineers (17-2131)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mechanical engineers (17-2141)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining and geological engineers, including mining safety engineers (17-2151)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nuclear engineers (17-2161)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Petroleum engineers (17-2171)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineers, all other (17-2199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drafters (17-3010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineering technicians, except drafters  (17-3020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Surveying and mapping technicians (17-3031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural and food scientists (19-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biological scientists (19-1020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Conservation scientists and foresters (19-1030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical scientists (19-1040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Astronomers and physicists (19-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Atmospheric and space scientists (19-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemists and materials scientists (19-2030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Environmental scientists and geoscientists (19-2040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical scientists, all other (19-2099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Economists (19-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Market and survey researchers (19-3020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Psychologists (19-3030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sociologists (19-3041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Urban and regional planners (19-3051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous social scientists and related workers (19-3090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural and food science technicians (19-4011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biological technicians  (19-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical technicians (19-4031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Geological and petroleum technicians (19-4041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nuclear technicians (19-4051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 1960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other life, physical, and social science technicians (19-40XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Counselors (21-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social workers (21-1020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous community and social service specialists (21-1090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Clergy (21-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Directors, religious activities and education (21-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Religious workers, all other  (21-2099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lawyers, Judges, magistrates, and other judicial workers (23-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paralegals and legal assistants (23-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous legal support workers (23-2090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postsecondary teachers  (25-1000)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Preschool and kindergarten teachers (25-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Elementary and middle school teachers (25-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Secondary school teachers (25-2030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Special education teachers (25-2040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other teachers and instructors (25-3000)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Archivists, curators, and museum technicians (25-4010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Librarians (25-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Library technicians (25-4031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Teacher assistants (25-9041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other education, training, and library workers  (25-90XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Artists and related workers (27-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Designers (27-1020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Actors (27-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Producers and directors (27-2012)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Athletes, coaches, umpires, and related workers (27-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dancers and choreographers (27-2030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Musicians, singers, and related workers (27-2040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Entertainers and performers, sports and related workers, all other (27-2099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Announcers (27-3010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "News analysts, reporters and correspondents (27-3020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public relations specialists (27-3031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Editors (27-3041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Technical writers (27-3042)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Writers and authors (27-3043)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous media and communication workers (27-3090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Broadcast and sound engineering technicians and radio operators (27-4010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Photographers (27-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Television, video, and motion picture camera operators and editors (27-4030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 2960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Media and communication equipment workers, all other (27-4099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chiropractors (29-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dentists (29-1020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dietitians and nutritionists (29-1031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Optometrists (29-1041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pharmacists (29-1051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physicians and surgeons (29-1060)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physician assistants (29-1071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Podiatrists (29-1081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Registered nurses (29-1111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Audiologists (29-1121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Occupational therapists (29-1122)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical therapists (29-1123)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Radiation therapists (29-1124)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Recreational therapists (29-1125)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Respiratory therapists  (29-1126)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Speech-language pathologists  (29-1127)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Therapists, all other (29-1129)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Veterinarians (29-1131)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health diagnosing and treating practitioners, all other (29-1199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Clinical laboratory technologists and technicians (29-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dental hygienists (29-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Diagnostic related technologists and technicians (29-2030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Emergency medical technicians and paramedics (29-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health diagnosing and treating practitioner support technicians (29-2050)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Licensed practical and licensed vocational nurses (29-2061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical records and health information technicians (29-2071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Opticians, dispensing (29-2081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous health technologists and technicians (29-2090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other healthcare practitioners and technical occupations (29-90000"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nursing, psychiatric, and home health aides (31-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Occupational therapist assistants and aides (31-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical therapist assistants and aides (31-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Massage therapists (31-9011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dental assistants (31-9091)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 3650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Med
"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources assistants, except payroll and timekeeping (43-4161)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Receptionists and information clerks (43-4171)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Reservation and transportation ticket agents and travel clerks (43-4181)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information and record clerks, all other  (43-4199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cargo and freight agents (43-5011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Couriers and messengers (43-5021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dispatchers (43-5030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Meter readers, utilities (43-5041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service clerks (43-5051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service mail carriers  (43-5052)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service mail sorters, processors, and processing machine operators (43-5053)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production, planning, and expediting clerks (43-5061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shipping, receiving, and traffic clerks (43-5071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Stock clerks and order fillers (43-5081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Weighers, measurers, checkers, and samplers, recordkeeping (43-5111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Secretaries and administrative assistants (43-6010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer operators (43-9011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Data entry keyers (43-9021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Word processors and typists (43-9022)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Desktop publishers (43-9031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance claims and policy processing clerks (43-9041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mail clerks and mail machine operators, except postal service (43-9051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office clerks, general  (43-9061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office machine operators, except computer (43-9071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Proofreaders and copy markers (43-9081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Statistical assistants  (43-9111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 5930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support workers, all other  (43-9199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of farming, fishing, and forestry workers (45-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural inspectors (45-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal breeders (45-2021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Graders and sorters, agricultural products (45-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous agricultural workers (45-2090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fishers and related fishing workers (45-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hunters and trappers (45-3021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forest and conservation workers (45-4011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Logging workers (45-4020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of construction trades and extraction workers (47-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boilermakers (47-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Brickmasons, blockmasons, and stonemasons (47-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Carpenters  (47-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Carpet, floor, and tile installers and finishers (47-2040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cement masons, concrete finishers, and terrazzo workers (47-2050)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction laborers (47-2061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paving, surfacing, and tamping equipment operators (47-2071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pile-driver operators (47-2072)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Operating engineers and other construction equipment operators (47-2073)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drywall installers, ceiling tile installers, and tapers (47-2080)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electricians (47-2111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Glaziers (47-2121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insulation workers (47-2130)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Painters, construction and maintenance (47-2141)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paperhangers (47-2142)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pipelayers, plumbers, pipefitters, and steamfitters (47-2150)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Plasterers and stucco masons  (47-2161)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Reinforcing iron and rebar workers  (47-2171)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Roofers (47-2181)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sheet metal workers (47-2211)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Structural iron and steel workers (47-2221)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Helpers, construction trades  (47-3010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6660
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction and building inspectors (47-4011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Elevator installers and repairers (47-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fence erectors (47-4031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hazardous materials removal workers (47-4041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Highway maintenance workers (47-4051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Rail-track laying and maintenance equipment operators (47-4061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Septic tank servicers and sewer pipe cleaners (47-4071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous construction and related workers  (47-4090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Derrick, rotary drill, and service unit operators, oil, gas, and mining (47-5010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Earth drillers, except oil and gas  (47-5021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Explosives workers, ordnance handling experts, and blasters (47-5031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining machine operators (47-5040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Roof bolters, mining (47-5061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Roustabouts, oil and gas (47-5071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Helpers--extraction workers (47-5081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 6940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other extraction workers (47-50XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of mechanics, installers, and repairers (49-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer, automated teller, and office machine repairers (49-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Radio and telecommunications equipment installers and repairers (49-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Avionics technicians (49-2091)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electric motor, power tool, and related repairers (49-2092)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical and electronics installers and repairers, transportation equipment (49-2093)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical and electronics repairers, industrial and utility (49-209X)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electronic equipment installers and repairers, motor vehicles (49-2096)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electronic home entertainment equipment installers and repairers  (49-2097)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Security and fire alarm systems installers (49-2098)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aircraft mechanics and service technicians (49-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Automotive body and related repairers (49-3021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Automotive glass installers and repairers (49-3022)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Automotive service technicians and mechanics (49-3023)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bus and truck mechanics and diesel engine specialists (49-3031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Heavy vehicle and mobile equipment service technicians and mechanics (49-3040)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Small engine mechanics (49-3050)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous vehicle and mobile equipment mechanics, installers, and repairers (49-3090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Control and valve installers and repairers (49-9010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Heating, air conditioning, and refrigeration mechanics and installers (49-9021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Home appliance repairers (49-9031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial and refractory machinery mechanics (49-904X)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Maintenance and repair workers, general (49-9042)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Maintenance workers, machinery (49-9043)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Millwrights (49-9044)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical power-line installers and repairers  (49-9051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Telecommunications line installers and repairers (49-9052)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Precision instrument and equipment repairers (49-9060)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Coin, vending, and amusement machine servicers and repairers (49-9091)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Commercial divers (49-9092)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Locksmiths and safe repairers (49-9094)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Manufactured building and mobile home installers (49-9095)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Riggers (49-9096)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Signal and track switch repairers (49-9097)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Helpers--installation, maintenance, and repair workers (49-9098)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other installation, maintenance, and repair workers (49-909X)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of production and operating workers (51-1011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aircraft structure, surfaces, rigging, and systems assemblers (51-2011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical, electronics, and electromechanical assemblers (51-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engine and other machine assemblers (51-2031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Structural metal fabricators and fitters (51-2041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous assemblers and fabricators  (51-2090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bakers (51-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Butchers and other meat, poultry, and fish processing workers (51-3020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food and tobacco roasting, baking, and drying machine operators and tenders (51-3091)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food batchmakers  (51-3092)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food cooking machine operators and tenders (51-3093)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer control programmers and operators (51-4010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Extruding and drawing machine setters, operators, and tenders, metal and plastic (51-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forging machine setters, operators, and tenders, metal and plastic (51-4022)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Rolling machine setters, operators, and tenders, metal and plastic (51-4023)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cutting, punching, and press machine setters, operators, and tenders, metal and plastic (51-4031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 7960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drilling and boring machine tool setters, operators, and tenders, metal and plastic (51-4032)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Grinding, lapping, polishing, and buffing machine tool setters, operators, and tenders, metal and pl"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lathe and turning machine tool setters, operators, and tenders, metal and plastic (51-4034)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Milling and planing machine setters, operators, and tenders, metal and plastic (51-4035)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machinists  (51-4041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metal furnace and kiln operators and tenders (51-4050)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Model makers and patternmakers, metal and plastic (51-4060)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Molders and molding machine setters, operators, and tenders, metal and plastic (51-4070)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Multiple machine tool setters, operators, and tenders, metal and plastic (51-4081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tool and die makers (51-4111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Welding, soldering, and brazing workers (51-4120)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Heat treating equipment setters, operators, and tenders, metal and plastic (51-4191)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lay-out workers, metal and plastic  (51-4192)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Plating and coating machine setters, operators, and tenders, metal and plastic (51-4193)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tool grinders, filers, and sharpeners (51-4194)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metalworkers and plastic workers, all other (51-4199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bookbinders and bindery workers (51-5010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Job printers (51-5021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Prepress technicians and workers (51-5022)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Printing machine operators (51-5023)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Laundry and dry-cleaning workers (51-6011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pressers, textile, garment, and related materials (51-6021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sewing machine operators (51-6031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shoe and leather workers and repairers (51-6041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shoe machine operators and tenders (51-6042)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tailors, dressmakers, and sewers (51-6050)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile bleaching and dyeing machine operators and tenders  (51-6061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile cutting machine setters, operators, and tenders (51-6062)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile knitting and weaving machine setters, operators, and tenders (51-6063)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile winding, twisting, and drawing out machine setters, operators, and tenders (51-6064)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Extruding and forming machine setters, operators, and tenders, synthetic and glass fibers (51-6091)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fabric and apparel patternmakers (51-6092)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8450
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Upholsterers (51-6093)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile, apparel, and furnishings workers, all other  (51-6099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cabinetmakers and bench carpenters  (51-7011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furniture finishers (51-7021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Model makers and patternmakers, wood (51-7030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sawing machine setters, operators, and tenders, wood (51-7041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Woodworking machine setters, operators, and tenders, except sawing (51-7042)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Woodworkers, all other (51-7099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Power plant operators, distributors, and dispatchers (51-8010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Stationary engineers and boiler operators (51-8021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Water and liquid waste treatment plant and system operators (51-8031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous plant and system operators  (51-8090)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical processing machine setters, operators, and tenders (51-9010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crushing, grinding, polishing, mixing, and blending workers (51-9020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cutting workers (51-9030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Extruding, forming, pressing, and compacting machine setters, operators, and tenders  (51-9041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furnace, kiln, oven, drier, and kettle operators and tenders (51-9051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Inspectors, testers, sorters, samplers, and weighers  (51-9061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Jewelers and precious stone and metal workers (51-9071)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical, dental, and ophthalmic laboratory technicians (51-9080)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Packaging and filling machine operators and tenders (51-9111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Painting workers  (51-9120)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Photographic process workers and processing machine operators (51-9130)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Semiconductor processors (51-9141)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cementing and gluing machine operators and tenders (51-9191)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cleaning, washing, and metal pickling equipment operators and tenders (51-9192)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cooling and freezing equipment operators and tenders (51-9193)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Etchers and engravers (51-9194)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Molders, shapers, and casters, except metal and plastic (51-9195)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paper goods machine setters, operators, and tenders (51-9196)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tire builders (51-9197)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Helpers--production workers (51-9198)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 8960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production workers, all other (51-9199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Supervisors, transportation and material moving workers (53-1000)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aircraft pilots and flight engineers (53-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Air traffic controllers and airfield operations specialists (53-2020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ambulance drivers and attendants, except emergency medical technicians (53-3011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bus drivers (53-3020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Driver/sales workers and truck drivers (53-3030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Taxi drivers and chauffeurs (53-3041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Motor vehicle operators, all other  (53-3099)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Locomotive engineers and operators  (53-4010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Railroad brake, signal, and switch operators (53-4021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Railroad conductors and yardmasters (53-4031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Subway, streetcar, and other rail transportation workers (53-30XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sailors and marine oilers (53-5011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ship and boat captains and operators (53-5020)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ship engineers (53-5031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bridge and lock tenders (53-6011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Parking lot attendants  (53-6021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Service station attendants (53-6031)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation inspectors (53-6051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other transportation workers (53-60XX)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Conveyor operators and tenders (53-7011)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crane and tower operators (53-7021)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dredge, excavating, and loading machine operators (53-7030)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hoist and winch operators (53-7041)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial truck and tractor operators (53-7051)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cleaners of vehicles and equipment  (53-7061)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Laborers and freight, stock, and material movers, hand (53-7062)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machine feeders and offbearers (53-7063)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Packers and packagers, hand (53-7064)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pumping station operators (53-7070)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refuse and recyclable material collectors (53-7081)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shuttle car operators (53-7111)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tank car, truck, and ship loaders (53-7121)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Material moving workers, all other (53-7199)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military officer special and tactical operations leaders/managers (55-1010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line enlisted military supervisors/managers (55-2010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military enlisted tactical operations and air/weapons specialists and crew members (55-3010)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military, rank not specified  (99-9999)"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9970
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Problem referral"
      }

      when: {
        sql: ${TABLE}.ptio1ocd = 9990
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not reported  (Includes Refused, Classified, blank and all other noncodable entries)"
      }
    }
  }

  dimension: ptio2ocd {
    label: "(second job)occupation code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptio2ocd = -1
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chief executives  (11-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "General and operations managers (11-1021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Advertising and promotions managers (11-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Marketing and sales managers  (11-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public relations managers (11-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administrative services managers (11-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer and information systems managers (11-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial managers (11-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources managers (11-3040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial production managers (11-3051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing managers (11-3061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation, storage, and distribution managers (11-3071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farm, ranch, and other agricultural managers (11-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Farmers and ranchers (11-9012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction managers (11-9021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Education administrators (11-9030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineering managers  (11-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food service managers (11-9051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Funeral directors (11-9061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Gaming managers (11-9071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lodging managers (11-9081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical and health services managers (11-9111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Natural sciences managers (11-9121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Property, real estate, and community association managers (11-9141)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social and community service managers (11-9151)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Managers, all other (11-9199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agents and business managers of artists, performers, and athletes (13-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing agents and buyers, farm products (13-1021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wholesale and retail buyers, except farm products (13-1022)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Purchasing agents, except wholesale, retail, and farm products (13-1023)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Claims adjusters, appraisers, examiners, and investigators (13-1030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Compliance officers, except agriculture, construction, health and safety, and transportation (13-104"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cost estimators (13-1051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources, training, and labor relations specialists (13-1070)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Logisticians (13-1081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Management analysts (13-1111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Meeting and convention planners (13-1121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other business operations specialists (13-11XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Accountants and auditors (13-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Appraisers and assessors of real estate (13-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Budget analysts (13-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Credit analysts (13-2041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial analysts (13-2051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal financial advisors (13-2052)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance underwriters  (13-2053)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial examiners (13-2061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Loan counselors and officers  (13-2070)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tax examiners, collectors, and revenue agents (13-2081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tax prepares (13-2082)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 0950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Financial specialists, all other (13-2099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer scientists and systems analysts  (15-10XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer programmers (15-1021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer software engineers (15-1030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer support specialists  (15-1041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Database administrators (15-1061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Network and computer systems administrators (15-1071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Network systems and data communications analysts (15-1081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Actuaries (15-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mathematicians (15-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Operations research analysts  (15-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Statisticians (15-2041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous mathematical science occupations  (15-2090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Architects, except naval (17-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Surveyors, cartographers, and photogrammetrists (17-1020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aerospace engineers (17-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural engineers  (17-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biomedical engineers (17-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical engineers (17-2041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Civil engineers (17-2051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer hardware engineers (17-2061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electrical and electronic engineers (17-2070)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Environmental engineers (17-2081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial engineers, including health and safety (17-2110)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Marine engineers and naval architects (17-2121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1450
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Materials engineers (17-2131)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mechanical engineers (17-2141)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mining and geological engineers, including mining safety engineers (17-2151)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nuclear engineers (17-2161)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Petroleum engineers (17-2171)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineers, all other (17-2199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drafters (17-3010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Engineering technicians, except drafters  (17-3020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Surveying and mapping technicians (17-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural and food scientists (19-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biological scientists (19-1020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Conservation scientists and foresters (19-1030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical scientists (19-1040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Astronomers and physicists (19-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Atmospheric and space scientists (19-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemists and materials scientists (19-2030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Environmental scientists and geoscientists (19-2040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical scientists, all other (19-2099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Economists (19-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Market and survey researchers (19-3020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Psychologists (19-3030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sociologists (19-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Urban and regional planners (19-3051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous social scientists and related workers (19-3090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural and food science technicians (19-4011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Biological technicians  (19-4021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical technicians (19-4031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Geological and petroleum technicians (19-4041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nuclear technicians (19-4051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 1960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other life, physical, and social science technicians (19-40XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Counselors (21-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Social workers (21-1020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous community and social service specialists (21-1090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Clergy (21-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Directors, religious activities and education (21-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Religious workers, all other  (21-2099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lawyers, Judges, magistrates, and other judicial workers (23-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paralegals and legal assistants (23-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous legal support workers (23-2090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postsecondary teachers  (25-1000)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Preschool and kindergarten teachers (25-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Elementary and middle school teachers (25-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Secondary school teachers (25-2030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Special education teachers (25-2040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other teachers and instructors (25-3000)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Archivists, curators, and museum technicians (25-4010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Librarians (25-4021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Library technicians (25-4031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Teacher assistants (25-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other education, training, and library workers  (25-90XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Artists and related workers (27-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Designers (27-1020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Actors (27-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Producers and directors (27-2012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Athletes, coaches, umpires, and related workers (27-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dancers and choreographers (27-2030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Musicians, singers, and related workers (27-2040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Entertainers and performers, sports and related workers, all other (27-2099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Announcers (27-3010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "News analysts, reporters and correspondents (27-3020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public relations specialists (27-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Editors (27-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Technical writers (27-3042)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Writers and authors (27-3043)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous media and communication workers (27-3090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Broadcast and sound engineering technicians and radio operators (27-4010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Photographers (27-4021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Television, video, and motion picture camera operators and editors (27-4030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 2960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Media and communication equipment workers, all other (27-4099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chiropractors (29-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dentists (29-1020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dietitians and nutritionists (29-1031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Optometrists (29-1041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pharmacists (29-1051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physicians and surgeons (29-1060)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physician assistants (29-1071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Podiatrists (29-1081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Registered nurses (29-1111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Audiologists (29-1121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Occupational therapists (29-1122)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical therapists (29-1123)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Radiation therapists (29-1124)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Recreational therapists (29-1125)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Respiratory therapists  (29-1126)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Speech-language pathologists  (29-1127)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Therapists, all other (29-1129)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Veterinarians (29-1131)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health diagnosing and treating practitioners, all other (29-1199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Clinical laboratory technologists and technicians (29-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dental hygienists (29-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Diagnostic related technologists and technicians (29-2030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Emergency medical technicians and paramedics (29-2041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Health diagnosing and treating practitioner support technicians (29-2050)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Licensed practical and licensed vocational nurses (29-2061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical records and health information technicians (29-2071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Opticians, dispensing (29-2081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous health technologists and technicians (29-2090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other healthcare practitioners and technical occupations (29-90000"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nursing, psychiatric, and home health aides (31-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Occupational therapist assistants and aides (31-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Physical therapist assistants and aides (31-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Massage therapists (31-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dental assistants (31-9091)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical assistants and other healthcare support occupations (31-909X)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of correctional officers (33-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of police and detectives (33-10120"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of fire fighting and prevention workers (33-1021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Supervisors, protective service workers, all other (33-1099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fire fighters (33-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fire inspectors (33-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bailiffs, correctional officers, and jailers (33-30100"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Detectives and criminal investigators (33-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fish and game wardens (33-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Parking enforcement workers (33-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Police and sheriff's patrol officers (33-3051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transit and railroad police (33-3052)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal control workers  (33-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private detectives and investigators (33-9021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Security guards and gaming surveillance officers (33-9030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crossing guards (33-9091)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 3950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lifeguards and other protective service workers (33-909X)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chefs and head cooks (35-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of food preparation and serving workers (35-1012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cooks (35-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food preparation workers (35-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bartenders (35-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Combined food preparation and serving workers, including fast food (35-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Counter attendants, cafeteria, food concession, and coffee shop (35-3022)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waiters and waitresses  (35-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food servers, nonrestaurant (35-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dining room and cafeteria attendants and bartender helpers (35-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dishwashers (35-9021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hosts and hostesses, restaurant, lounge, and coffee shop (35-9031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Food preparation and serving related workers, all other (35-9099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of housekeeping and janitorial workers (37-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of landscaping, lawn service, and groundskeeping workers  (37-1012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Janitors and building cleaners (31-201X)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Maids and housekeeping cleaners (37-2012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pest control workers (37-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Grounds maintenance workers (37-3010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of gaming workers (39-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of personal service workers (39-1021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal trainers (39-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonfarm animal caretakers (39-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Gaming services workers (39-3010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Motion picture projectionists (39-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ushers, lobby attendants, and ticket takers (39-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous entertainment attendants and related workers (39-3090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Funeral service workers (39-4000)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Barbers (39-5011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hairdressers, hairstylists, and cosmetologists (39-5012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous personal appearance workers (39-5090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Baggage porters, bellhops, and concierges (39-6010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tour and travel guides  (39-6020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation attendants (39-6030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Child care workers (39-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal and home care aides (39-9021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Recreation and fitness workers (39-9030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Residential advisors (39-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal care and service workers, all other (39-9099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of retail sales workers (41-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of non-retail sales workers (41-1012)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cashiers (41-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Counter and rental clerks (41-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Parts salespersons (41-2022)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Retail salespersons (41-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Advertising sales agents (41-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance sales agents  (41-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Securities, commodities, and financial services sales agents (41-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Travel agents (41-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales representatives, services, all other (41-3099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales representatives, wholesale and manufacturing (41-4010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Models, demonstrators, and product promoters (41-9010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Real estate brokers and sales agents (41-9020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales engineers (41-9031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Telemarketers (41-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Door-to-door sales workers, news and street vendors, and related workers (41-9091)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sales and related workers, all other (41-9099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of office and administrative support workers (43-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Switchboard operators, including answering service (43-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Telephone operators (43-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Communications equipment operators, all other (43-2099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bill and account collectors (43-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Billing and posting clerks and machine operators (43-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bookkeeping, accounting, and auditing clerks (43-3031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Gaming cage workers (43-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Payroll and timekeeping clerks (43-3051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Procurement clerks (43-3061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tellers (43-3071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Brokerage clerks  (43-4011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Correspondence clerks (43-4021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Court, municipal, and license clerks (43-4031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Credit authorizers, checkers, and clerks  (43-4041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Customer service representatives (43-4051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Eligibility interviewers, government programs (43-4061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "File Clerks (43-4071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hotel, motel, and resort desk clerks (43-4081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Interviewers, except eligibility and loan (43-4111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Library assistants, clerical  (43-4121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Loan interviewers and clerks  943-4131)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "New accounts clerks (43-4141)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Order clerks 943-4151)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Human resources assistants, except payroll and timekeeping (43-4161)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Receptionists and information clerks (43-4171)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Reservation and transportation ticket agents and travel clerks (43-4181)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Information and record clerks, all other  (43-4199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cargo and freight agents (43-5011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Couriers and messengers (43-5021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dispatchers (43-5030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Meter readers, utilities (43-5041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service clerks (43-5051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service mail carriers  (43-5052)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Postal service mail sorters, processors, and processing machine operators (43-5053)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production, planning, and expediting clerks (43-5061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shipping, receiving, and traffic clerks (43-5071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Stock clerks and order fillers (43-5081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Weighers, measurers, checkers, and samplers, recordkeeping (43-5111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5700
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Secretaries and administrative assistants (43-6010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Computer operators (43-9011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Data entry keyers (43-9021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Word processors and typists (43-9022)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Desktop publishers (43-9031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Insurance claims and policy processing clerks (43-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Mail clerks and mail machine operators, except postal service (43-9051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office clerks, general  (43-9061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office machine operators, except computer (43-9071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Proofreaders and copy markers (43-9081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Statistical assistants  (43-9111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Office and administrative support workers, all other  (43-9199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of farming, fishing, and forestry workers (45-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Agricultural inspectors (45-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal breeders (45-2021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Graders and sorters, agricultural products (45-2041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6050
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous agricultural workers (45-2090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fishers and related fishing workers (45-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hunters and trappers (45-3021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forest and conservation workers (45-4011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Logging workers (45-4020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line supervisors/managers of construction trades and extraction workers (47-1011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boilermakers (47-2011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Brickmasons, blockmasons, and stonemasons (47-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Carpenters  (47-2031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Carpet, floor, and tile installers and finishers (47-2040)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cement masons, concrete finishers, and terrazzo workers (47-2050)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction laborers (47-2061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paving, surfacing, and tamping equipment operators (47-2071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pile-driver operators (47-2072)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Operating engineers and other construction equipment operators (47-2073)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "
)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 7960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drilling and boring machine tool setters, operators, and tenders, metal and plastic (51-4032)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Grinding, lapping, polishing, and buffing machine tool setters, operators, and tenders, metal and pl"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8010
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lathe and turning machine tool setters, operators, and tenders, metal and plastic (51-4034)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8020
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Milling and planing machine setters, operators, and tenders, metal and plastic (51-4035)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machinists  (51-4041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metal furnace and kiln operators and tenders (51-4050)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8060
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Model makers and patternmakers, metal and plastic (51-4060)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8100
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Molders and molding machine setters, operators, and tenders, metal and plastic (51-4070)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Multiple machine tool setters, operators, and tenders, metal and plastic (51-4081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tool and die makers (51-4111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Welding, soldering, and brazing workers (51-4120)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Heat treating equipment setters, operators, and tenders, metal and plastic (51-4191)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Lay-out workers, metal and plastic  (51-4192)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Plating and coating machine setters, operators, and tenders, metal and plastic (51-4193)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8210
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tool grinders, filers, and sharpeners (51-4194)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8220
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metalworkers and plastic workers, all other (51-4199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bookbinders and bindery workers (51-5010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Job printers (51-5021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8250
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Prepress technicians and workers (51-5022)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Printing machine operators (51-5023)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Laundry and dry-cleaning workers (51-6011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pressers, textile, garment, and related materials (51-6021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8320
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sewing machine operators (51-6031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shoe and leather workers and repairers (51-6041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shoe machine operators and tenders (51-6042)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tailors, dressmakers, and sewers (51-6050)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile bleaching and dyeing machine operators and tenders  (51-6061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8400
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile cutting machine setters, operators, and tenders (51-6062)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile knitting and weaving machine setters, operators, and tenders (51-6063)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile winding, twisting, and drawing out machine setters, operators, and tenders (51-6064)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8430
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Extruding and forming machine setters, operators, and tenders, synthetic and glass fibers (51-6091)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8440
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fabric and apparel patternmakers (51-6092)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8450
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Upholsterers (51-6093)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8460
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Textile, apparel, and furnishings workers, all other  (51-6099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cabinetmakers and bench carpenters  (51-7011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furniture finishers (51-7021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Model makers and patternmakers, wood (51-7030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8530
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sawing machine setters, operators, and tenders, wood (51-7041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8540
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Woodworking machine setters, operators, and tenders, except sawing (51-7042)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8550
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Woodworkers, all other (51-7099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Power plant operators, distributors, and dispatchers (51-8010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Stationary engineers and boiler operators (51-8021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Water and liquid waste treatment plant and system operators (51-8031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Miscellaneous plant and system operators  (51-8090)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chemical processing machine setters, operators, and tenders (51-9010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crushing, grinding, polishing, mixing, and blending workers (51-9020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8710
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cutting workers (51-9030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Extruding, forming, pressing, and compacting machine setters, operators, and tenders  (51-9041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Furnace, kiln, oven, drier, and kettle operators and tenders (51-9051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Inspectors, testers, sorters, samplers, and weighers  (51-9061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Jewelers and precious stone and metal workers (51-9071)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8760
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Medical, dental, and ophthalmic laboratory technicians (51-9080)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Packaging and filling machine operators and tenders (51-9111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Painting workers  (51-9120)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Photographic process workers and processing machine operators (51-9130)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Semiconductor processors (51-9141)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8850
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cementing and gluing machine operators and tenders (51-9191)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8860
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cleaning, washing, and metal pickling equipment operators and tenders (51-9192)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8900
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cooling and freezing equipment operators and tenders (51-9193)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8910
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Etchers and engravers (51-9194)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8920
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Molders, shapers, and casters, except metal and plastic (51-9195)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8930
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Paper goods machine setters, operators, and tenders (51-9196)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8940
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tire builders (51-9197)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8950
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Helpers--production workers (51-9198)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8960
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Production workers, all other (51-9199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9000
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Supervisors, transportation and material moving workers (53-1000)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9030
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Aircraft pilots and flight engineers (53-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9040
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Air traffic controllers and airfield operations specialists (53-2020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9110
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ambulance drivers and attendants, except emergency medical technicians (53-3011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9120
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bus drivers (53-3020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9130
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Driver/sales workers and truck drivers (53-3030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9140
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Taxi drivers and chauffeurs (53-3041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9150
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Motor vehicle operators, all other  (53-3099)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9200
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Locomotive engineers and operators  (53-4010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9230
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Railroad brake, signal, and switch operators (53-4021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9240
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Railroad conductors and yardmasters (53-4031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9260
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Subway, streetcar, and other rail transportation workers (53-30XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9300
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sailors and marine oilers (53-5011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9310
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ship and boat captains and operators (53-5020)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9330
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ship engineers (53-5031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9340
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bridge and lock tenders (53-6011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9350
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Parking lot attendants  (53-6021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9360
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Service station attendants (53-6031)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9410
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation inspectors (53-6051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9420
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other transportation workers (53-60XX)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9500
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Conveyor operators and tenders (53-7011)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9510
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crane and tower operators (53-7021)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9520
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dredge, excavating, and loading machine operators (53-7030)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9560
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hoist and winch operators (53-7041)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9600
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Industrial truck and tractor operators (53-7051)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9610
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cleaners of vehicles and equipment  (53-7061)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9620
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Laborers and freight, stock, and material movers, hand (53-7062)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9630
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Machine feeders and offbearers (53-7063)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9640
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Packers and packagers, hand (53-7064)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9650
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Pumping station operators (53-7070)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9720
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refuse and recyclable material collectors (53-7081)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9730
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Shuttle car operators (53-7111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9740
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tank car, truck, and ship loaders (53-7121)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9750
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Material moving workers, all other (53-7199)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9800
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military officer special and tactical operations leaders/managers (55-1010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9810
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "First-line enlisted military supervisors/managers (55-2010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9820
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military enlisted tactical operations and air/weapons specialists and crew members (55-3010)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9830
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military, rank not specified  (99-9999)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9840
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9970
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Problem referral"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9990
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2012', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not reported  (Includes Refused, Classified, blank and all other noncodable entries)"
      }
    }
  }

  dimension: prdasian {
    label: "Demographics detailed asian subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdasian = -1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdasian = 1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Asian Indian"
      }

      when: {
        sql: ${TABLE}.prdasian = 2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Chinese"
      }

      when: {
        sql: ${TABLE}.prdasian = 3
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Filipino"
      }

      when: {
        sql: ${TABLE}.prdasian = 4
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Japanese"
      }

      when: {
        sql: ${TABLE}.prdasian = 5
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Korean"
      }

      when: {
        sql: ${TABLE}.prdasian = 6
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Vietnamese"
      }

      when: {
        sql: ${TABLE}.prdasian = 7
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Other Asian"
      }
    }
  }

  dimension: peio1icd {
    label: "(main job)industry code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio1icd = -1
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0170
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Crop production (111)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0180
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal production (112)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0190
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forestry except logging (1131,1132)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0270
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Logging (1133)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0280
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fishing, hunting, and trapping (114)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0290
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Support activities for agriculture and forestry (115)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0370
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Oil and gas extraction (211)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0380
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Coal mining (2121)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0390
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Metal ore mining (2122)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0470
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetallic mineral mining and quarrying (2123)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0480
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not specified type of mining (Part of 21)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0490
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Support activities for mining (213)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0570
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electric power generation, transmission and distribution (Pt. 2211)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0580
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Natural gas distribution (Pt. 2212)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0590
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electric and gas, and other combinations  (Pts. 2211,2212)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0670
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Water, steam, air-conditioning, and irrigation systems (22131,22133)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0680
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sewage treatment facilities (22132)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0690
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not specified utilities (Part of 22)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 0770
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Construction (Includes the cleaning of buildings and dwellings is incidental during  construction an"
      }

      when: {
        sql: ${TABLE}.peio1icd = 1070
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Animal food, grain and oilseed milling (3111,3112)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 1080
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sugar and confectionery products (3113)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 1090
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fruit and vegetable preserving and specialty food manufacturing (3114)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 1170
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dairy product manufacturing (3115)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 1180
          AND ${TABLE}.src_table in ('census_2010', 'census_2012', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Anim
"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8770
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Automotive repair and maintenance (8111 exc. 811192)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8780
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Car washes  (811192)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8790
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Electronic and precision equipment repair and maintenance (8112)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8870
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Commercial and industrial machinery and equipment repair and maintenance (8113)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8880
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Personal and household goods repair and maintenance   (8114 exc. 81143)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8890
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Footwear and leather goods repair   (81143)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8970
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Barber shops (812111)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8980
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Beauty salons (812112)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8990
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nail salons and other personal care services (812113, 81219)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9070
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Drycleaning and laundry services (8123)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9080
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Funeral homes, cemeteries, and crematories (8122)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9090
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other personal services (8129)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9160
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Religious organizations (8131)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9170
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Civic, social, advocacy organizations, and grantmaking and giving services (8132, 8133, 8134)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9180
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Labor unions (81393)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9190
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Business, professional, political, and similar organizations (8139 exc. 81393)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9290
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Private households (814)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9370
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Executive offices and legislative bodies  (92111, 92112, 92114, pt. 92115)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9380
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Public finance activities (92113)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9390
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other general government and support (92119)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9470
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Justice, public order, and safety activities (922, pt. 92115)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9480
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administration of human resource programs (923)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9490
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administration of environmental quality and housing programs (924, 925)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9570
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Administration of economic programs and space research (926, 927)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9590
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "National security and international affairs (928)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9670
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Army"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9680
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Air Force"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9690
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Navy"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9770
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Marines"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9780
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Coast Guard"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9790
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "U. S. Armed Forces, Branch Not Specified"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9870
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Military Reserves or National Guard"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9890
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Armed Forces"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9970
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Problem referral"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9990
          AND ${TABLE}.src_table in ('census_2010', 'census_2014', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Uncodable  (Includes Refused or reported Classified)"
      }
    }
  }

  dimension: prdthsp {
    label: "Detailed hispanic recode"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Dominican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Salvadoran"
      }

      when: {
        sql: ${TABLE}.prdthsp = 6
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Central American, excluding Salvadoran"
      }

      when: {
        sql: ${TABLE}.prdthsp = 7
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 8
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Other Hispanic"
      }
    }
  }

  dimension: pepdemp1 {
    label: "Individual has paid employees?"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pepdemp1 = -1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pepdemp1 = 1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pepdemp1 = 2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "No"
      }
    }
  }

  dimension: pepdemp2 {
    label: "Individual has paid employees, second job"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pepdemp2 = -1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pepdemp2 = 1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pepdemp2 = 2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "No"
      }
    }
  }

  dimension: ptnmemp1 {
    label: "-main job"
    view_label: "Cohort Industry"
    type: tier
    tiers: [1, 20, 39, 58, 76]
    style: classic
    sql: CASE WHEN ${TABLE}.ptnmemp1 between 01 AND 75 THEN ${TABLE}.ptnmemp1 END ;;
  }

  dimension: ptnmemp2 {
    label: "-second job"
    view_label: "Cohort Industry"
    type: tier
    tiers: [1, 4, 6, 9, 11]
    style: classic
    sql: CASE WHEN ${TABLE}.ptnmemp2 between 01 AND 10 THEN ${TABLE}.ptnmemp2 END ;;
  }

  dimension: prinuyer {
    label: "Recoded year of entry for foreign born"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prinuyer = -1
          ;;
        label: "Not In Universe (Born In U.S.)"
      }

      when: {
        sql: ${TABLE}.prinuyer = 0
          ;;
        label: "Not Foreign Born,(Born in the U. S.)"
      }

      when: {
        sql: ${TABLE}.prinuyer = 1
          ;;
        label: "Before 1950"
      }

      when: {
        sql: ${TABLE}.prinuyer = 2
          ;;
        label: "1950-1959"
      }

      when: {
        sql: ${TABLE}.prinuyer = 3
          ;;
        label: "1960-1964"
      }

      when: {
        sql: ${TABLE}.prinuyer = 4
          ;;
        label: "1965-1969"
      }

      when: {
        sql: ${TABLE}.prinuyer = 5
          ;;
        label: "1970-1974"
      }

      when: {
        sql: ${TABLE}.prinuyer = 6
          ;;
        label: "1975-1979"
      }

      when: {
        sql: ${TABLE}.prinuyer = 7
          ;;
        label: "1980-1981"
      }

      when: {
        sql: ${TABLE}.prinuyer = 8
          ;;
        label: "1982-1983"
      }

      when: {
        sql: ${TABLE}.prinuyer = 9
          ;;
        label: "1984-1985"
      }

      when: {
        sql: ${TABLE}.prinuyer = 10
          ;;
        label: "1986-1987"
      }

      when: {
        sql: ${TABLE}.prinuyer = 11
          ;;
        label: "1988-1989"
      }

      when: {
        sql: ${TABLE}.prinuyer = 12
          ;;
        label: "1990-1991"
      }

      when: {
        sql: ${TABLE}.prinuyer = 13
          ;;
        label: "1992-1993"
      }

      when: {
        sql: ${TABLE}.prinuyer = 14
          ;;
        label: "1994-1995"
      }

      when: {
        sql: ${TABLE}.prinuyer = 15
          ;;
        label: "1996-1997"
      }

      when: {
        sql: ${TABLE}.prinuyer = 16
          ;;
        label: "1998-1999"
      }

      when: {
        sql: ${TABLE}.prinuyer = 17
          ;;
        label: "2000-2001"
      }

      when: {
        sql: ${TABLE}.prinuyer = 18
          ;;
        label: "2002-2003"
      }

      when: {
        sql: ${TABLE}.prinuyer = 19
          ;;
        label: "2004-2005"
      }

      when: {
        sql: ${TABLE}.prinuyer = 20
          ;;
        label: "2006-2007"
      }

      when: {
        sql: ${TABLE}.prinuyer = 21
          ;;
        label: "2008-2009"
      }

      when: {
        sql: ${TABLE}.prinuyer = 22
          ;;
        label: "2010-2011"
      }

      when: {
        sql: ${TABLE}.prinuyer = 23
          ;;
        label: "2012-2014"
      }
    }
  }

  dimension: gtcsa {
    label: "Combined statistical area fips code"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtcsa = 000
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Nonmetropolitan or Not Identified"
      }

      when: {
        sql: ${TABLE}.gtcsa = 118
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Appleton-Oshkosh-Neenah, WI"
      }

      when: {
        sql: ${TABLE}.gtcsa = 176
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Chicago-Naperville-Michigan City, IL-IN-WI (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 178
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cincinnati-Middletown-Wilmington, OH-KY-IN (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 184
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cleveland-Akron-Elyria, OH (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 206
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dallas-Fort Worth, TX (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 212
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Dayton-Springfield-Greenville, OH (Part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 216
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Denver-Aurora-Boulder, CO"
      }

      when: {
        sql: ${TABLE}.gtcsa = 220
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Detroit-Warren-Flint, MI"
      }

      when: {
        sql: ${TABLE}.gtcsa = 260
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Fresno-Madera, CA"
      }

      when: {
        sql: ${TABLE}.gtcsa = 266
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Grand Rapids-Muskegon-Holland, MI (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 268
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Greensboro-Winston-Salem-High Point, NC (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 272
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Greenville-Anderson-Seneca, SC (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 288
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Houston-Baytown-Huntsville, TX (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 290
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Huntsville-Decatur, AL"
      }

      when: {
        sql: ${TABLE}.gtcsa = 294
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Indianapolis-Anderson-Columbus, IN (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 304
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Johnson City-Kingsport-Bristol, TN-VA (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 348
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Los Angeles-Long Beach-Riverside, CA"
      }

      when: {
        sql: ${TABLE}.gtcsa = 356
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Macon-Warner-Robins-Fort Valley, GA (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 376
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Milwaukee-Racine-Waukesha, WI"
      }

      when: {
        sql: ${TABLE}.gtcsa = 378
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Minneapolis-St. Paul-St. Cloud, MN-WI (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 408
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "New York-Newark-Bridgeport, NY-NJ-CT-PA (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 428
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Philadelphia-Camden-Vineland, PA-NJ-DE-MD (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 450
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Raleigh-Durham-Cary, NC (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 472
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sacramento–Arden-Arcade–Truckee, CA-NV (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 482
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Salt Lake City-Ogden-Clearfield, UT (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 488
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "San Jose-San Francisco-Oakland, CA"
      }

      when: {
        sql: ${TABLE}.gtcsa = 500
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Seattle-Tacoma-Olympia, WA"
      }

      when: {
        sql: ${TABLE}.gtcsa = 548
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Washington-Baltimore-Northern Virginia, DC-MD-VA-WV (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 715
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boston-Worchester-Manchester, MS-NH-CT-ME (part)"
      }

      when: {
        sql: ${TABLE}.gtcsa = 720
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bridgeport-New Haven-Stamford, CT"
      }
    }
  }

  dimension: gtcbsa {
    label: "Metropolitan statistical area fips code"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtcbsa = 00000
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not Identified or Nonmetropolitan"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 00460
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Appleton-Oshkosh-Neenah, WI MSA*"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 03000
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Grand Rapids-Muskegon-Holland, MI MSA*"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 03160
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Greenville-Spartanburg-Anderson, SC MSA*"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 03610
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Jamestown, NY MSA*"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 03720
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Kalamazoo-Battle Creek, MI MSA* (Van Buren County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 06450
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Portsmouth-Rochester, NH-ME MSA* (ME portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 10420
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Akron, OH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 10500
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Albany, GA (Baker, Terrell, and Worth Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 10580
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Albany-Schenectady-Troy, NY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 10740
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Albuquerque, NM"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 10900
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Allentown-Bethlehem-Easton, PA-NJ"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11020
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Altoona, PA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11100
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Amarillo, TX (Armstrong and Carson Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11260
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Anchorage, AK"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11300
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Anderson, IN"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11340
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Anderson, SC"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11460
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Ann Arbor, MI"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11500
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Anniston-Oxford, AL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11540
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Appleton,WI"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 11700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Asheville, NC (Haywood and Henderson Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12020
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Athens-Clark County, GA (Oglethorpe County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12060
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Atlanta-Sandy Springs-Marietta, GA (Haralson, Heard, Jasper, Meriwether and Spalding C"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12100
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Atlantic City, NJ"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12260
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Augusta-Richmond County, GA-SC"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12420
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Austin-Round Rock, TX"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12540
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bakersfield, CA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12580
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Baltimore-Towson, MD"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 12940
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Baton Rouge, LA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13140
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Beaumont-Port Author, TX"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13380
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bellingham, WA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13460
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bend, OR"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13740
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Billings, MT (Carbon County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13780
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Binghamton, NY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 13820
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Birmingham-Hoover, AL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14020
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bloomington, IN (Owen County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14060
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bloomington-Normal IL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14260
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boise City-Nampa, ID (Owyhee County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14500
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boulder, CO"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14540
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bowling Green, KY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 14740
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bremerton-Silverdale, WA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 15180
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Brownsville-Harlingen, TX"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 15380
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Buffalo-Niagara Falls, NY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 15940
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Canton-Massillon, OH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 15980
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cape Coral-Fort Myers, FL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16300
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Cedar Rapids, IA (Benton and Jones Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16580
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Champaign-Urbana, IL (Ford County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16620
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Charleston, WV (Clay County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Charleston-North Charleston, SC"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16740
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Charlotte-Gastonia-Concord, NC-SC (Anson County, NC not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 16860
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "

          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Sioux Falls, SD"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 43780
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "South Bend-Mishawaka, IN-MI (Michigan portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 43900
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Spartanburg, SC"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 44060
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Spokane, WA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 44100
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Springfield, IL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 44180
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Springfield, MO (Dallas and Polk Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 44220
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Springfield, OH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 44700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Stockton, CA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45060
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Syracuse, NY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45220
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tallahassee, FL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45300
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tampa-St. Petersburg-Clearwater, FL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45780
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Toledo, OH (Ottawa County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45820
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Topeka, KS (Jackson and Jefferson Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 45940
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Trenton-Ewing, NJ"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46060
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tucson, AZ"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46140
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tulsa, OK (Okmulgee County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46220
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Tuscaloosa, AL (Greene and Hale Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46540
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Utica-Rome, NY"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46660
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Valdosta, GA (Lanier County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Vallejo-Fairfield, CA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 46940
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Vero Beach, FL"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47020
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Victoria, TX"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47220
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Vineland-Millville-Bridgeton, NJ"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47260
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Virginia Beach-Norfolk-Newport News, VA-NC (North Carolina portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47300
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Visalia-Porterville, CA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47380
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waco, TX"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47580
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Warner Robins, GA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47900
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Washington-Arlington-Alexandria, DC-VA-MD-WV (West Virginia portion not identified.  R"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 47940
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waterloo-Cedar Falls, IA (Grundy County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 48140
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wausau, WI"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 48540
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wheeling, WV-OH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 48620
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Wichita, KS"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 49180
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Winston-Salem, NC"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 49420
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Yakima, WA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 49620
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "York-Hanover, PA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 49660
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Youngstown-Warren-Boardman, OH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 70750
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bangor, ME"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 70900
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Barnstable Town, MA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 71650
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Boston-Cambridge-Quincy, MA-NH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 71950
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bridgeport-Stamford-Norwalk, CT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 72400
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Burlington-South Burlington, VT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 72850
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Danbury, CT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 73450
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Hartford-West Hartford-East Hartford, CT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 74500
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Leominster-Fitchburg-Gardner, MA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 74950
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Manchester, NH"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 75550
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "New Bedford, MA"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 75700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "New Haven, CT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 76450
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Norwich-New London, CT-RI (RI portion recoded to Providence NECTA)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 76750
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Portland-South Portland, ME"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 77200
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Providence-Fall River-Warwick, MA-RI"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 77350
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Rochester-Dover, NH-ME (Maine portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 78100
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Springfield, MA-CT (Connecticut portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 78700
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Waterbury, CT"
      }

      when: {
        sql: ${TABLE}.gtcbsa = 79600
          AND ${TABLE}.src_table in ('census_2012', 'census_2014', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Worcester, MA-CT (Connecticut portion not identified)"
      }
    }
  }

  dimension: pes1 {
    label: "Vote in the november election"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes1 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes1 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes1 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes1 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes1 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes1 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No"
      }
    }
  }

  dimension: pes2 {
    label: "Registered to vote in the november election"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes2 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes2 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes2 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes2 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes2 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes2 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No"
      }
    }
  }

  dimension: pusck4 {
    label: "Self or other reported for person"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pusck4 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pusck4 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Self"
      }

      when: {
        sql: ${TABLE}.pusck4 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pes3 {
    label: "Reason not registered"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes3 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "No answer"
      }

      when: {
        sql: ${TABLE}.pes3 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.pes3 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes3 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Out of Universe"
      }

      when: {
        sql: ${TABLE}.pes3 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Did not meet registration deadlines"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Did not know where or how to register"
      }

      when: {
        sql: ${TABLE}.pes3 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Did not meet residency requirements/did not live here long enough"
      }

      when: {
        sql: ${TABLE}.pes3 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Permanent illness or disability"
      }

      when: {
        sql: ${TABLE}.pes3 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Difficulty with English"
      }

      when: {
        sql: ${TABLE}.pes3 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not interested in the election or not involved in politics"
      }

      when: {
        sql: ${TABLE}.pes3 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "My vote would not make a difference"
      }

      when: {
        sql: ${TABLE}.pes3 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not eligible to vote"
      }

      when: {
        sql: ${TABLE}.pes3 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other reason"
      }
    }
  }

  dimension: pes4 {
    label: "Main reason (you/name) did not vote?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes4 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "No answer"
      }

      when: {
        sql: ${TABLE}.pes4 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.pes4 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes4 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Out of Universe"
      }

      when: {
        sql: ${TABLE}.pes4 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Illness or disability (own or family's)"
      }

      when: {
        sql: ${TABLE}.pes4 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Out of town or away from home"
      }

      when: {
        sql: ${TABLE}.pes4 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Forgot to vote (or send in absentee ballot)"
      }

      when: {
        sql: ${TABLE}.pes4 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Not interested, felt my vote wouldn't make a difference"
      }

      when: {
        sql: ${TABLE}.pes4 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Too busy, conflicting work or school schedule"
      }

      when: {
        sql: ${TABLE}.pes4 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Transportation problems"
      }

      when: {
        sql: ${TABLE}.pes4 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Didn't like candidates or campaign issues"
      }

      when: {
        sql: ${TABLE}.pes4 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Registration problems (i.e. didn't receive absentee ballot, not registered in current location)"
      }

      when: {
        sql: ${TABLE}.pes4 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Bad weather conditions"
      }

      when: {
        sql: ${TABLE}.pes4 = 10
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Inconvenient hours, polling place or hours or lines too long"
      }

      when: {
        sql: ${TABLE}.pes4 = 11
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pes5 {
    label: "Did you vote in person or did you vote by mail?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes5 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "No answer"
      }

      when: {
        sql: ${TABLE}.pes5 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.pes5 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes5 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Out of Universe"
      }

      when: {
        sql: ${TABLE}.pes5 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "In person"
      }

      when: {
        sql: ${TABLE}.pes5 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "By mail"
      }
    }
  }

  dimension: pes6 {
    label: "Was that on election day or before election day"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes6 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "No answer"
      }

      when: {
        sql: ${TABLE}.pes6 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.pes6 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes6 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Out of Universe"
      }

      when: {
        sql: ${TABLE}.pes6 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "On election day"
      }

      when: {
        sql: ${TABLE}.pes6 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004')
           ;;
        label: "Before election day"
      }
    }
  }

  dimension: pes7 {
    label: "How did (you/name) register to vote?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "At a department of motor vehicles (for example, when obtaining a driver's license or other identification card)"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "At a public assistance agency (for example, a Medicaid, AFDC, or Food Stamps office, an office serving di)"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Registered using the internet or online"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Filled out form at a registration drive (library, post office, or someone came to your door)"
      }

      when: {
        sql: ${TABLE}.pes7 = 8
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 9
          AND ${TABLE}.src_table in ('census_2014', 'census_2012', 'census_2010')
           ;;
        label: "Other"
      }
    }
  }

  dimension: prs8 {
    label: "Time at current address"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prs8 = -9
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.prs8 = -3
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.prs8 = -2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.prs8 = -1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prs8 = 1
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "Less than 1 year"
      }

      when: {
        sql: ${TABLE}.prs8 = 2
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "1-2 years"
      }

      when: {
        sql: ${TABLE}.prs8 = 3
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "3-4 years"
      }

      when: {
        sql: ${TABLE}.prs8 = 4
          AND ${TABLE}.src_table in ('census_2014')
           ;;
        label: "5 YEARS OR LONGER"
      }
    }
  }

  dimension: gestcen {
    label: "Census state code"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gestcen = 11
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "ME"
      }

      when: {
        sql: ${TABLE}.gestcen = 12
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NH"
      }

      when: {
        sql: ${TABLE}.gestcen = 13
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "VT"
      }

      when: {
        sql: ${TABLE}.gestcen = 14
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MA"
      }

      when: {
        sql: ${TABLE}.gestcen = 15
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "RI"
      }

      when: {
        sql: ${TABLE}.gestcen = 16
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "CT"
      }

      when: {
        sql: ${TABLE}.gestcen = 21
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NY"
      }

      when: {
        sql: ${TABLE}.gestcen = 22
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NJ"
      }

      when: {
        sql: ${TABLE}.gestcen = 23
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "PA"
      }

      when: {
        sql: ${TABLE}.gestcen = 31
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "OH"
      }

      when: {
        sql: ${TABLE}.gestcen = 32
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "IN"
      }

      when: {
        sql: ${TABLE}.gestcen = 33
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "IL"
      }

      when: {
        sql: ${TABLE}.gestcen = 34
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MI"
      }

      when: {
        sql: ${TABLE}.gestcen = 35
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "WI"
      }

      when: {
        sql: ${TABLE}.gestcen = 41
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MN"
      }

      when: {
        sql: ${TABLE}.gestcen = 42
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "IA"
      }

      when: {
        sql: ${TABLE}.gestcen = 43
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MO"
      }

      when: {
        sql: ${TABLE}.gestcen = 44
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "ND"
      }

      when: {
        sql: ${TABLE}.gestcen = 45
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "SD"
      }

      when: {
        sql: ${TABLE}.gestcen = 46
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NE"
      }

      when: {
        sql: ${TABLE}.gestcen = 47
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "KS"
      }

      when: {
        sql: ${TABLE}.gestcen = 51
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "DE"
      }

      when: {
        sql: ${TABLE}.gestcen = 52
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MD"
      }

      when: {
        sql: ${TABLE}.gestcen = 53
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "DC"
      }

      when: {
        sql: ${TABLE}.gestcen = 54
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "VA"
      }

      when: {
        sql: ${TABLE}.gestcen = 55
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "WV"
      }

      when: {
        sql: ${TABLE}.gestcen = 56
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NC"
      }

      when: {
        sql: ${TABLE}.gestcen = 57
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "SC"
      }

      when: {
        sql: ${TABLE}.gestcen = 58
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "GA"
      }

      when: {
        sql: ${TABLE}.gestcen = 59
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "FL"
      }

      when: {
        sql: ${TABLE}.gestcen = 61
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "KY"
      }

      when: {
        sql: ${TABLE}.gestcen = 62
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "TN"
      }

      when: {
        sql: ${TABLE}.gestcen = 63
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "AL"
      }

      when: {
        sql: ${TABLE}.gestcen = 64
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MS"
      }

      when: {
        sql: ${TABLE}.gestcen = 71
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "AR"
      }

      when: {
        sql: ${TABLE}.gestcen = 72
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "LA"
      }

      when: {
        sql: ${TABLE}.gestcen = 73
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "OK"
      }

      when: {
        sql: ${TABLE}.gestcen = 74
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "TX"
      }

      when: {
        sql: ${TABLE}.gestcen = 81
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "MT"
      }

      when: {
        sql: ${TABLE}.gestcen = 82
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "ID"
      }

      when: {
        sql: ${TABLE}.gestcen = 83
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "WY"
      }

      when: {
        sql: ${TABLE}.gestcen = 84
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "CO"
      }

      when: {
        sql: ${TABLE}.gestcen = 85
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NM"
      }

      when: {
        sql: ${TABLE}.gestcen = 86
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "AZ"
      }

      when: {
        sql: ${TABLE}.gestcen = 87
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "UT"
      }

      when: {
        sql: ${TABLE}.gestcen = 88
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "NV"
      }

      when: {
        sql: ${TABLE}.gestcen = 91
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "WA"
      }

      when: {
        sql: ${TABLE}.gestcen = 92
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "OR"
      }

      when: {
        sql: ${TABLE}.gestcen = 93
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "CA"
      }

      when: {
        sql: ${TABLE}.gestcen = 94
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "AK"
      }

      when: {
        sql: ${TABLE}.gestcen = 95
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "HI"
      }
    }
  }

  dimension: prdthsp_census_2012 {
    label: "Demographics- detailed hispanic origin subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = -1
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Central/South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other Spanish"
      }
    }
  }

  dimension: peio2icd_census_2012 {
    label: "(second job)industry code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio2icd = 0170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Crop production"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Animal production"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Forestry except logging"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Logging"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Fishing, hunting, and trapping"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Support activities for agriculture and forestry"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Oil and gas extraction"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Coal mining"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Metal ore mining"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Nonmetallic mineral mining and quarrying and not specified type of mining"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Support activities for mining"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electric power generation, transmission and distribution"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Natural gas distribution"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electric and gas, and other combinations"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Water, steam, air-conditioning, and irrigation systems"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sewage treatment facilities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified utilities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 0770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Construction  (Includes the cleaning of buildings and dwellings is incidental ...)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Animal food, grain and oilseed milling"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sugar and confectionery products"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Fruit and vegetable preserving and specialty food manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Dairy product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Animal slaughtering and processing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Retail bakeries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Bakeries, except retail"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Seafood and other miscellaneous foods, n.e.c."
      }

      when: {
        sql: ${TABLE}.peio2icd = 1290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified food industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Beverage manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Tobacco manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Fiber, yarn, and thread mills"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Fabric mills, except knitting"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Textile and fabric finishing and coating mills"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Carpet and rug mills"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Textile product mills, except carpets and rugs"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Knitting mills"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Cut and sew apparel manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Apparel accessories and other apparel manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Footwear manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Leather tanning and products, except footwear manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Pulp, paper, and paperboard mills"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Paperboard containers and boxes"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous paper and pulp products"
      }

      when: {
        sql: ${TABLE}.peio2icd = 1990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Printing and related support activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Petroleum refining"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous petroleum and coal products"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Resin, synthetic rubber and fibers, and filaments manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Agricultural chemical manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Pharmaceutical and medicine manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Paint, coating, and adhesive manufacturing B46"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Soap, cleaning compound, and cosmetics manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Industrial and miscellaneous chemicals"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Plastics product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Tire manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Rubber products, except tires, manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Pottery, ceramics, and related products manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Structural clay product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Glass and glass product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Cement, concrete, lime, and gypsum product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous nonmetallic mineral product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Iron and steel mills and steel product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Aluminum production and processing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Nonferrous metal, except aluminum, production and processing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Foundries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Metal forgings and stampings"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Cutlery and hand tool manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Structural metals, and tank and shipping container manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Machine shops; turned product; screw, nut and bolt manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Coating, engraving, heat treating and allied activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Ordnance"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2980
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous fabricated metal products manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 2990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified metal industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Agricultural implement manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Construction, mining and oil field machinery manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Commercial and service industry machinery manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Metalworking machinery manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Engines, turbines, and power transmission equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Machinery manufacturing, n.e.c."
      }

      when: {
        sql: ${TABLE}.peio2icd = 3360
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Computer and peripheral equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Communications, audio, and video equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Navigational, measuring, electromedical, and control instruments manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electronic component and product manufacturing, n.e.c."
      }

      when: {
        sql: ${TABLE}.peio2icd = 3470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Household appliance manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electrical lighting, equipment, and supplies manufacturing, n.e.c."
      }

      when: {
        sql: ${TABLE}.peio2icd = 3570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Motor vehicles and motor vehicle equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Aircraft and parts manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Aerospace products and parts manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Railroad rolling stock manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Ship and boat building"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other transportation equipment manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sawmills and wood preservation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Veneer, plywood, and engineered wood products"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Prefabricated wood buildings and mobile homes"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous wood products"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Furniture and related product manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3960
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Medical equipment and supplies manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Toys, amusement, and sporting goods manufacturing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 3980
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous manufacturing, n.e.c."
      }

      when: {
        sql: ${TABLE}.peio2icd = 3990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified manufacturing industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Motor vehicles, parts and supplies, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Furniture and home furnishing, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Lumber and other construction materials, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Professional and commercial equipment and supplies, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Metals and minerals, except petroleum, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electrical goods, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4260
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Hardware, plumbing and heating equipment, and supplies, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Machinery, equipment, and supplies, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Recyclable material, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous durable goods, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Paper and paper products, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Drugs, sundries, and chemical and allied products, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Apparel, fabrics, and notions, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Groceries and related products, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Farm product raw materials, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Petroleum and petroleum products, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4560
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Alcoholic beverages, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Farm supplies, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous nondurable goods, merchant wholesalers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4585
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Wholesale electronic markets, agents and brokers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified wholesale trade"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Automobile dealers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other motor vehicle dealers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Auto parts, accessories, and tire stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Furniture and home furnishings stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Household appliance stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Radio, TV, and computer stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Building material and supplies dealers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Hardware stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Lawn and garden equipment and supplies stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Grocery stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4980
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Specialty food stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 4990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Beer, wine, and liquor stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Pharmacies and drug stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Health and personal care, except drug, stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Gasoline stations"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Clothing and accessories, except shoe, stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Shoe stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Jewelry, luggage, and leather goods stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sporting goods, camera, and hobby and toy stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sewing, needlework, and piece goods stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Music stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Book stores and news dealers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Department stores and discount stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous general merchandise stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Retail florists"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Office supplies and stationery stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Used merchandise stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Gift, novelty, and souvenir shops"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Miscellaneous retail stores"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electronic shopping"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5591
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electronic auctions"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5592
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Mail order houses"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Vending machine operators"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Fuel dealers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other direct selling establishments"
      }

      when: {
        sql: ${TABLE}.peio2icd = 5790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Not specified retail trade"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Air transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Rail transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Water transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Truck transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Bus service and urban transit"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Taxi and limousine service"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Pipeline transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Scenic and sightseeing transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Services incidental to transportation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Postal Service"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Couriers and messengers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Warehousing and storage"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Newspaper publishers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Publishing, except newspapers and software"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Software publishing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Motion pictures and video industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Sound recording industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Radio and television broadcasting and cable"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6672
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Internet Publishing and Broadcasting"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Wired telecommunications carriers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other telecommunications services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6695
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Data processing, hosting, and related services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Libraries and archives"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other information services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Banking and related activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Savings institutions, including credit unions"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Non-depository credit and related activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Securities, commodities, funds, trusts, and other financial investments"
      }

      when: {
        sql: ${TABLE}.peio2icd = 6990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Insurance carriers and related activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Real estate"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Automotive equipment rental and leasing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Video tape and disk rental"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other consumer goods rental"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Commercial, industrial, and other intangible assets rental and leasing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Legal services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7280
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Accounting, tax preparation, bookkeeping, and payroll services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Architectural, engineering, and related services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Specialized design services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Computer systems design and related services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Management, scientific, and technical consulting services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7460
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Scientific research and development services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Advertising and related services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Veterinary services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other professional, scientific, and technical services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Management of companies and enterprises"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Employment services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Business support services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Travel arrangements and reservation services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Investigation and security services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Services to buildings and dwellings (except cleaning during construction and immediately after construction)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Landscaping services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other administrative and other support services  ,"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Waste management and remediation services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7860
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Elementary and secondary schools"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Colleges and universities, including junior colleges"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Business, technical, and trade schools and training"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other schools, instruction, and educational services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Offices of physicians"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7980
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Offices of dentists"
      }

      when: {
        sql: ${TABLE}.peio2icd = 7990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Offices of chiropractors"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Offices of optometrists"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Offices of other health practitioners"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Outpatient care centers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Home health care services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other health care services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8270
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Nursing care facilities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Residential care facilities, without nursing"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Individual and family services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Community food and housing, and emergency services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Vocational rehabilitation services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Child day care services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8560
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Independent artists, performing arts, spectator sports, and related industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Museums, art galleries, historical sites, and similar institutions"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8580
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Bowling centers"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other amusement, gambling, and recreation industries"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8660
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Traveler accommodation"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8670
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Recreational vehicle parks and camps, and rooming and boarding houses"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8680
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Restaurants and other food services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8690
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Drinking places, alcoholic beverages"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8770
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Automotive repair and maintenance"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8780
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Car washes"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8790
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Electronic and precision equipment repair and maintenance"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8870
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Commercial and industrial machinery and equipment repair and maintenance"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8880
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Personal and household goods repair and maintenance and footwear and leather goods repair"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8970
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Barber shops"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8980
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Beauty salons"
      }

      when: {
        sql: ${TABLE}.peio2icd = 8990
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Nail salons and other personal care services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9070
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Dry cleaning and laundry services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9080
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Funeral homes, cemeteries, and crematories"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9090
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other personal services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9160
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Religious organizations"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9170
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Civic, social, advocacy organizations, and grant making and giving services"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9180
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Labor unions"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9190
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Business, professional, political, and similar organizations"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9290
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Private households"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9370
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Executive offices and legislative bodies"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9380
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Public finance activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9390
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Other general government and support"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9470
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Justice, public order, and safety activities"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9480
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Administration of human resource programs"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9490
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Administration of environmental quality and housing programs"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9570
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Administration of economic programs and space research"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9590
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "National security and international affairs"
      }

      when: {
        sql: ${TABLE}.peio2icd = 9890
          AND ${TABLE}.src_table in ('census_2012')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: pes8 {
    label: "Time at current address"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes8 = -9
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes8 = -3
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes8 = -2
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes8 = -1
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes8 = 1
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Less than 1 month"
      }

      when: {
        sql: ${TABLE}.pes8 = 2
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "1-6 months"
      }

      when: {
        sql: ${TABLE}.pes8 = 3
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "7-11 months"
      }

      when: {
        sql: ${TABLE}.pes8 = 4
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "1-2 years"
      }

      when: {
        sql: ${TABLE}.pes8 = 5
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "3-4 years"
      }

      when: {
        sql: ${TABLE}.pes8 = 6
          AND ${TABLE}.src_table in ('census_2012', 'census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "5 years or longer"
      }
    }
  }

  dimension: pupelig {
    label: "Interview status of persons in the household"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pupelig = 1
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Eligible for interview"
      }

      when: {
        sql: ${TABLE}.pupelig = 2
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Labor force fully complete"
      }

      when: {
        sql: ${TABLE}.pupelig = 3
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Missing labor force data for person"
      }

      when: {
        sql: ${TABLE}.pupelig = 4
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "(not Used)"
      }

      when: {
        sql: ${TABLE}.pupelig = 5
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Assigned If Age Is Blank"
      }

      when: {
        sql: ${TABLE}.pupelig = 6
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Armed forces member"
      }

      when: {
        sql: ${TABLE}.pupelig = 7
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Under 15 years old"
      }

      when: {
        sql: ${TABLE}.pupelig = 8
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not a HH Member"
      }

      when: {
        sql: ${TABLE}.pupelig = 9
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Deleted"
      }

      when: {
        sql: ${TABLE}.pupelig = 10
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Deceased"
      }

      when: {
        sql: ${TABLE}.pupelig = 11
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "End of list"
      }

      when: {
        sql: ${TABLE}.pupelig = 12
          AND ${TABLE}.src_table in ('census_2010', 'census_2008', 'census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "After End Of List"
      }
    }
  }

  dimension: ptdtrace_census_2010 {
    label: "Demographics- race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptdtrace = 1
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "White Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 2
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Black Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 3
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "American Indian, Alaskan Native Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 4
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Asian Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 5
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Hawaiian/Pacific Islander Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 6
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "White-Black"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 7
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "White-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 8
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "White-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 9
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "White-Hawaiian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 10
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Black-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 11
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Black-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 12
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Black-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 13
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "AI-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 14
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Asian-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 15
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "W-B-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 16
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "W-B-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 17
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "W-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 18
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "W-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 19
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "W-B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 20
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "2 or 3 Races"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 21
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "4 or 5 Races"
      }
    }
  }

  dimension: prdthsp_census_2010 {
    label: "Demographics- detailed hispanic origin subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = -1
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Central/South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2010')
           ;;
        label: "Other Spanish"
      }
    }
  }

  dimension: hutypea_census_2008 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: hufaminc {
    label: "Total family income in past 12 months"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufaminc = -3
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufaminc = -2
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hufaminc = -1
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hufaminc = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Less Than $5,000"
      }

      when: {
        sql: ${TABLE}.hufaminc = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "5,000 To 7,499"
      }

      when: {
        sql: ${TABLE}.hufaminc = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "7,500 To 9,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "10,000 To 12,499"
      }

      when: {
        sql: ${TABLE}.hufaminc = 5
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "12,500 To 14,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 6
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "15,000 To 19,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 7
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "20,000 To 24,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 8
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "25,000 To 29,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 9
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "30,000 To 34,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 10
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "35,000 To 39,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 11
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "40,000 To 49,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 12
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "50,000 To 59,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 13
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "60,000 To 74,999"
      }

      when: {
        sql: ${TABLE}.hufaminc = 14
          AND ${TABLE}.src_table in ('census_2002', 'census_2008', 'census_2006', 'census_2004', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "75,000 Or More"
      }
    }
  }

  dimension: ptdtrace_census_2008 {
    label: "Demographics- race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptdtrace = 1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "White Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Black Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "American Indian, Alaskan Native Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 4
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Asian Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 5
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Hawaiian/Pacific Islander Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 6
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "White-Black"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 7
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "White-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 8
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "White-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 9
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "White-Hawaiian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 10
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Black-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 11
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Black-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 12
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Black-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 13
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "AI-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 14
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Asian-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 15
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "W-B-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 16
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "W-B-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 17
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "W-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 18
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "W-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 19
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "W-B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 20
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "2 or 3 Races"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 21
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "4 or 5 Races"
      }
    }
  }

  dimension: prdthsp_census_2008 {
    label: "Demographics- detailed hispanic origin subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = -1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Central/South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Other Spanish"
      }
    }
  }

  dimension: pes7_census_2008 {
    label: "How did you register to vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "At a department of motor vehicles (for example, when obtaining a driver's license or other identific"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "At a public assistance agency (for example, a Medicaid, AFDC, or Food Stamps office, an office servi"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Filled out form at a registration drive (library, post office, or someone came to your door)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 8
          AND ${TABLE}.src_table in ('census_2008')
           ;;
        label: "Other"
      }
    }
  }

  dimension: huspnish {
    label: "Spanish only spoken"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.huspnish = -3
          AND ${TABLE}.src_table in ('census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.huspnish = -2
          AND ${TABLE}.src_table in ('census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.huspnish = -1
          AND ${TABLE}.src_table in ('census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.huspnish = 1
          AND ${TABLE}.src_table in ('census_2006', 'census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Spanish Only Language Spoken"
      }
    }
  }

  dimension: hutypea_census_2006 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: pefntvty_census_2006 {
    label: "Native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 57
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 72
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 96
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 155
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 180
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 183
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 184
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 185
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 192
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 195
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 200
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 202
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pefntvty = 205
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pefntvty = 206
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 207
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pefntvty = 209
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pefntvty = 210
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pefntvty = 211
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 212
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pefntvty = 213
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pefntvty = 214
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pefntvty = 215
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 216
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 217
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 218
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 221
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pefntvty = 222
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 224
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 229
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 231
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 233
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 234
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pefntvty = 237
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 238
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 239
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 240
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pefntvty = 242
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 245
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 252
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pefntvty = 253
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 300
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 301
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 304
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 310
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pefntvty = 311
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 312
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 313
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pefntvty = 314
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pefntvty = 315
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 316
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pefntvty = 317
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pefntvty = 318
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 333
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pefntvty = 334
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pefntvty = 337
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pefntvty = 338
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 339
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 340
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 342
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pefntvty = 343
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 351
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pefntvty = 353
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pefntvty = 375
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pefntvty = 376
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 377
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pefntvty = 378
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pefntvty = 379
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 380
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 383
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 385
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pefntvty = 387
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pefntvty = 388
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pefntvty = 389
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 415
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pefntvty = 417
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 421
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 427
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pefntvty = 436
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pefntvty = 440
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 449
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 462
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 468
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 501
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 507
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pefntvty = 514
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 527
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 555
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pemntvty_census_2006 {
    label: "Native country of mother"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemntvty = 57
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pemntvty = 72
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 96
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pemntvty = 102
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 103
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pemntvty = 105
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 106
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pemntvty = 108
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 109
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pemntvty = 110
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pemntvty = 116
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pemntvty = 117
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pemntvty = 119
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pemntvty = 120
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pemntvty = 126
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 127
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pemntvty = 128
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 129
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 130
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pemntvty = 132
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 134
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 136
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pemntvty = 137
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 138
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 139
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pemntvty = 140
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 142
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 147
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 148
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pemntvty = 155
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 156
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 180
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pemntvty = 183
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 184
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 185
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 192
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 195
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 200
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 202
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pemntvty = 205
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pemntvty = 206
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 207
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pemntvty = 209
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pemntvty = 210
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pemntvty = 211
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 212
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pemntvty = 213
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pemntvty = 214
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pemntvty = 215
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 216
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 217
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 218
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 221
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pemntvty = 222
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pemntvty = 224
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 229
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 231
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pemntvty = 233
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 234
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pemntvty = 237
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 238
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 239
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 240
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pemntvty = 242
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pemntvty = 245
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 252
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pemntvty = 253
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 300
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 301
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 304
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 310
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pemntvty = 311
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 312
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 313
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pemntvty = 314
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pemntvty = 315
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 316
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pemntvty = 317
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pemntvty = 318
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 333
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pemntvty = 334
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pemntvty = 337
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pemntvty = 338
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 339
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 340
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 342
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pemntvty = 343
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 351
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pemntvty = 353
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pemntvty = 375
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pemntvty = 376
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 377
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pemntvty = 378
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pemntvty = 379
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 380
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 383
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 385
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pemntvty = 387
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pemntvty = 388
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pemntvty = 389
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 415
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pemntvty = 417
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 421
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 427
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pemntvty = 436
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pemntvty = 440
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 449
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 462
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 468
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 501
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 507
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pemntvty = 514
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 527
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 555
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other"
      }
    }
  }

  dimension: penatvty_census_2006 {
    label: "Native country of sample person"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penatvty = 57
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.penatvty = 72
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.penatvty = 96
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.penatvty = 102
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.penatvty = 103
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.penatvty = 105
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 106
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.penatvty = 108
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.penatvty = 109
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.penatvty = 110
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.penatvty = 116
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.penatvty = 117
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.penatvty = 119
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.penatvty = 120
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.penatvty = 126
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.penatvty = 127
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.penatvty = 128
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.penatvty = 129
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.penatvty = 130
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.penatvty = 132
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.penatvty = 134
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.penatvty = 136
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.penatvty = 137
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.penatvty = 138
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.penatvty = 139
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.penatvty = 140
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.penatvty = 142
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 147
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.penatvty = 148
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.penatvty = 155
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 156
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 180
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.penatvty = 183
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.penatvty = 184
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.penatvty = 185
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.penatvty = 192
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.penatvty = 195
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.penatvty = 200
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 202
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.penatvty = 205
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.penatvty = 206
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.penatvty = 207
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.penatvty = 209
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.penatvty = 210
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.penatvty = 211
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.penatvty = 212
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.penatvty = 213
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.penatvty = 214
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.penatvty = 215
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.penatvty = 216
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.penatvty = 217
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 218
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 221
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.penatvty = 222
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.penatvty = 224
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.penatvty = 229
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 231
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.penatvty = 233
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.penatvty = 234
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.penatvty = 237
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.penatvty = 238
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.penatvty = 239
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.penatvty = 240
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.penatvty = 242
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.penatvty = 245
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.penatvty = 252
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.penatvty = 253
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.penatvty = 300
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 301
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.penatvty = 304
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.penatvty = 310
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.penatvty = 311
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.penatvty = 312
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.penatvty = 313
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.penatvty = 314
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.penatvty = 315
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.penatvty = 316
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.penatvty = 317
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.penatvty = 318
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.penatvty = 333
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.penatvty = 334
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.penatvty = 337
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.penatvty = 338
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.penatvty = 339
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 340
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.penatvty = 342
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.penatvty = 343
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.penatvty = 351
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.penatvty = 353
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.penatvty = 375
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.penatvty = 376
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.penatvty = 377
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.penatvty = 378
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.penatvty = 379
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.penatvty = 380
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.penatvty = 383
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.penatvty = 385
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.penatvty = 387
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 388
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.penatvty = 389
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.penatvty = 415
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.penatvty = 417
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.penatvty = 421
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.penatvty = 427
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.penatvty = 436
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.penatvty = 440
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 449
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 462
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 468
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 501
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.penatvty = 507
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.penatvty = 514
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.penatvty = 527
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.penatvty = 555
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other"
      }
    }
  }

  dimension: ptdtrace_census_2006 {
    label: "Demographics- race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptdtrace = 1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "White Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Black Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "American Indian, Alaskan Native Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 4
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Asian Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 5
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Hawaiian/Pacific Islander Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 6
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "White-Black"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 7
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "White-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 8
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "White-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 9
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "White-Hawaiian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 10
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Black-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 11
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Black-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 12
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Black-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 13
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "AI-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 14
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Asian-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 15
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "W-B-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 16
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "W-B-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 17
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "W-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 18
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "W-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 19
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "W-B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 20
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "2 or 3 Races"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 21
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "4 or 5 Races"
      }
    }
  }

  dimension: prdthsp_census_2006 {
    label: "Demographics- detailed hispanic origin subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = -1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Central/South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other Spanish"
      }
    }
  }

  dimension: pes7_census_2006 {
    label: "How did you register to vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "At a department of motor vehicles (for example, when obtaining a driver's license or other identific"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "At a public assistance agency (for example, a Medicaid, AFDC, or Food Stamps office, an office servi"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Filled out form at a registration drive (library, post office, or someone came to your door)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 8
          AND ${TABLE}.src_table in ('census_2006')
           ;;
        label: "Other"
      }
    }
  }

  dimension: hutypea_census_2004 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: peafwhen {
    label: "Past military service,period of active duty"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peafwhen = -1
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peafwhen = 1
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Vietnam Era (8/64-4/75)"
      }

      when: {
        sql: ${TABLE}.peafwhen = 2
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Korean War (6/50-1/55)"
      }

      when: {
        sql: ${TABLE}.peafwhen = 3
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "World War Ii (9/40-7/47)"
      }

      when: {
        sql: ${TABLE}.peafwhen = 4
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "World War I (4/17-11/18)"
      }

      when: {
        sql: ${TABLE}.peafwhen = 5
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Other Service (all Other Periods)"
      }

      when: {
        sql: ${TABLE}.peafwhen = 6
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Nonveteran"
      }
    }
  }

  dimension: pefntvty_census_2004 {
    label: "Native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 57
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 72
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 96
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 155
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 180
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 183
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 184
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 185
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 192
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 195
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 200
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 202
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pefntvty = 205
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pefntvty = 206
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 207
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pefntvty = 209
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pefntvty = 210
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pefntvty = 211
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 212
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pefntvty = 213
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pefntvty = 214
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pefntvty = 215
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 216
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 217
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 218
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 221
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pefntvty = 222
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 224
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 229
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 231
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 233
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 234
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pefntvty = 237
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 238
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 239
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 240
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pefntvty = 242
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 245
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 252
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pefntvty = 253
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 300
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 301
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 304
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 310
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pefntvty = 311
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 312
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 313
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pefntvty = 314
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pefntvty = 315
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 316
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pefntvty = 317
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pefntvty = 318
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 333
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pefntvty = 334
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pefntvty = 337
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pefntvty = 338
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 339
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 340
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 342
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pefntvty = 343
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 351
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pefntvty = 353
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pefntvty = 375
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pefntvty = 376
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 377
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pefntvty = 378
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pefntvty = 379
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 380
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 383
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 385
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pefntvty = 387
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pefntvty = 388
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pefntvty = 389
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 415
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pefntvty = 417
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 421
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 427
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pefntvty = 436
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pefntvty = 440
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 449
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 462
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 468
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 501
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 507
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pefntvty = 514
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 527
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 555
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pemntvty_census_2004 {
    label: "Native country of mother"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemntvty = 57
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pemntvty = 72
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 96
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pemntvty = 102
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 103
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pemntvty = 105
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 106
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pemntvty = 108
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 109
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pemntvty = 110
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pemntvty = 116
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pemntvty = 117
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pemntvty = 119
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pemntvty = 120
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pemntvty = 126
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 127
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pemntvty = 128
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 129
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 130
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pemntvty = 132
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 134
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 136
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pemntvty = 137
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 138
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 139
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pemntvty = 140
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 142
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 147
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 148
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pemntvty = 155
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 156
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 180
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pemntvty = 183
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 184
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 185
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 192
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 195
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 200
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 202
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pemntvty = 205
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pemntvty = 206
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 207
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pemntvty = 209
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pemntvty = 210
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pemntvty = 211
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 212
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pemntvty = 213
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pemntvty = 214
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pemntvty = 215
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 216
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 217
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 218
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 221
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pemntvty = 222
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pemntvty = 224
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 229
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 231
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pemntvty = 233
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 234
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pemntvty = 237
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 238
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 239
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 240
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pemntvty = 242
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pemntvty = 245
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 252
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pemntvty = 253
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 300
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 301
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 304
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 310
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pemntvty = 311
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 312
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 313
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pemntvty = 314
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pemntvty = 315
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 316
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pemntvty = 317
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pemntvty = 318
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 333
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pemntvty = 334
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pemntvty = 337
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pemntvty = 338
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 339
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 340
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 342
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pemntvty = 343
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 351
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pemntvty = 353
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pemntvty = 375
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pemntvty = 376
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 377
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pemntvty = 378
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pemntvty = 379
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 380
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 383
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 385
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pemntvty = 387
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pemntvty = 388
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pemntvty = 389
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 415
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pemntvty = 417
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 421
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 427
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pemntvty = 436
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pemntvty = 440
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 449
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 462
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 468
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 501
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 507
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pemntvty = 514
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 527
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 555
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other"
      }
    }
  }

  dimension: penatvty_census_2004 {
    label: "Native country of sample person"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penatvty = 57
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.penatvty = 72
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.penatvty = 96
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.penatvty = 102
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.penatvty = 103
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.penatvty = 105
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 106
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.penatvty = 108
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.penatvty = 109
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.penatvty = 110
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.penatvty = 116
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.penatvty = 117
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.penatvty = 119
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.penatvty = 120
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.penatvty = 126
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.penatvty = 127
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.penatvty = 128
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.penatvty = 129
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.penatvty = 130
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.penatvty = 132
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.penatvty = 134
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.penatvty = 136
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.penatvty = 137
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.penatvty = 138
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.penatvty = 139
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.penatvty = 140
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.penatvty = 142
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 147
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.penatvty = 148
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.penatvty = 155
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 156
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 180
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.penatvty = 183
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.penatvty = 184
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.penatvty = 185
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.penatvty = 192
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.penatvty = 195
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.penatvty = 200
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 202
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.penatvty = 205
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.penatvty = 206
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.penatvty = 207
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.penatvty = 209
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.penatvty = 210
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.penatvty = 211
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.penatvty = 212
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.penatvty = 213
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.penatvty = 214
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.penatvty = 215
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.penatvty = 216
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.penatvty = 217
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 218
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 221
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.penatvty = 222
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.penatvty = 224
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.penatvty = 229
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 231
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.penatvty = 233
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.penatvty = 234
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.penatvty = 237
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.penatvty = 238
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.penatvty = 239
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.penatvty = 240
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.penatvty = 242
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.penatvty = 245
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.penatvty = 252
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.penatvty = 253
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.penatvty = 300
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 301
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.penatvty = 304
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.penatvty = 310
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.penatvty = 311
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.penatvty = 312
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.penatvty = 313
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.penatvty = 314
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.penatvty = 315
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.penatvty = 316
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.penatvty = 317
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.penatvty = 318
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.penatvty = 333
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.penatvty = 334
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.penatvty = 337
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.penatvty = 338
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.penatvty = 339
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 340
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.penatvty = 342
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.penatvty = 343
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.penatvty = 351
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.penatvty = 353
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.penatvty = 375
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.penatvty = 376
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.penatvty = 377
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.penatvty = 378
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.penatvty = 379
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.penatvty = 380
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.penatvty = 383
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.penatvty = 385
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.penatvty = 387
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 388
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.penatvty = 389
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.penatvty = 415
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.penatvty = 417
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.penatvty = 421
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.penatvty = 427
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.penatvty = 436
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.penatvty = 440
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 449
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 462
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 468
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 501
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.penatvty = 507
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.penatvty = 514
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.penatvty = 527
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.penatvty = 555
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other"
      }
    }
  }

  dimension: proldrrp {
    label: "Recode to collapse new rrp categories into old"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.proldrrp = 1
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Ref Pers W/ Rels"
      }

      when: {
        sql: ${TABLE}.proldrrp = 2
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Ref Pers W/O Rels"
      }

      when: {
        sql: ${TABLE}.proldrrp = 3
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "SPOUSE"
      }

      when: {
        sql: ${TABLE}.proldrrp = 4
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "CHILD"
      }

      when: {
        sql: ${TABLE}.proldrrp = 5
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "GRANDCHILD"
      }

      when: {
        sql: ${TABLE}.proldrrp = 6
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "PARENT"
      }

      when: {
        sql: ${TABLE}.proldrrp = 7
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "BROTHER/SISTER"
      }

      when: {
        sql: ${TABLE}.proldrrp = 8
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Other Rel Of Ref Per"
      }

      when: {
        sql: ${TABLE}.proldrrp = 9
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "FOSTER CHILD"
      }

      when: {
        sql: ${TABLE}.proldrrp = 10
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Nonrel Of Ref Per W/ Own Rels In HH"
      }

      when: {
        sql: ${TABLE}.proldrrp = 11
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "PARTNER/ROOMMATE"
      }

      when: {
        sql: ${TABLE}.proldrrp = 12
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Nonrel. of ref per W/O Own Rels In HH"
      }
    }
  }

  dimension: puafever {
    label: "Ever did military service,y/n"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.puafever = -3
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.puafever = -2
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.puafever = -1
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.puafever = 1
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.puafever = 2
          AND ${TABLE}.src_table in ('census_2004', 'census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "No"
      }
    }
  }

  dimension: ptdtrace_census_2004 {
    label: "Demographics- race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptdtrace = 1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "White Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Black Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "American Indian, Alaskan Native Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 4
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Asian Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 5
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Hawaiian/Pacific Islander Only"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 6
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "White-Black"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 7
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "White-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 8
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "White-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 9
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "White-Hawaiian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 10
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Black-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 11
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Black-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 12
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Black-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 13
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "AI-Asian"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 14
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Asian-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 15
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "W-B-AI"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 16
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "W-B-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 17
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "W-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 18
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "W-A-HP"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 19
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "W-B-AI-A"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 20
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "2 or 3 Races"
      }

      when: {
        sql: ${TABLE}.ptdtrace = 21
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "4 or 5 Races"
      }
    }
  }

  dimension: prdthsp_census_2004 {
    label: "Demographics- detailed hispanic origin subgroup"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdthsp = -1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.prdthsp = 1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Mexican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prdthsp = 3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prdthsp = 4
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Central/South American"
      }

      when: {
        sql: ${TABLE}.prdthsp = 5
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other Spanish"
      }
    }
  }

  dimension: pes7_census_2004 {
    label: "How did (you/name) register to vote?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "No answer"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Out of Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "At a department of motor vehicles (e.g., when obtaining a drivers license or other identification"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "At a public assistance agency  (e.g., a Medicaid, AFDC, or Food Stamps office, an office serving di"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Filled out form at a registration drive (library, post office, or someone came to your door)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 8
          AND ${TABLE}.src_table in ('census_2004')
           ;;
        label: "Other"
      }
    }
  }

  dimension: hufinal_census_2002 {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: huhhnum {
    label: "Replacement number"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 3, 5, 7, 9]
    style: classic
    sql: CASE WHEN ${TABLE}.huhhnum between 1 AND 8 THEN ${TABLE}.huhhnum END ;;
  }

  dimension: huprscnt_census_2002 {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt\u0007census_2002 between 1 AND 99 THEN ${TABLE}.huprscnt\u0007census_2002 END ;;
  }

  dimension: hutypc_census_2002 {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: hutypea_census_2002 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: pefntvty_census_2002 {
    label: "Native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 57
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 72
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 96
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 155
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 180
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 183
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 184
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 185
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 192
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 195
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 200
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 202
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pefntvty = 205
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pefntvty = 206
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 207
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pefntvty = 209
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pefntvty = 210
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pefntvty = 211
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 212
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pefntvty = 213
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pefntvty = 214
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pefntvty = 215
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 216
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 217
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 218
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 221
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pefntvty = 222
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 224
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 229
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 231
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 233
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 234
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pefntvty = 237
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 238
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 239
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 240
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pefntvty = 242
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 245
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 252
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pefntvty = 253
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 300
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 301
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 304
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 310
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pefntvty = 311
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 312
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 313
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pefntvty = 314
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pefntvty = 315
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 316
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pefntvty = 317
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pefntvty = 318
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 333
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pefntvty = 334
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pefntvty = 337
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pefntvty = 338
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 339
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 340
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 342
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pefntvty = 343
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 351
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pefntvty = 353
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pefntvty = 375
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pefntvty = 376
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 377
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pefntvty = 378
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pefntvty = 379
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 380
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 383
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 385
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pefntvty = 387
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pefntvty = 388
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pefntvty = 389
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 415
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pefntvty = 417
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 421
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pefntvty = 427
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pefntvty = 436
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pefntvty = 440
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 449
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 462
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 468
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pefntvty = 501
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 507
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pefntvty = 514
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 527
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 555
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pemntvty_census_2002 {
    label: "Native country of mother"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pemntvty = 57
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pemntvty = 72
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 96
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pemntvty = 102
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 103
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pemntvty = 105
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 106
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pemntvty = 108
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 109
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pemntvty = 110
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pemntvty = 116
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pemntvty = 117
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pemntvty = 119
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pemntvty = 120
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pemntvty = 126
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 127
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pemntvty = 128
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 129
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pemntvty = 130
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pemntvty = 132
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 134
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 136
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pemntvty = 137
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 138
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pemntvty = 139
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pemntvty = 140
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 142
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pemntvty = 147
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 148
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pemntvty = 155
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 156
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 180
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pemntvty = 183
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 184
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pemntvty = 185
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 192
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 195
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 200
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 202
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pemntvty = 205
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pemntvty = 206
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 207
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pemntvty = 209
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pemntvty = 210
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pemntvty = 211
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 212
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pemntvty = 213
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pemntvty = 214
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pemntvty = 215
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 216
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 217
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 218
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pemntvty = 221
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pemntvty = 222
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pemntvty = 224
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 229
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 231
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pemntvty = 233
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 234
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pemntvty = 237
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 238
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pemntvty = 239
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 240
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pemntvty = 242
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pemntvty = 245
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 252
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pemntvty = 253
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pemntvty = 300
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pemntvty = 301
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 304
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 310
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pemntvty = 311
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 312
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 313
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pemntvty = 314
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pemntvty = 315
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pemntvty = 316
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pemntvty = 317
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.pemntvty = 318
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 333
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.pemntvty = 334
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.pemntvty = 337
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.pemntvty = 338
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 339
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.pemntvty = 340
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.pemntvty = 342
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.pemntvty = 343
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.pemntvty = 351
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.pemntvty = 353
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.pemntvty = 375
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.pemntvty = 376
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 377
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.pemntvty = 378
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.pemntvty = 379
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 380
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.pemntvty = 383
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 385
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.pemntvty = 387
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.pemntvty = 388
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.pemntvty = 389
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.pemntvty = 415
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.pemntvty = 417
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 421
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.pemntvty = 427
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.pemntvty = 436
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.pemntvty = 440
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.pemntvty = 449
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 462
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 468
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.pemntvty = 501
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.pemntvty = 507
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.pemntvty = 514
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.pemntvty = 527
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.pemntvty = 555
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other"
      }
    }
  }

  dimension: penatvty_census_2002 {
    label: "Native country of sample person"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.penatvty = 57
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.penatvty = 72
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.penatvty = 96
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.penatvty = 102
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.penatvty = 103
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.penatvty = 105
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 106
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.penatvty = 108
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.penatvty = 109
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.penatvty = 110
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.penatvty = 116
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.penatvty = 117
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.penatvty = 119
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.penatvty = 120
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.penatvty = 126
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.penatvty = 127
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.penatvty = 128
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.penatvty = 129
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.penatvty = 130
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.penatvty = 132
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.penatvty = 134
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.penatvty = 136
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.penatvty = 137
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.penatvty = 138
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.penatvty = 139
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.penatvty = 140
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.penatvty = 142
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 147
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.penatvty = 148
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.penatvty = 155
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 156
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 180
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.penatvty = 183
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.penatvty = 184
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.penatvty = 185
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.penatvty = 192
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.penatvty = 195
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.penatvty = 200
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 202
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.penatvty = 205
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.penatvty = 206
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.penatvty = 207
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.penatvty = 209
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.penatvty = 210
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.penatvty = 211
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.penatvty = 212
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.penatvty = 213
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.penatvty = 214
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.penatvty = 215
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.penatvty = 216
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.penatvty = 217
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 218
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 221
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.penatvty = 222
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.penatvty = 224
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.penatvty = 229
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 231
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.penatvty = 233
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.penatvty = 234
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.penatvty = 237
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.penatvty = 238
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.penatvty = 239
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.penatvty = 240
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.penatvty = 242
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.penatvty = 245
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.penatvty = 252
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.penatvty = 253
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.penatvty = 300
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 301
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.penatvty = 304
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.penatvty = 310
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.penatvty = 311
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.penatvty = 312
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.penatvty = 313
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.penatvty = 314
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.penatvty = 315
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.penatvty = 316
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.penatvty = 317
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.penatvty = 318
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.penatvty = 333
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.penatvty = 334
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.penatvty = 337
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.penatvty = 338
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.penatvty = 339
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 340
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.penatvty = 342
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.penatvty = 343
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.penatvty = 351
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.penatvty = 353
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.penatvty = 375
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.penatvty = 376
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.penatvty = 377
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.penatvty = 378
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.penatvty = 379
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.penatvty = 380
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.penatvty = 383
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.penatvty = 385
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.penatvty = 387
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 388
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.penatvty = 389
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.penatvty = 415
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.penatvty = 417
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.penatvty = 421
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.penatvty = 427
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.penatvty = 436
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.penatvty = 440
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 449
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 462
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 468
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 501
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.penatvty = 507
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.penatvty = 514
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.penatvty = 527
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.penatvty = 555
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other"
      }
    }
  }

  dimension: perace {
    label: "Race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.perace = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "White"
      }

      when: {
        sql: ${TABLE}.perace = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Black"
      }

      when: {
        sql: ${TABLE}.perace = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "American Indian, Aleut, Eskimo"
      }

      when: {
        sql: ${TABLE}.perace = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Asian or Pacific Islander"
      }
    }
  }

  dimension: prhspnon {
    label: "Hispanic/non-hispanic origin"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prhspnon = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Hispanic"
      }

      when: {
        sql: ${TABLE}.prhspnon = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Non-Hispanic"
      }
    }
  }

  dimension: prorigin {
    label: "Hispanic origin or descent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prorigin = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Mexican American"
      }

      when: {
        sql: ${TABLE}.prorigin = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Chicano"
      }

      when: {
        sql: ${TABLE}.prorigin = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Mexican (Mexicano)"
      }

      when: {
        sql: ${TABLE}.prorigin = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Puerto Rican"
      }

      when: {
        sql: ${TABLE}.prorigin = 5
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cuban"
      }

      when: {
        sql: ${TABLE}.prorigin = 6
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Central or South American"
      }

      when: {
        sql: ${TABLE}.prorigin = 7
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Other Spanish"
      }

      when: {
        sql: ${TABLE}.prorigin = 8
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "All other"
      }

      when: {
        sql: ${TABLE}.prorigin = 9
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Don't know"
      }

      when: {
        sql: ${TABLE}.prorigin = 10
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Na"
      }
    }
  }

  dimension: purelflg {
    label: "Flag for persons related to person owning bus."
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.purelflg = -3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.purelflg = -2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.purelflg = -1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.purelflg = 0
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not owner or related to owner"
      }

      when: {
        sql: ${TABLE}.purelflg = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Owner of bus or related to owner of bus"
      }
    }
  }

  dimension: pulbhsec {
    label: "Total seconds to complete labor force items"
    view_label: "Cohort Labor Force Variables"
    type: tier
    tiers: [0, 25000, 50000, 75000, 99999]
    style: classic
    sql: CASE WHEN ${TABLE}.pulbhsec between 0 AND 99999 THEN ${TABLE}.pulbhsec END ;;
  }

  dimension: peio1icd_census_2002 {
    label: "(main job)industry code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio1icd = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peio1icd = 10
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Agricultural production, crops (01)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 11
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Agricltrl production, livestock (02)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 12
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Veterinary services (074)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 20
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Landscape and horticultural services (078)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 30
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Agricultural services, n.e.c. (071, 072, 075, 076)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 31
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Forestry (08)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 32
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fishing, hunting, and trapping (09)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 40
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Metal mining (10)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 41
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Coal mining (12)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 42
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Oil and gas extraction (13)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 50
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Nonmetallic mining and quarrying, except fuel (14)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 60
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "CONSTRUCTION (15, 16, 17)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 100
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Meat products (201)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 101
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dairy products (202)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 102
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Canned, frozen and preserved fruits and vegetables (203)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 110
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Grain mill products (204)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 111
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bakery products (205)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 112
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sugar and confectionery products (206)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 120
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Beverage industries (208)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 121
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous food preparations and kindred products (207, 209)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 122
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified food industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 130
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Tobacco manufactures (21)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 132
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Knitting mills (225)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 140
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dyeing and finishing textiles, except wool and knit goods (226)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 141
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Carpets and rugs (227)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 142
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Yarn, thread, and fabric mills (221-224, 228)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 150
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous textile mill products (229)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 151
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Apparel and accessories, except knit (231-238)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 152
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous fabricated textile products (239)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 160
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pulp, paper, and paperboard mills (261-263)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 161
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous paper and pulp products (267)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 162
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Paperboard containers and boxes (265)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 171
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Newspaper publishing and printing (271)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 172
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Printing, publishing, and allied industries, except newspapers (272-279)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 180
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Plastics, synthetics, and resins (282)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 181
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Drugs (283)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 182
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Soaps and cosmetics (284)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 190
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Paints, varnishes, and rel. products (285)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 191
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Agricultural chemicals (287)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 192
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Industrial and miscellaneous chemicals (281, 286, 289)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 200
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Petroleum refining (291)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 201
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous petroleum and coal products (295, 299)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 210
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Tires and inner tubes (301)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 211
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other rubber products, and plastics footwear and belting (302-306)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 212
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous plastics products (308)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 220
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Leather tanning and finishing (311)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 221
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Footwear, except rubber and plastic (313, 314)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 222
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Leather products, except footwear (315-317, 319)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 230
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Logging (241)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 231
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sawmills, planing mills, and millwork (242, 243)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 232
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Wood buildings and mobile homes (245)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 241
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous wood products (244, 249)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 242
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Furniture and fixtures (25)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 250
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Glass and glass products (321-323)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 251
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cement, concrete, gypsum, and plaster products (324, 327)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 252
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Structural clay products (325)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 261
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pottery and related products (326)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 262
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous nonmetallic mineral and stone products (328, 329)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 270
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Blast furnaces, steelworks, rolling and finishing mills (331)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 271
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Iron and steel foundries (332)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 272
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Primary aluminum industries (3334, part 334, 3353-3355, 3363, 3365)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 280
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other primary metal industries (3331, 3339, part 334, 3351, 3356, 3357, 3364, 3366, 3369, 339)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 281
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cutlery, handtools, and general hardware (342)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 282
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fabricated structural metal products (344)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 290
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Screw machine products (345)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 291
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Metal forgings and stampings (346)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 292
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ordnance (348)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 300
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Misc fabricated metal products (341, 343, 347, 349)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 301
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified metal industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 310
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Engines and turbines (351)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 311
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Farm machinery and equipment (352)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 312
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Construction and material handling machines (353)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 320
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Metalworking machinery (354)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 321
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Office and accounting machines (3578, 3579)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 322
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Computers and rel. equipment (3571-3577)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 331
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Machinery, except electrical, n.e.c. (355, 356, 358, 359)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 332
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified machinery"
      }

      when: {
        sql: ${TABLE}.peio1icd = 340
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Household appliances (363)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 341
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Radio, TV, and communication equipment (365, 366)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 342
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Electrical machinery, equipment, and supplies, n.e.c. (361, 362, 364, 367, 369)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 350
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified electrical machinery, equipment, and supplies"
      }

      when: {
        sql: ${TABLE}.peio1icd = 351
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Motor vehicles and motor vehicle equipment (371)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 352
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Aircraft and parts (372)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 360
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Ship and boat building and repairing (373)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 361
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Railroad locomotives and equipment (374)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 362
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Guided missiles, space vehicles, and parts (376)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 370
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Cycles and miscellaneous transportation equipment (375, 379)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 371
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Scientific and controlling instruments (381, 382 except 3827)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 372
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Medical, dental, and optical instruments and supplies (3827, 384, 385)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 380
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Photographic equipment and supplies (386)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 381
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Watches, clocks, and clockwork operated devices (387)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 390
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Toys, amusement, and sporting goods (394)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 391
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous manufacturing industries (39 except 394)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 392
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not spec manufacturing industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 400
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Railroads (40)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 401
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bus service and urban transit (41, except 412)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 402
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Taxicab service (412)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 410
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Trucking service (421, 423)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 411
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Warehousing and storage (422)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 412
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "U.S. Postal Service (43)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 420
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Water transportation (44)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 421
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Air transportation (45)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 422
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Pipe lines, except natural gas (46)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 432
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Services incidental to transportation (47)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 440
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Radio and television broadcasting and cable (483, 484)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 441
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Telephone communications (481)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 442
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Telegraph and miscellaneous communications services (482, 489)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 450
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Electric light and power (491)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 451
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Gas and steam supply systems (492, 496)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 452
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Electric and gas, and other combinations (493)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 470
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Water supply and irrigation (494, 497)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 471
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sanitary services (495)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 472
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified utilities"
      }

      when: {
        sql: ${TABLE}.peio1icd = 500
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Motor vehcls and equipment (501)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 501
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Furniture and home furnishings (502)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 502
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lumber and construction materials (503)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 510
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Professional and commercial equipment and supplies (504)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 511
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Metals and minerals, except petroleum (505)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 512
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Electrical goods (506)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 521
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hardware, plumbing and heating supplies (507)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 530
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Machinery, equipment, and supplies (508)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 531
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Scrap and waste materials (5093)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 532
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous wholesale, durable goods (509 except 5093)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 540
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Paper and paper products (511)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 541
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Drugs, chemicals and allied products (512, 516)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 542
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Apparel, fabrics, and notions (513)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 550
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Groceries and related products (514)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 551
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Farm-product raw materials (515)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 552
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Petroleum products (517)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 560
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Alcoholic beverages (518)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 561
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Farm supplies (5191)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 562
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Misc wholesale, nondurable goods (5192-5199)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 571
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified wholesale trade"
      }

      when: {
        sql: ${TABLE}.peio1icd = 580
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lumber and building material retailing (521, 523)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 581
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hardware stores (525)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 582
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, Retail nurseries and garden (526)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 590
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Mobile home dealers (527)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 591
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Department stores (531)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 592
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Variety stores (533)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 600
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, misc general merchandise (539)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 601
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Grocery stores (541)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 602
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, dairy products (545)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 610
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Retail bakeries (546)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 611
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Food stores, n.e.c. (542, 543, 544, 549)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 612
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Motor vehicle dealers (551, 552)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 620
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, Auto and home supply (553)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 621
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Gasoline service stations (554)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 622
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous vehicle dealers (555, 556, 557, 559)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 623
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, apparel and accessory, except shoe (56, except 566)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 630
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Shoe stores (566)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 631
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, furniture and home furnishings (571)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 632
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, household appliance (572)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 633
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, radio, TV, and computer (5731, 5734)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 640
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Music stores (5735, 5736)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 641
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Eating and drinking places (58)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 642
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Drug stores (591)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 650
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Liquor stores (592)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 651
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, sporting goods, bicycles, and hobby (5941, 5945, 5946)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 652
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, Book and stationery (5942, 5943)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 660
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Jewelry stores (5944)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 661
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Gift, novelty, and souvenir shops (5947)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 662
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Sewing, needlework and piece goods stores (5949)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 663
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Catalog and mail order houses (5961)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 670
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Vending machine operators (5962)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 671
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Direct selling establishments (5963)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 672
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Fuel dealers (598)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 681
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Retail florists (5992)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 682
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Stores, Miscellaneous retail (593, 5948, 5993-5995, 5999)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 691
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not specified retail trade"
      }

      when: {
        sql: ${TABLE}.peio1icd = 700
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Banking (60 except 603 and 606)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 701
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Savings institutions, including credit unions (603, 606)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 702
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Credit agencies, n.e.c. (61)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 710
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Security, commodity brokerage, and investment companies (62, 67)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 711
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Insurance (63, 64)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 712
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Real estate, including real estate-insurance offices (65)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 721
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Advertising (731)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 722
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Services to dwellings and other buildings (734)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 731
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Personnel supply services (736)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 732
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Computer and data processing services (737)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 740
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Detective and protective services (7381, 7382)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 741
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Business services, n.e.c. (732, 733, 735, 7383-7389)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 742
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Automotive rental and leasing, w/out drivers (751)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 750
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Automotive parking and carwashes (752, 7542)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 751
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Automotive repair and rel. services (753, 7549)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 752
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Electrical repair shops (762, 7694)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 760
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Misc repair services (763, 764, 7692, 7699)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 761
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "PRIVATE HOUSEHOLDS (88)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 762
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Hotels and motels (701)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 770
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Lodging places, except hotels and motels (702, 703, 704)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 771
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Laundry, cleaning, and garment services (721 except part 7219)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 772
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Beauty shops (723)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 780
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Barber shops (724)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 781
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Funeral service and crematories (726)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 782
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Shoe repair shops (725)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 790
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dressmaking shops (part 7219)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 791
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Misc personal services (722, 729)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 800
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Theaters and motion pictures (781-783, 792)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 801
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Video tape rental (784)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 802
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bowling centers (793)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 810
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Miscellaneous entertainment and recreation services (791, 794, 799)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 812
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Physicians offices and clinics (801, 803)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 820
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Dentists offices and clinics (802)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 821
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Chiropractors offices and clinics (8041)"
      }

      when: {
        sql: ${TABLE}.peio1icd =

      }

      when: {
        sql: ${TABLE}.gtcmsa = 14
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Chicago-Gary-Kenosha, IL-IN-WI"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 21
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cincinnati-Hamilton, OH-KY-IN"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 28
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cleveland-Akron, OH"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 31
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Dallas-Fort Worth, TX"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 34
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Denver-Boulder-Greeley, CO"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 35
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Detroit-Ann Arbor-Flint, MI"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 42
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Houston-Galveston-Brazoria, TX"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 49
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Los Angeles-Riverside-Orange County, CA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 56
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Miami-Fort Lauderdale, FL"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 63
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Milwaukee-Racine, WI"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 70
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "New York-Northern New Jersey-Long Island, NY-NJ-CT-PA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 77
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Philadelphia-Wilmington-Atlantic City, PA-NJ-DE-MD"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 79
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Portland-Salem, OR-WA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 82
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Sacramento-Yolo, CA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 84
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Francisco-Oakland-San Jose, CA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 91
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Seattle-Tacoma-Bremerton, WA"
      }

      when: {
        sql: ${TABLE}.gtcmsa = 97
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Washington-Baltimore, DC-MD-VA-WV"
      }
    }
  }

  dimension: gecmsasz {
    label: "Cmsa/msa size"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gecmsasz = 0
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "Not Identified or NonMetropolitan"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "100,000 - 249,999"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "250,000 - 499,999"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "500,000 - 999,999"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 5
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "1,000,000 - 2,499,999"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 6
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "2,500,000 - 4,999,999"
      }

      when: {
        sql: ${TABLE}.gecmsasz = 7
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996')
           ;;
        label: "5,000,000+"
      }
    }
  }

  dimension: geindvcc {
    label: "Individual central city code"
    view_label: "Cohort Geography Variables"
    type: tier
    tiers: [1, 2, 3, 4, 5]
    style: classic
    sql: CASE WHEN ${TABLE}.geindvcc between 1 AND 4 THEN ${TABLE}.geindvcc END ;;
  }

  dimension: gemetsta {
    label: "Metropolitan status"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gemetsta = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Metropolitan"
      }

      when: {
        sql: ${TABLE}.gemetsta = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Nonmetropolitan"
      }

      when: {
        sql: ${TABLE}.gemetsta = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not Identified"
      }
    }
  }

  dimension: gtmsast {
    label: "Msa/central city status"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtmsast = 1
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Central City"
      }

      when: {
        sql: ${TABLE}.gtmsast = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Balance On Msa"
      }

      when: {
        sql: ${TABLE}.gtmsast = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Non-Metropolitan"
      }

      when: {
        sql: ${TABLE}.gtmsast = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not Identified"
      }
    }
  }

  dimension: gtmsasz {
    label: "Msa/pmsa size"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtmsasz = 0
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not Identified or NonMetropolitan"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 2
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "100,000 - 249,999"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 3
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "250,000 - 499,999"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 4
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "500,000 - 999,999"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 5
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "1,000,000 - 2,499,999"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 6
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "2,500,000 - 4,999,999"
      }

      when: {
        sql: ${TABLE}.gtmsasz = 7
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "5,000,000+"
      }
    }
  }

  dimension: gtmsa {
    label: "Msa code"
    view_label: "Cohort Geography Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.gtmsa = 0
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Not MSA/PMSA or Not Identifiable"
      }

      when: {
        sql: ${TABLE}.gtmsa = 80
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Akron, OH, PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Albany-Schenectady-Troy, NY  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 200
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Albuquerque, NM MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Allentown-Bethlehem-Easton, PA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 380
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Anchorage, AK  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 440
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Ann Arbor, MI  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 450
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Anniston, AL MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 460
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Appleton-Oshkosh-Neenah, WI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 480
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Asheville, NC  MSA  (Madison County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 520
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Atlanta, GA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Atlantic-Cape May, NJ  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Augusta-Aiken, GA-SC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 640
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Austin-San Marcos, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Bakersfield, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 720
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Baltimore, MD PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Baton Rouge, LA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Beaumont-Port Arthur, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 860
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Bellingham, WA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 870
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Benton Harbor, MI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 875
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Bergen-Passaic, NJ  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 960
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Binghamton, NY  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1000
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Birmingham, AL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1080
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Boise City, ID  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1120
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Boston, MA-NH  PMSA  (New Hampshire portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1125
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Boulder-Longmont, CO  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1145
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Brazoria, TX  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Bridgeport, CT  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1200
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Brockton, MA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Brownsville-Harlingen-San Benito, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1280
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Buffalo-Niagara Falls, NY  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1305
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Burlington, VT  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1320
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Canton-Massillon, OH  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1360
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cedar Rapids, IA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1440
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Charleston-North Charleston, SC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1480
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Charleston, WV  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1520
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Charlotte-Gastonia-Rock Hill, NC-SC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Chattanooga, TN-A  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Chicago, IL  PMSA (DeKalb County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1620
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Chico-Paradise, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1640
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cincinnati, OH-KY-IN  PMSA  (Dearborn County, IN not identified; Ohio County, IN not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1660
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Clarksville-Hopkinsville, TN-KY  MSA  (Kentucky portion not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Cleveland-Lorain-Elyria, OH  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1720
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Colorado Springs, CO  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Columbia, SC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1800
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Columbus, GA-AL  MSA  (Alabama portion not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Columbus, OH  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1880
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Corpus Christi, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1920
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Dallas, TX  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1930
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Danbury, CT  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 1960
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Davenport-Moline-Rock Island, IA-IL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2000
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Dayton-Springfield, OH  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2020
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Daytona Beach, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2030
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Decatur, AL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2040
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Decatur, IL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2080
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Denver, CO  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2120
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Des Moines, IA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Detroit, MI  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2190
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Dover, DE  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Duluth-Superior, MN-WI  MSA  (Wisconsin portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2281
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Dutchess County, NY  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2290
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Eau Claire, WI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2320
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "El Paso, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2360
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Erie, PA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2400
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Eugene-Springfield, OR  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2440
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Evansville-Henderson, IN-KY  MSA  (Kentucky portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2520
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fargo-Moorhead, ND-MN  MSA  (Minnesota portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fayetteville, NC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2580
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fayetteville-Springdale-Rogers, AR  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fitchburg-Leominster, MA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2640
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Flint, MI  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2650
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Florence, AL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2670
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Collins-Loveland, CO  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Lauderdale, FL  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2700
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Myers-Cape Coral,  FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2710
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Pierce-Port St. Lucie, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2720
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Smith, AR-OK  MSA  (Oklahoma portion not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2750
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Walton Beach, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Wayne, IN  MSA  (Adams, Huntington, and Wells Counties not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2800
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fort Worth-Arlington, TX  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Fresno, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2900
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Gainesville, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2920
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Galveston-Texas City, TX  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 2960
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Gary, IN  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3000
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Grand Rapids-Muskegon-Holland, MI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3060
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Greeley, CO  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3080
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Green Bay, WI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3120
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Greenboro-Winston Salem-High Point, NC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3150
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Greenville, NC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 3160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "
ion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6480
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Providence-Fall River-Warwick, RI-MA  MSA  (Newport County, RI portion suppressed)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6520
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Provo-Orem, UT  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Pueblo, CO  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6580
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Punta Gorda, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Racine, WI  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6640
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Raleigh-Durham-Chapel Hill, NC  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Reading, PA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6720
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Reno, NV  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Richmond-Petersburg, VA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6780
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Riverside-San Bernardino, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6800
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Roanoke, VA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Rochester, NY  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6880
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Rockford, IL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6920
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Sacramento, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 6960
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Saginaw-Bay City-Midland, MI  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7040
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "St. Louis, MO-IL  MSA  (Crawford County, MO [part] not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7080
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Salem, OR  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7120
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Salinas, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Salt Lake City-Ogden, UT  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Antonio, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7320
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Diego, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7360
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Francisco, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7400
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Jose, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7460
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "San Luis Obisco-Atascaders-Paso Robles, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7480
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Santa Barbara-Santa Maria-Lompoc, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7490
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Santa Fe, NM  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7500
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Santa Rosa, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7510
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Sarasota-Bradenton, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Scranton-Wilkes Barre-Hazelton, PA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Seattle-Bellevue-Everett, WA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Shreveport-Bossier City, LA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Sioux Falls, SD  MSA  (Central City portion only identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7800
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "South Bend, IN  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Spokane, WA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7880
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Springfield, IL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 7920
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Springfield, MO  MSA  (Webster County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8000
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Springfield, MA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8040
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Stamford-Norwalk, CT  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8120
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Stockton-Lodi, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Syracuse, NY  MSA  (Cayuga County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8200
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tacoma, WA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tallahassee, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8280
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tampa-St, Petersburg-Clearwater, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8400
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Toledo, OH  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8440
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Topeka, KS  MSA  (Central City portion only identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8480
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Trenton, NJ  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8520
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tucson, AZ  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8560
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tulsa, OK  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8600
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Tuscaloosa, AL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8680
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Utica-Rome, NY  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8720
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Vallejo-Fairfield-Napa, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8735
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Ventura, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8760
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Vineland-Millville-Bridgeton, NJ  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8780
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Visalia-Tulare-Porterville, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8800
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Waco, TX  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8840
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Washington, DC-MD-VA-WV  MSA  (West Virginia portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8880
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Waterbury, CT  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8920
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Waterloo-Cedar Falls, IA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 8960
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "West Palm Beach-Boca Raton, FL  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9000
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Wheeling, WV-OH  MSA  (Ohio portion not identified)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9040
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Wichita, KS  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9160
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Wilmington-Newark, DE-MD  PMSA  (Maryland portion suppressed)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9200
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Wilmington, NC  MSA  (Brunswick County not in sample)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9240
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Worcester, MA-CT  PMSA  (Connecticut portion suppressed)"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9270
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Yolo, CA  PMSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9280
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "York, PA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9320
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Youngstown-Warren, OH  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9340
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Yuba City, CA  MSA"
      }

      when: {
        sql: ${TABLE}.gtmsa = 9360
          AND ${TABLE}.src_table in ('census_2002', 'census_2000', 'census_1998', 'census_1996', 'census_1994')
           ;;
        label: "Yuma, AZ  MSA"
      }
    }
  }

  dimension: pes4_census_2002 {
    label: "Vote in person on election day or before, or by mail"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes4 = -9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes4 = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes4 = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes4 = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes4 = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "In person on election day"
      }

      when: {
        sql: ${TABLE}.pes4 = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "In person before election day"
      }

      when: {
        sql: ${TABLE}.pes4 = 3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Voted by mail (absentee)"
      }
    }
  }

  dimension: pes5_census_2002 {
    label: "Did you register to vote after january 1, 1995"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes5 = -9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes5 = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes5 = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes5 = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes5 = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes5 = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No"
      }
    }
  }

  dimension: pes6_census_2002 {
    label: "Register when obtained or renewed license or other way"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes6 = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes6 = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes6 = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes6 = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "When driver's license was obtained/renewed"
      }

      when: {
        sql: ${TABLE}.pes6 = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Some other way"
      }
    }
  }

  dimension: pes7_census_2002 {
    label: "How did you register to vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "At a public assistance agency"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Filled out form at a registration drive"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pes3_census_2002 {
    label: "Main reason did not vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes3 = -9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes3 = -3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes3 = -2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes3 = -1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes3 = 1
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Illness or disability (own or family's)"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Out of town or away from home"
      }

      when: {
        sql: ${TABLE}.pes3 = 3
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Forgot to vote (or send in absentee ballot)"
      }

      when: {
        sql: ${TABLE}.pes3 = 4
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Not interested, felt my vote wouldn't make a difference"
      }

      when: {
        sql: ${TABLE}.pes3 = 5
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Too busy, conflicting work or school schedule"
      }

      when: {
        sql: ${TABLE}.pes3 = 6
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Transportation problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 7
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Didn't like candidates or campaign issues"
      }

      when: {
        sql: ${TABLE}.pes3 = 8
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Registration problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 9
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Bad weather conditions"
      }

      when: {
        sql: ${TABLE}.pes3 = 10
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Inconvenient polling place or hours or lines too long"
      }

      when: {
        sql: ${TABLE}.pes3 = 11
          AND ${TABLE}.src_table in ('census_2002')
           ;;
        label: "Other"
      }
    }
  }

  dimension: hufinal_census_2000 {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: huprscnt_census_2000 {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt\u0007census_2000 between 1 AND 99 THEN ${TABLE}.huprscnt\u0007census_2000 END ;;
  }

  dimension: hutypc_census_2000 {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension

      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pes3_census_2000 {
    label: "Main reason did not vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes3 = -9
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes3 = -3
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes3 = -2
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes3 = -1
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes3 = 1
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Illness or disability (own or family's)"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Out of town or away from home"
      }

      when: {
        sql: ${TABLE}.pes3 = 3
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Forgot to vote (or send in absentee ballot)"
      }

      when: {
        sql: ${TABLE}.pes3 = 4
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Not interested, felt my vote wouldn't make a difference"
      }

      when: {
        sql: ${TABLE}.pes3 = 5
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Too busy, conflicting work or school schedule"
      }

      when: {
        sql: ${TABLE}.pes3 = 6
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Transportation problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 7
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Didn't like candidates or campaign issues"
      }

      when: {
        sql: ${TABLE}.pes3 = 8
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Registration problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 9
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Bad weather conditions"
      }

      when: {
        sql: ${TABLE}.pes3 = 10
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Inconvenient polling place or hours or lines too long"
      }

      when: {
        sql: ${TABLE}.pes3 = 11
          AND ${TABLE}.src_table in ('census_2000')
           ;;
        label: "Other"
      }
    }
  }

  dimension: hufinal_census_1998 {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: hulensec {
    label: "Total time(seconds) to complete interview"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [0, 25000, 50000, 75000, 99999]
    style: classic
    sql: CASE WHEN ${TABLE}.hulensec between 0 AND 99999 THEN ${TABLE}.hulensec END ;;
  }

  dimension: huprscnt_census_1998 {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt\u0007census_1998 between 1 AND 99 THEN ${TABLE}.huprscnt\u0007census_1998 END ;;
  }

  dimension: hutypc_census_1998 {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension
= 1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Too busy, conflicting work or school schedule"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Not interested, felt my vote wouldn't make a difference"
      }

      when: {
        sql: ${TABLE}.pes3 = 3
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Illness or disability (own or family's)"
      }

      when: {
        sql: ${TABLE}.pes3 = 4
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Didn't like candidates or campaign issues"
      }

      when: {
        sql: ${TABLE}.pes3 = 5
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Out of town or away from home"
      }

      when: {
        sql: ${TABLE}.pes3 = 6
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Forgot to vote (or send in absentee ballot)"
      }

      when: {
        sql: ${TABLE}.pes3 = 7
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Transportation problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 8
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Inconvenient polling place or hours or lines too long"
      }

      when: {
        sql: ${TABLE}.pes3 = 9
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Registration problems"
      }

      when: {
        sql: ${TABLE}.pes3 = 10
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Bad weather conditions"
      }

      when: {
        sql: ${TABLE}.pes3 = 11
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other"
      }
    }
  }

  dimension: pes7_census_1998 {
    label: "How did you register to vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "At a public assistance agency"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Registered by mail"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Went to a town hall or county/government registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Filled out form at a registration drive"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Registered at polling place (on election or primary day)"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_1998')
           ;;
        label: "Other"
      }
    }
  }

  dimension: hryear {
    label: "2 digit year of interview"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [0, 25, 50, 75, 99]
    style: classic
    sql: CASE WHEN ${TABLE}.hryear between 0 AND 99 THEN ${TABLE}.hryear END ;;
  }

  dimension: hufinal_census_1996 {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: huprscnt_census_1996 {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt\u0007census_1996 between 1 AND 99 THEN ${TABLE}.huprscnt\u0007census_1996 END ;;
  }

  dimension: hutypc_census_1996 {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: hutypea_census_1996 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: pefntvty_census_1996 {
    label: "Native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 57
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 72
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 96
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 155
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 180
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 183
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 184
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 185
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 192
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty =
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Miscellaneous printing machine operators (6849, 7449, 7649"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 738
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Winding and twisting machine operators (7451, 7651)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 739
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Knitting, looping, taping, and weaving machine operators (7452, 7652)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 743
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Textile cutting machine operators (7654)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 744
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Textile sewing machine operators (7655)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 745
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Shoe machine operators (7656)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 747
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Pressing machine operators (7657)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 748
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Laundering and dry cleaning machine operators (6855, 7658)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 749
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Miscellaneous textile machine operators (7459, 7659)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 753
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Cementing and gluing machine operators (7661)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 754
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Packaging and filling machine operators (7462, 7662)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 755
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Extruding and forming machine operators (7463, 7663)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 756
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mixing and blending machine operators (7664)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 757
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Separating, filtering, and clarifying machine operators (7476, 7666, 7676)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 758
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Compressing and compacting machine operators (7467, 7667)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 759
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Painting and paint spraying machine operators (7669)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 763
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Roasting and baking machine operators, food (7472, 7672)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 764
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Washing, cleaning, and pickling machine operators (7673)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 765
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Folding machine operators (7474, 7674)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 766
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Furnace, kiln, and oven operators, except food (7675)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 768
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Crushing and grinding machine operators (part 7477, part 7677)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 769
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Slicing and cutting machine operators (7478, 7678)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 773
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Motion picture projectionists (part 7479)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 774
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Photographic process machine operators (6863, 6868, 7671)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 777
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Miscellaneous machine operators, n.e.c. (part 7479, 7665, 7679)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 779
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine operators, not specified"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 783
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Welders and cutters (7332, 7532, 7714)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 784
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Solderers and brazers (7333, 7533, 7717)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 785
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Assemblers (772, 774)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 786
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hand cutting and trimming occupations (7753)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 787
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hand molding, casting, and forming occupations (7754, 7755)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 789
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hand painting, coating, and decorating occupations (7756)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 793
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hand engraving and printing occupations (7757)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 795
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Miscellaneous hand working occupations (7758, 7759)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 796
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Production inspectors, checkers, and examiners (782, 787)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 797
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Production testers (783)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 798
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Production samplers and weighers (784)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 799
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Graders and sorters, except agricultural (785)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 803
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors, motor vehicle operators (8111)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 804
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Truck drivers (8212-8214)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 806
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Driver-sales workers (8218)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 808
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Bus drivers (8215)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 809
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Taxicab drivers and chauffeurs (8216)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 813
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Parking lot attendants (874)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 814
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Motor transportation occupations, n.e.c. (8219)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 823
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Railroad conductors and yardmasters (8113)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 824
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Locomotive operating occupations (8232)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 825
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Railroad brake, signal, and switch operators (8233)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 826
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Rail vehicle operators, n.e.c. (8239)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 828
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Ship captains and mates, except fishing boats (part 8241, 8242)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 829
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sailors and deckhands (8243)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 833
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Marine engineers (8244)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 834
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Bridge, lock, and lighthouse tenders (8245)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 843
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors, material moving equipment operators (812)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 844
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Operating engineers (8312)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 845
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Longshore equipment operators (8313)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 848
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hoist and winch operators (8314)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 849
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Crane and tower operators (8315)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 853
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Excavating and loading machine operators (8316)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 855
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Grader, dozer, and scraper operators (8317)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 856
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Industrial truck and tractor equipment operators (8318)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 859
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Misc material moving equipment operators (8319)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 864
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors, handlers, equipment cleaners, and laborers, n.e.c. (85)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 865
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Helpers, mechanics, and repairers (863)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 866
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Helpers, construction trades (8641-8645, 8648)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 867
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Helpers, surveyor (8646)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 868
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Helpers, extractive occupations (865)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 869
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction laborers (871)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 874
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Production helpers (861, 862)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 875
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Garbage collectors (8722)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 876
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Stevedores (8723)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 877
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Stock handlers and baggers (8724)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 878
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine feeders and offbearers (8725)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 883
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Freight, stock, and material handlers, n.e.c. (8726)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 885
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Garage and service station rel. occupations (873)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 887
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Vehicle washers and equipment cleaners (875)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 888
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hand packers and packagers (8761)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 889
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Laborers, except construction (8769)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 905
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Assigned to persons whose current labor force status is unemployed and last job was Armed Forces"
      }
    }
  }

  dimension: prdtind1_census_1996 {
    label: "(main job)detailed industry"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtind1 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Goods Producing-Agricultural Services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Goods producing-Other Agricultural"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Lumber & Wood Prods, Ex Furniture"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Furniture & Fixtures"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Stone, Clay, Concrete, Glass Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Primary Metals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Fabricated Metals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Not Specified Metal Industries"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Machinery, Ex Electrical"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Electrical Machinery,equip Supplies"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Motor Vehicles & Equip"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Aircraft & Parts"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Other Transportation Equipment"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Professional & Photo Equip, Watches"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Toys,amusement & Sporting Goods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Misc & Nec Mfg Industries"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Food & Kindred Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Tobacco Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Textile Mill Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Apparel & Other Finished Textile Pr"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Paper & Allied Products"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 24
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Printing, Publishing & Allied Inds"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 25
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Chemicals & Allied Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 26
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Petroleum & Coal Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 27
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Rubber & Misc Plastic Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 28
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Leather & Leather Prods"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 29
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 30
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 31
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Utilities & sanitary services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 32
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 33
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Eating and drinking places"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 34
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other retail trade"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 35
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Banking and other finance"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 36
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Insurance and real estate"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 37
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private household services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 38
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Business services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 39
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Automobile and repair services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 40
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal serv exc private households"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 41
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Entertainment & recreation services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 42
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 43
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health services, exc. hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 44
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 45
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Social services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 46
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other professional services"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 47
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry & fisheries"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 48
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Justice, public order & safety"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 49
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Admin of human resource programs"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 50
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "National security & internal affairs"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 51
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other public administration"
      }

      when: {
        sql: ${TABLE}.prdtind1 = 52
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtind2_census_1996 {
    label: "(second job)detailed industry-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtind2 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Goods Producing-Agricultural Services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Goods producing-Other Agricultural"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Lumber & Wood Prods, Ex Furniture"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Furniture & Fixtures"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Stone, Clay, Concrete, Glass Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Primary Metals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Fabricated Metals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Not Specified Metal Industries"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Machinery, Ex Electrical"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Electrical Machinery,equip Supplies"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Motor Vehicles & Equip"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Aircraft & Parts"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Other Transportation Equipment"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Professional & Photo Equip, Watches"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Toys,amusement & Sporting Goods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Misc & Nec Mfg Industries"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Food & Kindred Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Tobacco Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Textile Mill Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Apparel & Other Finished Textile Pr"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Paper & Allied Products"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 24
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Printing, Publishing & Allied Inds"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 25
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Chemicals & Allied Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 26
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Petroleum & Coal Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 27
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Rubber & Misc Plastic Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 28
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg-Leather & Leather Prods"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 29
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 30
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 31
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Utilities & sanitary services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 32
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Wholesale trade"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 33
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Eating and drinking places"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 34
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other retail trade"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 35
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Banking and other finance"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 36
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Insurance and real estate"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 37
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private household services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 38
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Business services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 39
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Automobile and repair services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 40
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal serv exc private households"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 41
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Entertainment & recreation services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 42
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 43
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health services, exc. hospitals"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 44
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Educational services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 45
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Social services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 46
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other professional services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 47
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry & fisheries"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 48
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Justice, public order & safety"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 49
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Admin of human resource programs"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 50
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "National security & internal affairs"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 51
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other public administration"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 52
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtocc1_census_1996 {
    label: "(main job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc1 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Officials & administrators, pub. admin."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other executive, admin. & managerial"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Management related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Engineers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mathematical and computer scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Natural Scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health diagnosing occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health assessment and treatment occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Teachers, college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Teachers, except college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Lawyers and judges"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other professional specialty occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health technologists and technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Engineering and science technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Technicians, exc. health,engin.&science"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors and proprietors, sales occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales reps, finance and business serv."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales reps, commodities, exc. retail"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales workers, retail & personal serv."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales related occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors, admin. support"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Computer equipment operators"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Secretaries, stenographers, and typists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 24
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Financial records processing"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 25
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mail and message distributing"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 26
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other admin support, inc. clerical"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 27
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private household service occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 28
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Protective service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 29
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Food service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 30
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 31
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Cleaning and building service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 32
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 33
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mechanics and repairers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 34
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction trades"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 35
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other precision prod., craft, & repair"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 36
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine opertrs and tenders,exc precis."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 37
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Fabricatrs,assemblrs,inspectrs,samplrs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 38
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Motor vehicle operators"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 39
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other transp. & material moving occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 40
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction laborers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 41
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Freight, stock & materials handlers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 42
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Oth handlrs,equip.cleanrs,helprs,labrrs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 43
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farm operators and managers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 44
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farm workers and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 45
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry and fishing occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 46
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed forces"
      }
    }
  }

  dimension: prdtocc2_census_1996 {
    label: "(second job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc2 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Officials & administrators, pub. admin."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other executive, admin. & managerial"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Management related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Engineers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mathematical and computer scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Natural Scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health diagnosing occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health assessment and treatment occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Teachers, college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Teachers, except college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Lawyers and judges"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other professional specialty occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health technologists and technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Engineering and science technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Technicians, exc. health,engin.&science"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors and proprietors, sales occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales reps, finance and business serv."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales reps, commodities, exc. retail"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales workers, retail & personal serv."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales related occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Supervisors, admin. support"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Computer equipment operators"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Secretaries, stenographers, and typists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 24
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Financial records processing"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 25
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mail and message distributing"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 26
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other admin support, inc. clerical"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 27
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private household service occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 28
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Protective service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 29
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Food service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 30
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Health service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 31
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Cleaning and building service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 32
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 33
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mechanics and repairers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 34
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction trades"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 35
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other precision prod., craft, & repair"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 36
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine opertrs and tenders,exc precis."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 37
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Fabricatrs,assemblrs,inspectrs,samplrs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 38
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Motor vehicle operators"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 39
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other transp. & material moving occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 40
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction laborers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 41
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Freight, stock & materials handlers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 42
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Oth handlrs,equip.cleanrs,helprs,labrrs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 43
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farm operators and managers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 44
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farm workers and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 45
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry and fishing occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 46
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed forces"
      }
    }
  }

  dimension: prmjind1_census_1996 {
    label: "(main job)industry,major groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind1 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Agriculture"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Manufacturing - Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg. - Non-Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Utilities And Sanitary Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Finance, Insurance, And Real Estate"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private Households"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Business, Auto And Repair Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal Services, Exc. Private Hhlds"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Entertainment And Recreation Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Medical Services, Exc. Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Educational Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Social Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Professional Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry And Fisheries"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Public Administration"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjind2_census_1996 {
    label: "(second job)industry,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind2 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Agriculture"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Manufacturing - Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mfg. - Non-Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Utilities And Sanitary Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Finance, Insurance, And Real Estate"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private Households"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Business, Auto And Repair Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Personal Services, Exc. Private Hhlds"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 15
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Entertainment And Recreation Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 16
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 17
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Medical Services, Exc. Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 18
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Educational Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 19
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Social Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 20
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other Professional Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 21
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forestry And Fisheries"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 22
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Public Administration"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 23
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc1_census_1996 {
    label: "(main job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc1 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Executive, Admin, & Managerial Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Professional Specialty Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Technicians And Related Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Admin. Support Occs, Incl. Clerical"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private Household Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Protective Service Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Service Occs, Exc. Protective & Hhld"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Precision Prod., Craft & Repair Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine Opers, Assemblers & Inspectors"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation And Material Moving Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Handlers,equip Cleaners,helpers,laborrs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farming, Forestry And Fishing Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc2_census_1996 {
    label: "(second job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc2 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Executive, Admin, & Managerial Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Professional Specialty Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Technicians And Related Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sales Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Admin. Support Occs, Incl. Clerical"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Private Household Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Protective Service Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Service Occs, Exc. Protective & Hhld"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Precision Prod., Craft & Repair Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 10
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Machine Opers, Assemblers & Inspectors"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 11
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Transportation And Material Moving Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 12
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Handlers,equip Cleaners,helpers,laborrs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 13
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farming, Forestry And Fishing Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 14
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocgr_census_1996 {
    label: "(main job)occupation,4 groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocgr = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mgr. & Prof, Tech, Sales & Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Service Occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Production, Craft, Repair, Operators"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Farming, Forestry & Fishing Occupations"
      }
    }
  }

  dimension: pes3_census_1996 {
    label: "Main reason did not vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes3 = -9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes3 = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes3 = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes3 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes3 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Had no way to get to polls"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Could not take time off from work/school/too busy"
      }

      when: {
        sql: ${TABLE}.pes3 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Out of town or away from home"
      }

      when: {
        sql: ${TABLE}.pes3 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Sick, disabled, or family emergency"
      }

      when: {
        sql: ${TABLE}.pes3 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Did not prefer any of the candidates"
      }

      when: {
        sql: ${TABLE}.pes3 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not interested, don't care, etc."
      }

      when: {
        sql: ${TABLE}.pes3 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Forgot to vote"
      }

      when: {
        sql: ${TABLE}.pes3 = 8
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other reasons"
      }

      when: {
        sql: ${TABLE}.pes3 = 9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Lines too long at polls"
      }
    }
  }

  dimension: pes4_census_1996 {
    label: "Vote in person on election day or before, or by mail"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes4 = -9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes4 = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes4 = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes4 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes4 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In person on election day"
      }

      when: {
        sql: ${TABLE}.pes4 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "In person before election day"
      }

      when: {
        sql: ${TABLE}.pes4 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Voted by mail (absentee)"
      }
    }
  }

  dimension: pes5_census_1996 {
    label: "Did you register to vote after january 1, 1995"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes5 = -9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes5 = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes5 = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes5 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes5 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes5 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No"
      }
    }
  }

  dimension: pes6_census_1996 {
    label: "Register when obtained or renewed license or other way"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes6 = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes6 = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes6 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes6 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "When driver's license was obtained/renewed"
      }

      when: {
        sql: ${TABLE}.pes6 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Some other way"
      }
    }
  }

  dimension: pes7_census_1996 {
    label: "How did you register to vote"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "No response"
      }

      when: {
        sql: ${TABLE}.pes7 = -3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes7 = -2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes7 = -1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "At a public assistance agency"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Mailed in form to election office"
      }

      when: {
        sql: ${TABLE}.pes7 = 3
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "At a school, hospital, or on campus"
      }

      when: {
        sql: ${TABLE}.pes7 = 4
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Went to a county or government voter registration office"
      }

      when: {
        sql: ${TABLE}.pes7 = 5
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Filled out form at a registration drive"
      }

      when: {
        sql: ${TABLE}.pes7 = 6
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Other place/way"
      }

      when: {
        sql: ${TABLE}.pes7 = 7
          AND ${TABLE}.src_table in ('census_1996')
           ;;
        label: "Registered at the polls on election day"
      }
    }
  }

  dimension: hufinal_census_1994 {
    label: "Final code,status of interview"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hufinal = 0
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "New Interview - In Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fully Complete Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Partial Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Partial, Not Complete At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cati - Lf Complete; Sup Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Deceased"
      }

      when: {
        sql: ${TABLE}.hufinal = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Moved Since Last Contact"
      }

      when: {
        sql: ${TABLE}.hufinal = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Absent, Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 44
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unconvertible Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unpublished \# Or Not Listing For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Only \# Incorrect/Problem Dialing \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 24
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Entirely Occupied By Af"
      }

      when: {
        sql: ${TABLE}.hufinal = 25
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh With All Member Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 26
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Vacant (recycled To Field)"
      }

      when: {
        sql: ${TABLE}.hufinal = 29
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Non-Interview"
      }

      when: {
        sql: ${TABLE}.hufinal = 30
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hostile Break\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 31
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refusal\":\"no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 32
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hostile Break\":\"request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 33
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refusal\":\" Request Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 40
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Callback Needed\":\"int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 41
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Callbk needed;no Int Progress"
      }

      when: {
        sql: ${TABLE}.hufinal = 42
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 43
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Ill, Hospitalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 47
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Language Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 49
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Answering Service Machine For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 50
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "New \# From Da Or Recording"
      }

      when: {
        sql: ${TABLE}.hufinal = 53
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Da Unable To Confirm Hh \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 54
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Da Confirmed Hh\#"
      }

      when: {
        sql: ${TABLE}.hufinal = 55
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "\# Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 56
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "\# Changed.  No New \# Given"
      }

      when: {
        sql: ${TABLE}.hufinal = 57
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hh Unknown At Correctly Dialed \#"
      }

      when: {
        sql: ${TABLE}.hufinal = 59
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Can't Reach Da"
      }

      when: {
        sql: ${TABLE}.hufinal = 80
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ring No Anwser"
      }

      when: {
        sql: ${TABLE}.hufinal = 81
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Normal Busy Or Circuits Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 82
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fast Or Wats/Fts Busy"
      }

      when: {
        sql: ${TABLE}.hufinal = 83
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "\# Could Not Be Completed As Dialed"
      }

      when: {
        sql: ${TABLE}.hufinal = 84
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No Signal, Funny Signal"
      }

      when: {
        sql: ${TABLE}.hufinal = 85
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bad Connection"
      }

      when: {
        sql: ${TABLE}.hufinal = 86
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "\# Temporarily Not In Service"
      }

      when: {
        sql: ${TABLE}.hufinal = 87
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Wrong \# Dialed Or Reached"
      }

      when: {
        sql: ${TABLE}.hufinal = 88
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Possible Wrong \# Hh Wouldn't Confirm"
      }

      when: {
        sql: ${TABLE}.hufinal = 89
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Answering Machine - Unknown Status"
      }

      when: {
        sql: ${TABLE}.hufinal = 104
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Invalid Input"
      }

      when: {
        sql: ${TABLE}.hufinal = 105
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personal Int. Comp. For Hh"
      }

      when: {
        sql: ${TABLE}.hufinal = 106
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Case Deleted - Congressional"
      }

      when: {
        sql: ${TABLE}.hufinal = 108
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sample Reduction"
      }

      when: {
        sql: ${TABLE}.hufinal = 109
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hostile Breakoff"
      }

      when: {
        sql: ${TABLE}.hufinal = 110
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hostile Brkoff Req. PV"
      }

      when: {
        sql: ${TABLE}.hufinal = 111
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 112
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refusal Req. Pv"
      }

      when: {
        sql: ${TABLE}.hufinal = 115
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Partial With Callback Planned"
      }

      when: {
        sql: ${TABLE}.hufinal = 116
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pre-Refusal"
      }

      when: {
        sql: ${TABLE}.hufinal = 118
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Uncompleted Callback"
      }

      when: {
        sql: ${TABLE}.hufinal = 119
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Temp. Unavailable Absent, Ill"
      }

      when: {
        sql: ${TABLE}.hufinal = 120
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Temp. Institutionalized"
      }

      when: {
        sql: ${TABLE}.hufinal = 121
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lang. Barrier"
      }

      when: {
        sql: ${TABLE}.hufinal = 126
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Supervisory Referral"
      }

      when: {
        sql: ${TABLE}.hufinal = 127
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Never Tried"
      }

      when: {
        sql: ${TABLE}.hufinal = 200
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "New Interview-Contacted"
      }

      when: {
        sql: ${TABLE}.hufinal = 201
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Capi Complete"
      }

      when: {
        sql: ${TABLE}.hufinal = 202
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Callbk. Needed"
      }

      when: {
        sql: ${TABLE}.hufinal = 203
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sufficient Partial - Precloseout"
      }

      when: {
        sql: ${TABLE}.hufinal = 204
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Suffic. Partial - At Closeout"
      }

      when: {
        sql: ${TABLE}.hufinal = 205
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Labor Force Compete, Suppl. Incomplete"
      }

      when: {
        sql: ${TABLE}.hufinal = 210
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Capi Complete Reinterview"
      }

      when: {
        sql: ${TABLE}.hufinal = 216
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No One Home"
      }

      when: {
        sql: ${TABLE}.hufinal = 217
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Temporarily Absent"
      }

      when: {
        sql: ${TABLE}.hufinal = 218
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hufinal = 219
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Occupied - Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 224
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces Occupied Or Under Age 14"
      }

      when: {
        sql: ${TABLE}.hufinal = 225
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Temp. Occupied W/Persons With Ure"
      }

      when: {
        sql: ${TABLE}.hufinal = 226
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vacant Regular"
      }

      when: {
        sql: ${TABLE}.hufinal = 227
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vacant-Storage Of Hhld Furniture"
      }

      when: {
        sql: ${TABLE}.hufinal = 228
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unfit, To Be Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 229
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Under Construction, Not Ready"
      }

      when: {
        sql: ${TABLE}.hufinal = 230
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Converted To Temp Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 231
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unoccupied Tent Or Trailer Site"
      }

      when: {
        sql: ${TABLE}.hufinal = 232
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Permit Granted-Construction Not Start"
      }

      when: {
        sql: ${TABLE}.hufinal = 233
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other-Specify"
      }

      when: {
        sql: ${TABLE}.hufinal = 240
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hufinal = 241
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hufinal = 242
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hufinal = 243
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Converted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hufinal = 244
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hufinal = 245
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hufinal = 246
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Built After April 1, 1980"
      }

      when: {
        sql: ${TABLE}.hufinal = 247
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unused Serial No./Listing Sheet Line"
      }

      when: {
        sql: ${TABLE}.hufinal = 248
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: huprscnt_census_1994 {
    label: "# of actual & attempted personal contacts"
    view_label: "Cohort Household Variables"
    type: tier
    tiers: [1, 26, 51, 76, 100]
    style: classic
    sql: CASE WHEN ${TABLE}.huprscnt\u0007census_1994 between 1 AND 99 THEN ${TABLE}.huprscnt\u0007census_1994 END ;;
  }

  dimension: hutypc_census_1994 {
    label: "Type c non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypc = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypc = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypc = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypc = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Demolished"
      }

      when: {
        sql: ${TABLE}.hutypc = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "House Or Trailer Moved"
      }

      when: {
        sql: ${TABLE}.hutypc = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Outside Segment"
      }

      when: {
        sql: ${TABLE}.hutypc = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Coverted To Perm. Business Or Storage"
      }

      when: {
        sql: ${TABLE}.hutypc = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Merged"
      }

      when: {
        sql: ${TABLE}.hutypc = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Condemned"
      }

      when: {
        sql: ${TABLE}.hutypc = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Unused Line Of Listing Sheet"
      }

      when: {
        sql: ${TABLE}.hutypc = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other - Specify"
      }
    }
  }

  dimension: hutypea_census_1994 {
    label: "Type a non-interview categories"
    view_label: "Cohort Household Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.hutypea = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.hutypea = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.hutypea = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Blank"
      }

      when: {
        sql: ${TABLE}.hutypea = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No One Home (NOH)"
      }

      when: {
        sql: ${TABLE}.hutypea = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Temporarily Absent (TA)"
      }

      when: {
        sql: ${TABLE}.hutypea = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused (REF)"
      }

      when: {
        sql: ${TABLE}.hutypea = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Occupied - Specify"
      }
    }
  }

  dimension: pefntvty_census_1994 {
    label: "Native country of father"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pefntvty = 57
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "United States"
      }

      when: {
        sql: ${TABLE}.pefntvty = 72
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Puerto Rico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 96
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Outlying Areas of the US(American Samoa, Guam, US Virgin Islands, Northern Marianas, Other US Terr)"
      }

      when: {
        sql: ${TABLE}.pefntvty = 102
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 103
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.pefntvty = 105
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 106
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.pefntvty = 108
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 109
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.pefntvty = 110
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.pefntvty = 116
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.pefntvty = 117
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.pefntvty = 119
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.pefntvty = 120
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.pefntvty = 126
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.pefntvty = 127
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.pefntvty = 128
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 129
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.pefntvty = 130
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.pefntvty = 132
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 134
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 136
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.pefntvty = 137
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 138
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.pefntvty = 139
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.pefntvty = 140
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 142
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.pefntvty = 147
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 148
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.pefntvty = 155
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 156
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.pefntvty = 180
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.pefntvty = 183
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 184
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.pefntvty = 185
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 192
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 195
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 200
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 202
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.pefntvty = 205
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.pefntvty = 206
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 207
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.pefntvty = 209
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.pefntvty = 210
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.pefntvty = 211
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 212
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.pefntvty = 213
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.pefntvty = 214
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.pefntvty = 215
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 216
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 217
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 218
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.pefntvty = 221
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.pefntvty = 222
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.pefntvty = 224
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 229
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 231
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.pefntvty = 233
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 234
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.pefntvty = 237
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.pefntvty = 238
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.pefntvty = 239
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.pefntvty = 240
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.pefntvty = 242
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.pefntvty = 245
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.pefntvty = 252
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.pefntvty = 253
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.pefntvty = 300
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.pefntvty = 301
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.pefntvty = 304
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.pefntvty = 310
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.pefntvty = 311
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.pefntvty = 312
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.pefntvty = 313
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.pefntvty = 314
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.pefntvty = 315
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.pefntvty = 316
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.pefntvty = 317
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Austria"
      }

      when: {
        sql: ${TABLE}.penatvty = 103
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Belgium"
      }

      when: {
        sql: ${TABLE}.penatvty = 105
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Czechoslovakia"
      }

      when: {
        sql: ${TABLE}.penatvty = 106
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Denmark"
      }

      when: {
        sql: ${TABLE}.penatvty = 108
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Finland"
      }

      when: {
        sql: ${TABLE}.penatvty = 109
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "France"
      }

      when: {
        sql: ${TABLE}.penatvty = 110
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Germany"
      }

      when: {
        sql: ${TABLE}.penatvty = 116
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Greece"
      }

      when: {
        sql: ${TABLE}.penatvty = 117
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hungary"
      }

      when: {
        sql: ${TABLE}.penatvty = 119
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ireland/Eire"
      }

      when: {
        sql: ${TABLE}.penatvty = 120
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Italy"
      }

      when: {
        sql: ${TABLE}.penatvty = 126
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Holland/Netherlands"
      }

      when: {
        sql: ${TABLE}.penatvty = 127
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Norway"
      }

      when: {
        sql: ${TABLE}.penatvty = 128
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Poland"
      }

      when: {
        sql: ${TABLE}.penatvty = 129
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Portugal"
      }

      when: {
        sql: ${TABLE}.penatvty = 130
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Azores"
      }

      when: {
        sql: ${TABLE}.penatvty = 132
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Romania"
      }

      when: {
        sql: ${TABLE}.penatvty = 134
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Spain"
      }

      when: {
        sql: ${TABLE}.penatvty = 136
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sweden"
      }

      when: {
        sql: ${TABLE}.penatvty = 137
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Switzerland"
      }

      when: {
        sql: ${TABLE}.penatvty = 138
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Great Britain"
      }

      when: {
        sql: ${TABLE}.penatvty = 139
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "England"
      }

      when: {
        sql: ${TABLE}.penatvty = 140
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Scotland"
      }

      when: {
        sql: ${TABLE}.penatvty = 142
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Northern Ireland"
      }

      when: {
        sql: ${TABLE}.penatvty = 147
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Yugoslavia"
      }

      when: {
        sql: ${TABLE}.penatvty = 148
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Europe"
      }

      when: {
        sql: ${TABLE}.penatvty = 155
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Czech Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 156
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Slovakia/Slovak Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 180
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "USSR"
      }

      when: {
        sql: ${TABLE}.penatvty = 183
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Latvia"
      }

      when: {
        sql: ${TABLE}.penatvty = 184
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lithuania"
      }

      when: {
        sql: ${TABLE}.penatvty = 185
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armenia"
      }

      when: {
        sql: ${TABLE}.penatvty = 192
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Russia"
      }

      when: {
        sql: ${TABLE}.penatvty = 195
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ukraine"
      }

      when: {
        sql: ${TABLE}.penatvty = 200
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Afghanistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 202
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bangladesh"
      }

      when: {
        sql: ${TABLE}.penatvty = 205
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Burma"
      }

      when: {
        sql: ${TABLE}.penatvty = 206
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cambodia"
      }

      when: {
        sql: ${TABLE}.penatvty = 207
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "China"
      }

      when: {
        sql: ${TABLE}.penatvty = 209
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hong Kong"
      }

      when: {
        sql: ${TABLE}.penatvty = 210
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "India"
      }

      when: {
        sql: ${TABLE}.penatvty = 211
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Indonesia"
      }

      when: {
        sql: ${TABLE}.penatvty = 212
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Iran"
      }

      when: {
        sql: ${TABLE}.penatvty = 213
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Iraq"
      }

      when: {
        sql: ${TABLE}.penatvty = 214
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Israel"
      }

      when: {
        sql: ${TABLE}.penatvty = 215
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Japan"
      }

      when: {
        sql: ${TABLE}.penatvty = 216
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Jordan"
      }

      when: {
        sql: ${TABLE}.penatvty = 217
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Korea/South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 218
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Korea/ South Korea"
      }

      when: {
        sql: ${TABLE}.penatvty = 221
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Laos"
      }

      when: {
        sql: ${TABLE}.penatvty = 222
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lebanon"
      }

      when: {
        sql: ${TABLE}.penatvty = 224
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Malaysia"
      }

      when: {
        sql: ${TABLE}.penatvty = 229
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pakistan"
      }

      when: {
        sql: ${TABLE}.penatvty = 231
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Philippines"
      }

      when: {
        sql: ${TABLE}.penatvty = 233
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Saudi Arabia"
      }

      when: {
        sql: ${TABLE}.penatvty = 234
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Singapore"
      }

      when: {
        sql: ${TABLE}.penatvty = 237
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Syria"
      }

      when: {
        sql: ${TABLE}.penatvty = 238
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Taiwan"
      }

      when: {
        sql: ${TABLE}.penatvty = 239
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Thailand"
      }

      when: {
        sql: ${TABLE}.penatvty = 240
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Turkey"
      }

      when: {
        sql: ${TABLE}.penatvty = 242
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vietnam"
      }

      when: {
        sql: ${TABLE}.penatvty = 245
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Asia"
      }

      when: {
        sql: ${TABLE}.penatvty = 252
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Middle East"
      }

      when: {
        sql: ${TABLE}.penatvty = 253
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Palestine"
      }

      when: {
        sql: ${TABLE}.penatvty = 300
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bermuda"
      }

      when: {
        sql: ${TABLE}.penatvty = 301
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Canada"
      }

      when: {
        sql: ${TABLE}.penatvty = 304
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "North America"
      }

      when: {
        sql: ${TABLE}.penatvty = 310
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Belize"
      }

      when: {
        sql: ${TABLE}.penatvty = 311
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Costa Rica"
      }

      when: {
        sql: ${TABLE}.penatvty = 312
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "El Salvador"
      }

      when: {
        sql: ${TABLE}.penatvty = 313
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Guatemala"
      }

      when: {
        sql: ${TABLE}.penatvty = 314
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Honduras"
      }

      when: {
        sql: ${TABLE}.penatvty = 315
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mexico"
      }

      when: {
        sql: ${TABLE}.penatvty = 316
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Nicaragua"
      }

      when: {
        sql: ${TABLE}.penatvty = 317
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Panama"
      }

      when: {
        sql: ${TABLE}.penatvty = 318
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Central America"
      }

      when: {
        sql: ${TABLE}.penatvty = 333
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bahamas"
      }

      when: {
        sql: ${TABLE}.penatvty = 334
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Barbados"
      }

      when: {
        sql: ${TABLE}.penatvty = 337
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cuba"
      }

      when: {
        sql: ${TABLE}.penatvty = 338
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Dominica"
      }

      when: {
        sql: ${TABLE}.penatvty = 339
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Dominican Republic"
      }

      when: {
        sql: ${TABLE}.penatvty = 340
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Grenada"
      }

      when: {
        sql: ${TABLE}.penatvty = 342
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Haiti"
      }

      when: {
        sql: ${TABLE}.penatvty = 343
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Jamaica"
      }

      when: {
        sql: ${TABLE}.penatvty = 351
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Trinidad & Tobago"
      }

      when: {
        sql: ${TABLE}.penatvty = 353
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Caribbean"
      }

      when: {
        sql: ${TABLE}.penatvty = 375
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Argentina"
      }

      when: {
        sql: ${TABLE}.penatvty = 376
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bolivia"
      }

      when: {
        sql: ${TABLE}.penatvty = 377
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Brazil"
      }

      when: {
        sql: ${TABLE}.penatvty = 378
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Chile"
      }

      when: {
        sql: ${TABLE}.penatvty = 379
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Colombia"
      }

      when: {
        sql: ${TABLE}.penatvty = 380
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ecuador"
      }

      when: {
        sql: ${TABLE}.penatvty = 383
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Guyana"
      }

      when: {
        sql: ${TABLE}.penatvty = 385
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Peru"
      }

      when: {
        sql: ${TABLE}.penatvty = 387
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Uruguay"
      }

      when: {
        sql: ${TABLE}.penatvty = 388
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Venezuela"
      }

      when: {
        sql: ${TABLE}.penatvty = 389
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "South America"
      }

      when: {
        sql: ${TABLE}.penatvty = 415
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Egypt"
      }

      when: {
        sql: ${TABLE}.penatvty = 417
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ethiopia"
      }

      when: {
        sql: ${TABLE}.penatvty = 421
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ghana"
      }

      when: {
        sql: ${TABLE}.penatvty = 427
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Kenya"
      }

      when: {
        sql: ${TABLE}.penatvty = 436
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Morocco"
      }

      when: {
        sql: ${TABLE}.penatvty = 440
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Nigeria"
      }

      when: {
        sql: ${TABLE}.penatvty = 449
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "South Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 462
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 468
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "North Africa"
      }

      when: {
        sql: ${TABLE}.penatvty = 501
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Australia"
      }

      when: {
        sql: ${TABLE}.penatvty = 507
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fiji"
      }

      when: {
        sql: ${TABLE}.penatvty = 514
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "New Zealand"
      }

      when: {
        sql: ${TABLE}.penatvty = 527
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pacific Islands"
      }

      when: {
        sql: ${TABLE}.penatvty = 555
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other"
      }
    }
  }

  dimension: prioelg_census_1994 {
    label: "Industry and occupation edit eligibility flag"
    view_label: "Cohort Labor Force Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prioelg = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prioelg = 0
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not Eligible For Edit"
      }

      when: {
        sql: ${TABLE}.prioelg = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Eligible For Edit"
      }
    }
  }

  dimension: peio1icd_census_1994 {
    label: "(main job)industry code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.peio1icd = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.peio1icd = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agricultural production, crops (01)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agricltrl production, livestock (02)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Veterinary services (074)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 20
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Landscape and horticultural services (078)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 30
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agricultural services, n.e.c. (071, 072, 075, 076)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 31
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry (08)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 32
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fishing, hunting, and trapping (09)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 40
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Metal mining (10)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 41
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Coal mining (12)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 42
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Oil and gas extraction (13)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 50
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Nonmetallic mining and quarrying, except fuel (14)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 60
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "CONSTRUCTION (15, 16, 17)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 100
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Meat products (201)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 101
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Dairy products (202)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 102
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Canned, frozen and preserved fruits and vegetables (203)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 110
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Grain mill products (204)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 111
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bakery products (205)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 112
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sugar and confectionery products (206)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 120
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Beverage industries (208)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 121
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous food preparations and kindred products (207, 209)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 122
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified food industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 130
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Tobacco manufactures (21)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 132
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Knitting mills (225)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 140
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Dyeing and finishing textiles, except wool and knit goods (226)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 141
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Carpets and rugs (227)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 142
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Yarn, thread, and fabric mills (221-224, 228)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 150
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous textile mill products (229)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 151
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Apparel and accessories, except knit (231-238)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 152
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous fabricated textile products (239)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 160
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pulp, paper, and paperboard mills (261-263)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 161
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous paper and pulp products (267)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 162
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Paperboard containers and boxes (265)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 171
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Newspaper publishing and printing (271)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 172
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Printing, publishing, and allied industries, except newspapers (272-279)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 180
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Plastics, synthetics, and resins (282)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 181
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Drugs (283)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 182
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Soaps and cosmetics (284)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 190
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Paints, varnishes, and rel. products (285)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 191
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agricultural chemicals (287)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 192
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Industrial and miscellaneous chemicals (281, 286, 289)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 200
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Petroleum refining (291)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 201
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous petroleum and coal products (295, 299)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 210
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Tires and inner tubes (301)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 211
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other rubber products, and plastics footwear and belting (302-306)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 212
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous plastics products (308)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 220
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Leather tanning and finishing (311)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 221
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Footwear, except rubber and plastic (313, 314)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 222
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Leather products, except footwear (315-317, 319)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 230
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Logging (241)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 231
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sawmills, planing mills, and millwork (242, 243)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 232
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Wood buildings and mobile homes (245)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 241
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous wood products (244, 249)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 242
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Furniture and fixtures (25)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 250
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Glass and glass products (321-323)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 251
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cement, concrete, gypsum, and plaster products (324, 327)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 252
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Structural clay products (325)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 261
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pottery and related products (326)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 262
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous nonmetallic mineral and stone products (328, 329)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 270
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Blast furnaces, steelworks, rolling and finishing mills (331)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 271
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Iron and steel foundries (332)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 272
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Primary aluminum industries (3334, part 334, 3353-3355, 3363, 3365)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 280
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other primary metal industries (3331, 3339, part 334, 3351, 3356, 3357, 3364, 3366, 3369, 339)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 281
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cutlery, handtools, and general hardware (342)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 282
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fabricated structural metal products (344)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 290
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Screw machine products (345)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 291
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Metal forgings and stampings (346)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 292
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ordnance (348)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 300
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Misc fabricated metal products (341, 343, 347, 349)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 301
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified metal industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 310
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engines and turbines (351)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 311
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm machinery and equipment (352)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 312
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction and material handling machines (353)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 320
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Metalworking machinery (354)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 321
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Office and accounting machines (3578, 3579)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 322
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Computers and rel. equipment (3571-3577)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 331
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machinery, except electrical, n.e.c. (355, 356, 358, 359)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 332
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified machinery"
      }

      when: {
        sql: ${TABLE}.peio1icd = 340
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Household appliances (363)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 341
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Radio, TV, and communication equipment (365, 366)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 342
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Electrical machinery, equipment, and supplies, n.e.c. (361, 362, 364, 367, 369)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 350
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified electrical machinery, equipment, and supplies"
      }

      when: {
        sql: ${TABLE}.peio1icd = 351
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Motor vehicles and motor vehicle equipment (371)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 352
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Aircraft and parts (372)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 360
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Ship and boat building and repairing (373)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 361
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Railroad locomotives and equipment (374)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 362
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Guided missiles, space vehicles, and parts (376)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 370
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cycles and miscellaneous transportation equipment (375, 379)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 371
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Scientific and controlling instruments (381, 382 except 3827)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 372
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Medical, dental, and optical instruments and supplies (3827, 384, 385)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 380
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Photographic equipment and supplies (386)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 381
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Watches, clocks, and clockwork operated devices (387)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 390
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Toys, amusement, and sporting goods (394)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 391
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous manufacturing industries (39 except 394)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 392
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not spec manufacturing industries"
      }

      when: {
        sql: ${TABLE}.peio1icd = 400
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Railroads (40)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 401
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Bus service and urban transit (41, except 412)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 402
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Taxicab service (412)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 410
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Trucking service (421, 423)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 411
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Warehousing and storage (422)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 412
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "U.S. Postal Service (43)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 420
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Water transportation (44)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 421
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Air transportation (45)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 422
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Pipe lines, except natural gas (46)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 432
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Services incidental to transportation (47)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 440
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Radio and television broadcasting and cable (483, 484)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 441
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Telephone communications (481)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 442
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Telegraph and miscellaneous communications services (482, 489)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 450
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Electric light and power (491)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 451
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Gas and steam supply systems (492, 496)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 452
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Electric and gas, and other combinations (493)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 470
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Water supply and irrigation (494, 497)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 471
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sanitary services (495)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 472
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified utilities"
      }

      when: {
        sql: ${TABLE}.peio1icd = 500
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Motor vehcls and equipment (501)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 501
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Furniture and home furnishings (502)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 502
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lumber and construction materials (503)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 510
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Professional and commercial equipment and supplies (504)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 511
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Metals and minerals, except petroleum (505)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 512
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Electrical goods (506)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 521
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hardware, plumbing and heating supplies (507)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 530
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machinery, equipment, and supplies (508)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 531
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Scrap and waste materials (5093)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 532
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous wholesale, durable goods (509 except 5093)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 540
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Paper and paper products (511)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 541
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Drugs, chemicals and allied products (512, 516)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 542
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Apparel, fabrics, and notions (513)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 550
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Groceries and related products (514)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 551
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm-product raw materials (515)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 552
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Petroleum products (517)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 560
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Alcoholic beverages (518)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 561
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm supplies (5191)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 562
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Misc wholesale, nondurable goods (5192-5199)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 571
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified wholesale trade"
      }

      when: {
        sql: ${TABLE}.peio1icd = 580
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lumber and building material retailing (521, 523)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 581
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hardware stores (525)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 582
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, Retail nurseries and garden (526)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 590
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mobile home dealers (527)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 591
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Department stores (531)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 592
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Variety stores (533)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 600
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, misc general merchandise (539)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 601
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Grocery stores (541)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 602
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, dairy products (545)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 610
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Retail bakeries (546)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 611
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Food stores, n.e.c. (542, 543, 544, 549)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 612
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Motor vehicle dealers (551, 552)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 620
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, Auto and home supply (553)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 621
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Gasoline service stations (554)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 622
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous vehicle dealers (555, 556, 557, 559)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 623
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, apparel and accessory, except shoe (56, except 566)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 630
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Shoe stores (566)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 631
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, furniture and home furnishings (571)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 632
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, household appliance (572)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 633
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, radio, TV, and computer (5731, 5734)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 640
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Music stores (5735, 5736)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 641
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Eating and drinking places (58)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 642
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Drug stores (591)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 650
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Liquor stores (592)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 651
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, sporting goods, bicycles, and hobby (5941, 5945, 5946)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 652
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, Book and stationery (5942, 5943)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 660
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Jewelry stores (5944)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 661
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Gift, novelty, and souvenir shops (5947)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 662
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sewing, needlework and piece goods stores (5949)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 663
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Catalog and mail order houses (5961)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 670
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vending machine operators (5962)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 671
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Direct selling establishments (5963)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 672
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fuel dealers (598)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 681
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Retail florists (5992)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 682
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Stores, Miscellaneous retail (593, 5948, 5993-5995, 5999)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 691
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not specified retail trade"
      }

      when: {
        sql: ${TABLE}.peio1icd = 700
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Banking (60 except 603 and 606)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 701
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Savings institutions, including credit unions (603, 606)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 702
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Credit agencies, n.e.c. (61)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 710
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Security, commodity brokerage, and investment companies (62, 67)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 711
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Insurance (63, 64)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 712
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Real estate, including real estate-insurance offices (65)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 721
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Advertising (731)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 722
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Services to dwellings and other buildings (734)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 731
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personnel supply services (736)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 732
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Computer and data processing services (737)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 740
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Detective and protective services (7381, 7382)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 741
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Business services, n.e.c. (732, 733, 735, 7383-7389)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 742
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Automotive rental and leasing, w/out drivers (751)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 750
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Automotive parking and carwashes (752, 7542)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 751
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Automotive repair and rel. services (753, 7549)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 752
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Electrical repair shops (762, 7694)"
      }

      when: {
        sql: ${TABLE}.peio1icd = 760
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Misc repair services (763, 764, 7692, 7699)"
      }

      when: {
        sql: ${TABLE}.peio1icd =
"
      }

      when: {
        sql: ${TABLE}.peio2icd = 831
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "HOSPITALS (806)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 832
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Nursing and personal care facilities (805)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 840
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health services, n.e.c. (807, 808, 809)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 841
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Legal services (81)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 842
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Elementary and secondary schools (821)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 850
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Colleges and universities (822)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 851
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Vocational schools (824)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 852
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Libraries (823)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 860
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Educational services, n.e.c. (829)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 861
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Job training and vocational rehabilitation services (833)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 862
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Child day care services (part 835)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 863
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Family child care homes (part 835)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 870
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Residential care facilities, w/out nursing (836)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 871
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Social services, n.e.c. (832, 839)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 872
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Museums, art galleries, and zoos (84)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 873
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Labor unions (863)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 880
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Religious organizations (866)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 881
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Membership organizations, n.e.c. (861, 862, 864, 865, 869)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 882
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineering, architectural, and surveying services (871)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 890
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Accounting, auditing, and bookkeeping services (872)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 891
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Research, development, and testing services (873)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 892
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Management and public relations services (874)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 893
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Miscellaneous professional and rel. services (899)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 900
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Executive and legislative offices (911-913)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 901
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "General government, n.e.c. (919)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 910
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Justice, public order, and safety (92)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 921
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Public finance, taxation, and monetary policy (93)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 922
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Human resources programs administration(94)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 930
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Environmental quality and housing programs administration(95)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 931
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Economic programs administration(96)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 932
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "National security and international affairs (97)"
      }

      when: {
        sql: ${TABLE}.peio2icd = 991
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Assigned to persons whose labor force status is unemployed and whose last job was Armed Forces"
      }
    }
  }

  dimension: ptio2ocd_census_1994 {
    label: "(second job)occupation code"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.ptio2ocd = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "chief executives and general administrators, public administration (112)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "administrators and officials, public administration (1132-1139)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "administrators, protective services (1131)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "financial managers (122)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "personnel and labor relations managers (123)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "purchasing managers (124)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers, marketing, advertising, and public relations (125)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "admin, education and rel. fields (128)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 15
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers, medicine and health (131)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers, food serving and lodging establishments (1351)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers, properties and real estate (1353)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 19
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "funeral directors (part 1359)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers, service organizations, n.e.c. (127, 1352, 1354, part 1359)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "managers and administrators, n.e.c. (121, 126, 132-1343, 136-139)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "accountants and auditors (1412)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 24
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "underwriters (1414)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 25
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "other financial officers (1415, 1419)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 26
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "management analysts (142)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 27
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "personnel, training, and labor relations specialists (143)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 28
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "purchasing agents and buyers, farm products (1443)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 29
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "buyers, wholesale and retail trade except farm products (1442)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 33
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "purch. agents and buyers, n.e.c. (1449)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 34
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "business and promotion agents (145)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 35
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "construction inspectors (1472)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 36
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "inspectors and compliance officers, except construction (1473)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 37
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "management rel. occupations, n.e.c. (149)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 43
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "architects (161)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 44
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "aerospace engineers(1622)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 45
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "metallurgical and materials engineers (1623)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 46
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "mining engineers (1624)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 47
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "petroleum engineers (1625)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 48
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "chemical engineers (1626)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 49
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "nuclear engineers (1627)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 53
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "civil engineers (1628)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 54
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "agricultural engineers (1632)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 55
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineers, electrical and electronic (1633, 1636)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 56
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineers, industrial (1634)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 57
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineers, mechanical (1635)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 58
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "marine and naval architects (1637)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 59
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "engineers, n.e.c. (1639)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 63
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "surveyors and mapping scientists (164)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 64
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "computer systems analysts and scientists (171)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 65
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "operations and systems researchers and analysts (172)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 66
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Actuaries (1732)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 67
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Statisticians (1733)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 68
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mathematical scientists, n.e.c. (1739)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 69
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Physicists and astronomers (1842, 1843)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 73
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Chemists, except biochemists (1845)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 74
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Atmospheric and space scientists (1846)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 75
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Geologists and geodesists (1847)"
      }

      when: {
        sql: ${TABLE}.ptio2ocd = 76
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "
"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 45
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Social services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 46
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other professional services"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 47
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry & fisheries"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 48
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Justice, public order & safety"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 49
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Admin of human resource programs"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 50
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "National security & internal affairs"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 51
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other public administration"
      }

      when: {
        sql: ${TABLE}.prdtind2 = 52
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prdtocc1_census_1994 {
    label: "(main job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc1 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Officials & administrators, pub. admin."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other executive, admin. & managerial"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Management related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mathematical and computer scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Natural Scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health diagnosing occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health assessment and treatment occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Teachers, college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Teachers, except college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lawyers and judges"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other professional specialty occs."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health technologists and technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineering and science technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 15
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Technicians, exc. health,engin.&science"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 16
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Supervisors and proprietors, sales occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales reps, finance and business serv."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales reps, commodities, exc. retail"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 19
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales workers, retail & personal serv."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 20
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales related occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Supervisors, admin. support"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Computer equipment operators"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Secretaries, stenographers, and typists"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 24
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Financial records processing"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 25
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mail and message distributing"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 26
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other admin support, inc. clerical"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 27
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private household service occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 28
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Protective service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 29
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Food service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 30
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 31
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cleaning and building service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 32
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personal service"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 33
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mechanics and repairers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 34
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction trades"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 35
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other precision prod., craft, & repair"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 36
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machine opertrs and tenders,exc precis."
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 37
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fabricatrs,assemblrs,inspectrs,samplrs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 38
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Motor vehicle operators"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 39
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other transp. & material moving occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 40
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction laborers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 41
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Freight, stock & materials handlers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 42
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Oth handlrs,equip.cleanrs,helprs,labrrs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 43
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm operators and managers"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 44
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm workers and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 45
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry and fishing occs"
      }

      when: {
        sql: ${TABLE}.prdtocc1 = 46
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed forces"
      }
    }
  }

  dimension: prdtocc2_census_1994 {
    label: "(second job)detailed occupation groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prdtocc2 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Officials & administrators, pub. admin."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other executive, admin. & managerial"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Management related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mathematical and computer scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Natural Scientists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health diagnosing occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health assessment and treatment occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Teachers, college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Teachers, except college and university"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Lawyers and judges"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other professional specialty occs."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health technologists and technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Engineering and science technicians"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 15
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Technicians, exc. health,engin.&science"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 16
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Supervisors and proprietors, sales occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales reps, finance and business serv."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales reps, commodities, exc. retail"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 19
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales workers, retail & personal serv."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 20
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales related occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Supervisors, admin. support"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Computer equipment operators"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Secretaries, stenographers, and typists"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 24
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Financial records processing"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 25
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mail and message distributing"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 26
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other admin support, inc. clerical"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 27
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private household service occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 28
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Protective service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 29
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Food service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 30
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Health service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 31
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Cleaning and building service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 32
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personal service"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 33
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mechanics and repairers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 34
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction trades"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 35
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other precision prod., craft, & repair"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 36
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machine opertrs and tenders,exc precis."
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 37
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Fabricatrs,assemblrs,inspectrs,samplrs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 38
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Motor vehicle operators"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 39
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other transp. & material moving occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 40
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction laborers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 41
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Freight, stock & materials handlers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 42
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Oth handlrs,equip.cleanrs,helprs,labrrs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 43
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm operators and managers"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 44
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farm workers and related occupations"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 45
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry and fishing occs"
      }

      when: {
        sql: ${TABLE}.prdtocc2 = 46
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed forces"
      }
    }
  }

  dimension: prmjind1_census_1994 {
    label: "(main job)industry,major groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind1 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agriculture"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Manufacturing - Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mfg. - Non-Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Utilities And Sanitary Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Finance, Insurance, And Real Estate"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private Households"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Business, Auto And Repair Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personal Services, Exc. Private Hhlds"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 15
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Entertainment And Recreation Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 16
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Medical Services, Exc. Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Educational Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 19
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Social Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 20
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Professional Services"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry And Fisheries"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Public Administration"
      }

      when: {
        sql: ${TABLE}.prmjind1 = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjind2_census_1994 {
    label: "(second job)industry,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjind2 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Agriculture"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mining"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Construction"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Manufacturing - Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mfg. - Non-Durable Goods"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Transportation"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Communications"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Utilities And Sanitary Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Wholesale Trade"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Retail Trade"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Finance, Insurance, And Real Estate"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private Households"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Business, Auto And Repair Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Personal Services, Exc. Private Hhlds"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 15
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Entertainment And Recreation Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 16
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 17
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Medical Services, Exc. Hospitals"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 18
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Educational Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 19
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Social Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 20
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other Professional Services"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 21
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Forestry And Fisheries"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 22
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Public Administration"
      }

      when: {
        sql: ${TABLE}.prmjind2 = 23
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc1_census_1994 {
    label: "(main job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc1 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Executive, Admin, & Managerial Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Professional Specialty Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Technicians And Related Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Admin. Support Occs, Incl. Clerical"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private Household Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Protective Service Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Service Occs, Exc. Protective & Hhld"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Precision Prod., Craft & Repair Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machine Opers, Assemblers & Inspectors"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Transportation And Material Moving Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Handlers,equip Cleaners,helpers,laborrs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farming, Forestry And Fishing Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc1 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocc2_census_1994 {
    label: "(second job)occupation,major groups-recode"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocc2 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Executive, Admin, & Managerial Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Professional Specialty Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Technicians And Related Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Sales Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Admin. Support Occs, Incl. Clerical"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Private Household Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 7
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Protective Service Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 8
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Service Occs, Exc. Protective & Hhld"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Precision Prod., Craft & Repair Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 10
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Machine Opers, Assemblers & Inspectors"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 11
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Transportation And Material Moving Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 12
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Handlers,equip Cleaners,helpers,laborrs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 13
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farming, Forestry And Fishing Occs"
      }

      when: {
        sql: ${TABLE}.prmjocc2 = 14
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Armed Forces"
      }
    }
  }

  dimension: prmjocgr_census_1994 {
    label: "(main job)occupation,4 groups"
    view_label: "Cohort Industry"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prmjocgr = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In Universe, Met No Conditions To Assign"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Mgr. & Prof, Tech, Sales & Support Occs"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Service Occupations"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Production, Craft, Repair, Operators"
      }

      when: {
        sql: ${TABLE}.prmjocgr = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Farming, Forestry & Fishing Occupations"
      }
    }
  }

  dimension: perace_census_1994 {
    label: "Race of respondent"
    view_label: "Cohort Demographic Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.perace = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "White"
      }

      when: {
        sql: ${TABLE}.perace = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Black"
      }

      when: {
        sql: ${TABLE}.perace = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "American Indian, Aleut, Eskimo"
      }

      when: {
        sql: ${TABLE}.perace = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Asian or Pacific Islander"
      }

      when: {
        sql: ${TABLE}.perace = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other"
      }
    }
  }

  dimension: prvel {
    label: "Voting eligibility recode"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.prvel = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Eligible for voting and registration questions"
      }

      when: {
        sql: ${TABLE}.prvel = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "In universe, but no data available"
      }

      when: {
        sql: ${TABLE}.prvel = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not eligible for voting and registration questions"
      }
    }
  }

  dimension: pes3_census_1994 {
    label: "Vote in the november _ election?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes3 = -9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No response (N/A)"
      }

      when: {
        sql: ${TABLE}.pes3 = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes3 = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes3 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes3 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes3 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No"
      }
    }
  }

  dimension: pes4_census_1994 {
    label: "Registered to vote in the november _ election?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes4 = -9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No response (N/A)"
      }

      when: {
        sql: ${TABLE}.pes4 = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes4 = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes4 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes4 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Yes"
      }

      when: {
        sql: ${TABLE}.pes4 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No"
      }
    }
  }

  dimension: pes5_census_1994 {
    label: "Time of day voted?"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes5 = -9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No response (N/A)"
      }

      when: {
        sql: ${TABLE}.pes5 = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes5 = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes5 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes5 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Before noon"
      }

      when: {
        sql: ${TABLE}.pes5 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Noon to 4 p.m."
      }

      when: {
        sql: ${TABLE}.pes5 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "4 p.m. to 6 p.m."
      }

      when: {
        sql: ${TABLE}.pes5 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "After 6 p.m."
      }

      when: {
        sql: ${TABLE}.pes5 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Voted absentee"
      }
    }
  }

  dimension: pes6_census_1994 {
    label: "Time at current address"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes6 = -9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No response (N/A)"
      }

      when: {
        sql: ${TABLE}.pes6 = -3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Refused"
      }

      when: {
        sql: ${TABLE}.pes6 = -2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Don't Know"
      }

      when: {
        sql: ${TABLE}.pes6 = -1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Not in Universe"
      }

      when: {
        sql: ${TABLE}.pes6 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Less than 1 month"
      }

      when: {
        sql: ${TABLE}.pes6 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "1-6 months"
      }

      when: {
        sql: ${TABLE}.pes6 = 3
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "7-11 months"
      }

      when: {
        sql: ${TABLE}.pes6 = 4
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "1-2 years"
      }

      when: {
        sql: ${TABLE}.pes6 = 5
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "3-4 years"
      }

      when: {
        sql: ${TABLE}.pes6 = 6
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "5 years or longer"
      }
    }
  }

  dimension: pes7_census_1994 {
    label: "Self or other reported for person"
    view_label: "Cohort Voting and Registration Supplement Variables"
    type: string

    case: {
      when: {
        sql: ${TABLE}.pes7 = -9
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "No response (N/A)"
      }

      when: {
        sql: ${TABLE}.pes7 = 1
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Self"
      }

      when: {
        sql: ${TABLE}.pes7 = 2
          AND ${TABLE}.src_table in ('census_1994')
           ;;
        label: "Other"
      }
    }
  }
}
